# v0.3 Phase A — M:N Relations & Composite Keys Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** blueprint v1 의 `relations[]` 가 N:M (junction) · 복합 PK · 복합 FK 케이스를 정확히 모델링하고 Stage 2 가 이를 DDL로 정확히 emit한다.

**Architecture:**
- Junction table 은 magical type 이 아니라 **일반 entity** (사용자가 직접 선언, 두 1:N relation으로 연결, junction PK = 두 FK 합) — Karpathy 정신 (명시적·파일 기반).
- 복합 PK 는 `columns[].pk: true` 가 여러 컬럼인 경우. 복합 FK 는 `relations[].fk.column` 이 list 인 경우. 둘 다 v0.2 spec의 자연 확장 (하위 호환).
- Stage 1 validator + Stage 2 DDL 양쪽이 list-form column 을 정확히 처리. 기존 single-column 케이스는 동작 유지.
- polymorphic FK 는 v0.3 범위 밖 (YAGNI). 별도 task로 후순위.

**Tech Stack:** Python 3.11+, PyYAML, Jinja2, pytest. PostgreSQL + HSQLDB dialect (기존).

**Repos:**
- Stage 1: `D:\AI\workspace\andrej-karpathy-rdb-skill`
- Stage 2: `D:\AI\workspace\andrej-karpathy-rdb-ddl`

---

## File Structure

| 파일 | 책임 | 변경 유형 |
|---|---|---|
| `andrej-karpathy-rdb-skill/scripts/rdb_index.py` | V001/V002 보강 (복합 PK / FK list) | Modify |
| `andrej-karpathy-rdb-skill/scripts/rdb_index.py` | V005 신규 (junction table 인지) | Modify (add) |
| `andrej-karpathy-rdb-skill/.claude/skills/karpathy-rdb/references/blueprint-spec.md` | 복합 PK·복합 FK·junction 패턴 문서화 | Modify |
| `andrej-karpathy-rdb-skill/tests/test_relations_mn.py` | Stage 1 신규 테스트 | Create |
| `andrej-karpathy-rdb-ddl/scripts/ddl_gen.py` | `_fk_column` → `_fk_columns` (list 반환), 복합 FK 처리 | Modify |
| `andrej-karpathy-rdb-ddl/templates/postgres/constraints.sql.j2` | 복합 컬럼 ADD CONSTRAINT 렌더링 | Modify |
| `andrej-karpathy-rdb-ddl/templates/hsqldb/constraints.sql.j2` | 동일 | Modify |
| `andrej-karpathy-rdb-ddl/.claude/skills/karpathy-rdb-ddl/references/ddl-spec.md` | 복합 FK 동작 1줄 명시 | Modify |
| `andrej-karpathy-rdb-ddl/tests/test_ddl_gen_composite_fk.py` | Stage 2 신규 테스트 | Create |
| 양 레포 합동: `business-fullstack-creater/tests/golden/v0.3-phase-a-mn.yaml` | E2E golden fixture | Create |

---

## Task 1: Stage 1 — V001 보강 (복합 PK 인지)

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-skill\scripts\rdb_index.py:55-67`
- Test: `D:\AI\workspace\andrej-karpathy-rdb-skill\tests\test_relations_mn.py` (신규)

현재 V001은 PK 1개 이상 있으면 통과 — 복합 PK도 그대로 통과. 변경 불필요. 다만 **복합 PK 인지 여부** (`pk_columns` 헬퍼)를 노출해 후속 V002·V005 에서 재사용한다.

- [ ] **Step 1: 신규 테스트 파일 생성 + 실패 테스트 작성**

```python
# tests/test_relations_mn.py
"""M:N junction · 복합 PK · 복합 FK 테스트."""
from rdb_index import _pk_columns, validate_v001_pk


def test_pk_columns_single():
    e = {"name": "x", "columns": [{"name": "id", "pk": True}, {"name": "n"}]}
    assert _pk_columns(e) == ["id"]


def test_pk_columns_composite():
    e = {"name": "x", "columns": [
        {"name": "a_id", "pk": True}, {"name": "b_id", "pk": True}, {"name": "n"}
    ]}
    assert _pk_columns(e) == ["a_id", "b_id"]


def test_pk_columns_none():
    e = {"name": "x", "columns": [{"name": "n"}]}
    assert _pk_columns(e) == []
```

- [ ] **Step 2: 테스트 실패 확인**

Run (rdb-skill 디렉터리에서):
```
python -m pytest tests/test_relations_mn.py -v
```
Expected: `ImportError: cannot import name '_pk_columns'`

- [ ] **Step 3: `_pk_columns` 헬퍼 추가**

`rdb_index.py` 의 `validate_v001_pk` 직전(line ~50)에 추가:

```python
def _pk_columns(entity):
    """Return list of column names where pk: true. Empty if none."""
    return [c.get("name") for c in (entity.get("columns") or []) if c.get("pk") is True]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_relations_mn.py -v`
Expected: 3 passed.

- [ ] **Step 5: 회귀 테스트**

Run: `python -m pytest -v`
Expected: 기존 테스트 모두 PASS (V001은 동작 무변경).

- [ ] **Step 6: per-file commit**

```
cd D:\AI\workspace\andrej-karpathy-rdb-skill
git add scripts/rdb_index.py
git commit -m "feat(rdb_index): add _pk_columns helper for composite PK"
git add tests/test_relations_mn.py
git commit -m "test(relations): _pk_columns helper coverage"
```

---

## Task 2: Stage 1 — V002 보강 (FK column이 list일 때 처리)

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-skill\scripts\rdb_index.py:69-106` (validate_v002_fk_targets)
- Test: 동일 파일에 추가

현재 V002는 `fk_column` 이 string 만 가정. v0.3은 **list 도 허용** — list 의 각 원소가 모두 to-entity의 컬럼이어야 함. 단일 컬럼은 string OR 1-원소 list 모두 OK (하위 호환).

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_relations_mn.py` 에 append:

```python
from rdb_index import validate_v002_fk_targets


def _entities(*decls):
    return {name: {"name": name, "columns": cols} for name, cols in decls}


def test_v002_composite_fk_all_columns_present():
    entities = _entities(
        ("address", [
            {"name": "user_id", "pk": True},
            {"name": "seq", "pk": True},
            {"name": "city"},
        ]),
    )
    rel = {"from": "user", "to": "address", "fk_column": ["user_id", "seq"]}
    assert validate_v002_fk_targets(rel, entities) == []


def test_v002_composite_fk_missing_column():
    entities = _entities(
        ("address", [
            {"name": "user_id", "pk": True},
            {"name": "city"},
        ]),
    )
    rel = {"from": "user", "to": "address", "fk_column": ["user_id", "missing"]}
    errs = validate_v002_fk_targets(rel, entities)
    assert len(errs) == 1
    assert errs[0]["code"] == "V002"
    assert "missing" in errs[0]["message"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_relations_mn.py::test_v002_composite_fk_all_columns_present -v`
Expected: FAIL — 현재는 list가 fk_column에 들어오면 `fk_column not in col_names` 가 항상 True가 되어 잘못된 에러 메시지 반환.

- [ ] **Step 3: `validate_v002_fk_targets` 수정**

`rdb_index.py` 의 line 97-105 블록을 다음으로 교체:

```python
    if fk_column:
        col_names = {c.get("name") for c in cols}
        # fk_column may be string (single FK) or list (composite FK)
        fk_cols = [fk_column] if isinstance(fk_column, str) else list(fk_column)
        missing = [c for c in fk_cols if c not in col_names]
        if missing:
            return [{
                "code": "V002",
                "level": "ERROR",
                "target": target,
                "message": f"fk_column {missing} 이(가) {to_name}에 존재하지 않음",
            }]
    return []
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_relations_mn.py -v`
Expected: 5 passed (이전 3 + 신규 2).

- [ ] **Step 5: 회귀 테스트**

Run: `python -m pytest -v`
Expected: 기존 테스트 모두 PASS — string fk_column 케이스는 1-원소 list로 동등 처리됨.

- [ ] **Step 6: per-file commit**

```
git add scripts/rdb_index.py
git commit -m "feat(rdb_index): V002 supports composite fk_column (list)"
git add tests/test_relations_mn.py
git commit -m "test(relations): V002 composite FK cases"
```

---

## Task 3: Stage 1 — V005 junction table 인지

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-skill\scripts\rdb_index.py` (새 validator + _collect_relations 통합)
- Test: 동일 테스트 파일

**정의**: junction entity = "PK 컬럼이 모두 FK 컬럼이고, PK 외 컬럼이 0개 또는 메타 컬럼만 (created_at, updated_at)" 인 entity.

V005 (INFO 수준): junction entity 가 감지되면 양쪽 relation 의 cardinality 를 자동으로 `N:M` 으로 표시하고 INFO 레벨 메시지를 emit. ERROR/WARN 아님 — 사용자가 의도적으로 junction 을 만든 경우 정보 표시.

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_relations_mn.py` 에 append:

```python
from rdb_index import is_junction_entity, validate_v005_junction


def test_is_junction_entity_pure():
    e = {"name": "user_role", "columns": [
        {"name": "user_id", "pk": True},
        {"name": "role_id", "pk": True},
    ]}
    fk_columns = {"user_id", "role_id"}
    assert is_junction_entity(e, fk_columns) is True


def test_is_junction_entity_with_meta_only():
    e = {"name": "user_role", "columns": [
        {"name": "user_id", "pk": True},
        {"name": "role_id", "pk": True},
        {"name": "created_at"}, {"name": "updated_at"},
    ]}
    fk_columns = {"user_id", "role_id"}
    assert is_junction_entity(e, fk_columns) is True


def test_is_junction_entity_with_extra_data_column():
    e = {"name": "user_role", "columns": [
        {"name": "user_id", "pk": True},
        {"name": "role_id", "pk": True},
        {"name": "granted_by"},
    ]}
    fk_columns = {"user_id", "role_id"}
    assert is_junction_entity(e, fk_columns) is False


def test_v005_emits_info_for_junction():
    entity = {"name": "user_role", "columns": [
        {"name": "user_id", "pk": True}, {"name": "role_id", "pk": True}
    ]}
    fk_columns = {"user_id", "role_id"}
    msgs = validate_v005_junction(entity, fk_columns)
    assert len(msgs) == 1
    assert msgs[0]["code"] == "V005"
    assert msgs[0]["level"] == "INFO"
    assert "N:M" in msgs[0]["message"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_relations_mn.py -v -k junction`
Expected: ImportError.

- [ ] **Step 3: `is_junction_entity` + `validate_v005_junction` 추가**

`rdb_index.py` 의 V003 (line ~118) 직전에 추가:

```python
META_COLUMNS = frozenset(["created_at", "updated_at", "created_by", "updated_by"])


def is_junction_entity(entity, fk_columns):
    """Junction = all PK cols are FK cols AND non-PK cols are meta-only.

    fk_columns: set of column names that participate in any outgoing FK.
    """
    pk = _pk_columns(entity)
    if len(pk) < 2:
        return False
    if not all(c in fk_columns for c in pk):
        return False
    non_pk = [c.get("name") for c in entity.get("columns") or [] if not c.get("pk")]
    return all(n in META_COLUMNS for n in non_pk)


def validate_v005_junction(entity, fk_columns):
    if not is_junction_entity(entity, fk_columns):
        return []
    pk = _pk_columns(entity)
    return [{
        "code": "V005",
        "level": "INFO",
        "target": f"entity:{entity.get('name')}",
        "message": f"junction table 감지 — N:M 관계 (PK={pk})",
    }]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_relations_mn.py -v -k junction`
Expected: 4 passed.

- [ ] **Step 5: `_collect_relations` 또는 `build_blueprint`에 V005 통합**

`build_blueprint` (line ~389) 에서 `_collect_relations` 호출 직후, fk_columns set을 모은 뒤 각 entity 에 대해 `validate_v005_junction` 결과를 `validation_results.extend(...)` 로 누적.

`build_blueprint` 의 validation 합산 부분(현재 entities·extends 에러를 합치는 곳 근처) 에 다음 블록 추가:

```python
    # V005: junction table detection (after relations collected)
    fk_cols_used = set()
    for r in blueprint_rels:
        fk_obj = r.get("fk")
        col = fk_obj.get("column") if isinstance(fk_obj, dict) else fk_obj
        if isinstance(col, list):
            fk_cols_used.update(col)
        elif col:
            fk_cols_used.add(col)
    for ename, fm in data["entities"].items():
        validation_results.extend(validate_v005_junction(fm, fk_cols_used))
```

(정확한 변수명·삽입 위치는 현재 build_blueprint 본체 확인 후 조정 — 실행자는 line 389 부근의 validation 누적 패턴을 따른다.)

- [ ] **Step 6: 회귀 테스트 + 골든 fixture 영향 확인**

Run: `python -m pytest -v`
Expected: 기존 테스트 모두 PASS. V005 는 INFO 레벨이라 `validation.passed` 에 영향 없어야 함.

- [ ] **Step 7: per-file commit**

```
git add scripts/rdb_index.py
git commit -m "feat(rdb_index): V005 detect junction table for N:M"
git add tests/test_relations_mn.py
git commit -m "test(relations): junction table detection"
```

---

## Task 4: Stage 2 — `_fk_columns` (list 반환) + `_gather_foreign_keys` 보강

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-ddl\scripts\ddl_gen.py:69-118`
- Test: `D:\AI\workspace\andrej-karpathy-rdb-ddl\tests\test_ddl_gen_composite_fk.py` (신규)

**API 변경**: 기존 `_fk_column(rel) -> str` 을 `_fk_columns(rel) -> list[str]` 으로 교체. single-string 입력은 1-원소 list로 wrap. owner-detection 로직은 **list 의 모든 컬럼이 한쪽 entity 에 존재하는지** 체크.

생성되는 FK constraint 의 name 은 `fk_<table>__<col1>_<col2>` (다중 컬럼은 `__` 로 분리).

- [ ] **Step 1: 신규 테스트 파일**

```python
# tests/test_ddl_gen_composite_fk.py
"""복합 FK constraint 생성 테스트."""
from ddl_gen import _fk_columns, _gather_foreign_keys


def test_fk_columns_single_string():
    assert _fk_columns({"fk": "user_id"}) == ["user_id"]


def test_fk_columns_nested_string():
    assert _fk_columns({"fk": {"column": "user_id"}}) == ["user_id"]


def test_fk_columns_nested_list():
    assert _fk_columns({"fk": {"column": ["a_id", "b_id"]}}) == ["a_id", "b_id"]


def test_fk_columns_missing():
    assert _fk_columns({}) == []


def test_gather_composite_fk_constraint_name_and_columns():
    blueprint = {
        "relations": [{
            "from": "user_role", "to": "user",
            "fk": {"column": ["user_id"], "on_delete": "cascade"},
        }],
    }
    by_name = {
        "user_role": {
            "name": "user_role", "schema": "app", "table": "user_role",
            "columns": [{"name": "user_id", "pk": True}, {"name": "role_id", "pk": True}],
        },
        "user": {
            "name": "user", "schema": "app", "table": "user",
            "columns": [{"name": "user_id", "pk": True}],
        },
    }
    fks = _gather_foreign_keys(blueprint, by_name)
    assert len(fks) == 1
    assert fks[0]["columns"] == ["user_id"]
    assert fks[0]["ref_columns"] == ["user_id"]
    assert fks[0]["name"] == "fk_user_role__user_id"
```

- [ ] **Step 2: 테스트 실패 확인**

Run (rdb-ddl 디렉터리에서): `python -m pytest tests/test_ddl_gen_composite_fk.py -v`
Expected: `ImportError: cannot import name '_fk_columns'` 또는 KeyError on `columns`.

- [ ] **Step 3: `ddl_gen.py` 수정 — `_fk_columns` 도입 + `_gather_foreign_keys` 갱신**

line 69-118 (`_fk_column` ~ `_gather_foreign_keys`) 를 다음으로 교체:

```python
def _fk_columns(relation: dict):
    """Return FK column(s) as a list. Accepts:
    - nested {fk: {column: 'x'}}  → ['x']
    - nested {fk: {column: ['a','b']}} → ['a','b']
    - legacy flat {fk: 'x'} → ['x']
    """
    fk = relation.get("fk")
    if isinstance(fk, dict):
        col = fk.get("column")
    else:
        col = fk
    if col is None:
        return []
    if isinstance(col, list):
        return list(col)
    return [col]


def _fk_on_delete(relation: dict) -> str:
    fk = relation.get("fk")
    if isinstance(fk, dict) and fk.get("on_delete"):
        raw = fk["on_delete"]
    else:
        raw = relation.get("on_delete") or "restrict"
    return _ON_DELETE_SQL.get(str(raw).lower(), str(raw).upper())


def _gather_foreign_keys(blueprint: dict, by_name):
    """Identify which side actually owns the FK column(s).

    blueprint-spec convention: from=parent (1), to=child (N, holds FK).
    Legacy convention: from=child (N, holds FK), to=parent (1).
    Owner is whichever side has ALL fk_cols among its columns; fall back to
    `from` to preserve legacy behavior when neither has them.
    """
    fks = []
    for r in blueprint.get("relations") or []:
        a = by_name.get(r.get("from"))
        b = by_name.get(r.get("to"))
        if not a or not b:
            continue
        fk_cols = _fk_columns(r)
        if not fk_cols:
            continue
        a_names = {c.get("name") for c in (a.get("columns") or [])}
        b_names = {c.get("name") for c in (b.get("columns") or [])}
        if all(c in b_names for c in fk_cols) and not all(c in a_names for c in fk_cols):
            owner, parent = b, a
        else:
            owner, parent = a, b
        ref_pks = [c["name"] for c in parent.get("columns") or [] if c.get("pk")]
        if not ref_pks:
            continue
        # composite FK references composite PK — assume positional pairing
        ref_cols = ref_pks if len(fk_cols) > 1 else [ref_pks[0]]
        fks.append({
            "name": f"fk_{owner['table']}__{'_'.join(fk_cols)}",
            "src_schema": owner["schema"], "src_table": owner["table"],
            "columns": fk_cols,
            "ref_schema": parent["schema"], "ref_table": parent["table"],
            "ref_columns": ref_cols,
            "on_delete": _fk_on_delete(r),
        })
    return fks
```

- [ ] **Step 4: 신규 테스트 통과 확인**

Run: `python -m pytest tests/test_ddl_gen_composite_fk.py -v`
Expected: 5 passed.

- [ ] **Step 5: 기존 테스트 회귀 — 키 이름 변경 영향 (column → columns, ref_column → ref_columns)**

Run: `python -m pytest tests/test_ddl_gen.py tests/test_ddl_compile.py -v`
Expected: 일부 테스트 FAIL — 기존 코드/템플릿이 `column` (단수) 기대. 다음 Task 5 에서 템플릿·소비 코드 수정.

(이 Step 은 의도적 회귀 노출 — 다음 Task 의 진입 신호.)

- [ ] **Step 6: per-file commit (기존 테스트 깨진 채로 — 다음 Task에서 수습)**

```
git add scripts/ddl_gen.py
git commit -m "feat(ddl_gen): _fk_columns returns list (composite FK support)"
git add tests/test_ddl_gen_composite_fk.py
git commit -m "test(ddl_gen): composite FK gather coverage"
```

---

## Task 5: Stage 2 — constraints.sql.j2 템플릿 (postgres + hsqldb) 수정

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-ddl\templates\postgres\constraints.sql.j2`
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-ddl\templates\hsqldb\constraints.sql.j2`

기존 템플릿이 `fk.column` / `fk.ref_column` 단수형을 사용 — 이를 `fk.columns | join(', ')` 등으로 갱신.

- [ ] **Step 1: 현재 postgres constraints 템플릿 읽기**

Run: `Read D:\AI\workspace\andrej-karpathy-rdb-ddl\templates\postgres\constraints.sql.j2`

- [ ] **Step 2: 템플릿 수정 — `column` 단수 참조를 `columns` list 로**

기존 패턴:
```jinja
ALTER TABLE {{ fk.src_schema }}.{{ fk.src_table }}
  ADD CONSTRAINT {{ fk.name }} FOREIGN KEY ({{ fk.column }})
  REFERENCES {{ fk.ref_schema }}.{{ fk.ref_table }} ({{ fk.ref_column }})
  ON DELETE {{ fk.on_delete }};
```

신규 패턴:
```jinja
ALTER TABLE {{ fk.src_schema }}.{{ fk.src_table }}
  ADD CONSTRAINT {{ fk.name }} FOREIGN KEY ({{ fk.columns | join(', ') }})
  REFERENCES {{ fk.ref_schema }}.{{ fk.ref_table }} ({{ fk.ref_columns | join(', ') }})
  ON DELETE {{ fk.on_delete }};
```

- [ ] **Step 3: HSQLDB 템플릿 동일하게 수정**

Run: `Read D:\AI\workspace\andrej-karpathy-rdb-ddl\templates\hsqldb\constraints.sql.j2`
동일한 패턴 변경 적용.

- [ ] **Step 4: 기존 테스트 회귀 복구**

Run: `python -m pytest -v`
Expected: 모두 PASS. 단일 컬럼 케이스는 `['x'] | join(', ') = 'x'` 로 동작 동일.

- [ ] **Step 5: 복합 FK SQL 출력 검증 — 테스트 추가**

`tests/test_ddl_gen_composite_fk.py` 에 append:

```python
import pathlib
from ddl_gen import generate_ddl


def test_generate_ddl_composite_fk_emits_multi_column_constraint(tmp_path):
    blueprint = {
        "project": {"name": "t"},
        "entities": [
            {"name": "address", "schema": "app", "table": "address",
             "columns": [
                 {"name": "user_id", "type": "bigint", "pk": True},
                 {"name": "seq", "type": "int", "pk": True},
                 {"name": "city", "type": "varchar(100)"},
             ]},
            {"name": "address_tag", "schema": "app", "table": "address_tag",
             "columns": [
                 {"name": "user_id", "type": "bigint", "pk": True},
                 {"name": "seq", "type": "int", "pk": True},
                 {"name": "tag", "type": "varchar(50)", "pk": True},
             ]},
        ],
        "relations": [{
            "from": "address", "to": "address_tag",
            "fk": {"column": ["user_id", "seq"], "on_delete": "cascade"},
        }],
    }
    generate_ddl(blueprint, tmp_path, dialect="postgres")
    sql = (tmp_path / "migrations" / "V004__create_constraints.sql").read_text(encoding="utf-8")
    assert "FOREIGN KEY (user_id, seq)" in sql
    assert "REFERENCES app.address (user_id, seq)" in sql
    assert "ON DELETE CASCADE" in sql
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `python -m pytest tests/test_ddl_gen_composite_fk.py -v`
Expected: 6 passed.

- [ ] **Step 7: per-file commit**

```
git add templates/postgres/constraints.sql.j2
git commit -m "feat(template-postgres): composite FK ADD CONSTRAINT"
git add templates/hsqldb/constraints.sql.j2
git commit -m "feat(template-hsqldb): composite FK ADD CONSTRAINT"
git add tests/test_ddl_gen_composite_fk.py
git commit -m "test(ddl_gen): composite FK SQL output"
```

---

## Task 6: 양 레포 합동 골든 패스 — M:N junction E2E

**Files:**
- Create: `D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\wiki\entities\user\profile.md`
- Create: `...wiki\entities\role\profile.md`
- Create: `...wiki\entities\user_role\profile.md`
- Create: `D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\expected\V004__create_constraints.sql.expected`

3개 entity (`user`, `role`, `user_role` junction) 로 Stage 1→2 전체 파이프라인이 동작하는지 검증.

- [ ] **Step 1: golden fixture 디렉터리 생성**

Run:
```powershell
New-Item -ItemType Directory -Force `
  D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\wiki\entities\user,
  D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\wiki\entities\role,
  D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\wiki\entities\user_role,
  D:\AI\workspace\business-fullstack-creater\tests\golden\v0.3-phase-a-mn\expected
```

- [ ] **Step 2: user/profile.md 작성**

```yaml
---
type: entity
name: user
domain: [auth]
columns:
  - { name: id, type: bigserial, pk: true }
  - { name: email, type: varchar(255), nullable: false }
relations:
  - { kind: has_many, to: user_role, fk: user_id }
---

# user
```

- [ ] **Step 3: role/profile.md 작성**

```yaml
---
type: entity
name: role
domain: [auth]
columns:
  - { name: id, type: bigserial, pk: true }
  - { name: code, type: varchar(50), nullable: false }
relations:
  - { kind: has_many, to: user_role, fk: role_id }
---

# role
```

- [ ] **Step 4: user_role/profile.md 작성 (junction)**

```yaml
---
type: entity
name: user_role
domain: [auth]
columns:
  - { name: user_id, type: bigint, pk: true }
  - { name: role_id, type: bigint, pk: true }
  - { name: created_at, type: timestamp }
---

# user_role (junction)
```

- [ ] **Step 5: Stage 1 build — blueprint 생성**

Run (rdb-skill 디렉터리에서):
```
python -c "from rdb_index import build_blueprint_from_wiki_dir; \
  import json; \
  bp, vr = build_blueprint_from_wiki_dir('D:/AI/workspace/business-fullstack-creater/tests/golden/v0.3-phase-a-mn/wiki'); \
  print(json.dumps(vr, ensure_ascii=False, indent=2))"
```

(정확한 함수명은 rdb_index.py 의 build entry point 따름 — 현재 `build_blueprint` 가 dict 입력. wiki 디렉터리 빌드 entry point는 rdb_index 에 있음.)

Expected: V005 INFO 메시지 1개 (junction 인지) + V001/V002/V003/V004 ERROR 0개. validation.passed=true.

- [ ] **Step 6: Stage 2 compile — DDL 생성**

(Stage 1 산출 `_blueprint.yaml` 을 이용) Run (rdb-ddl 디렉터리에서):
```
python -m ddl_compile --blueprint D:/AI/workspace/business-fullstack-creater/tests/golden/v0.3-phase-a-mn/wiki/_blueprint.yaml --out D:/AI/workspace/business-fullstack-creater/tests/golden/v0.3-phase-a-mn/db --dialect postgres
```

- [ ] **Step 7: 기대 SQL 검증**

생성된 `V004__create_constraints.sql` 에 다음 두 줄이 모두 포함되어야 함:
```
ADD CONSTRAINT fk_user_role__user_id FOREIGN KEY (user_id) REFERENCES auth.user (id)
ADD CONSTRAINT fk_user_role__role_id FOREIGN KEY (role_id) REFERENCES auth.role (id)
```

생성된 `V002__create_tables.sql` 에 user_role의 복합 PK 가 표현되어야 함:
```
PRIMARY KEY (user_id, role_id)
```

- [ ] **Step 8: golden 결과 .gitignore 추가**

`tests/golden/v0.3-phase-a-mn/db/` (생성 산출물) 은 결과물이라 .gitignore. wiki/ 와 expected/ 만 commit.

`business-fullstack-creater/.gitignore` 에 추가:
```
tests/golden/*/db/
tests/golden/*/wiki/_blueprint.yaml
```

- [ ] **Step 9: per-file commit**

```
cd D:\AI\workspace\business-fullstack-creater
git add tests/golden/v0.3-phase-a-mn/wiki/entities/user/profile.md
git commit -m "test(golden): v0.3-phase-a-mn user entity"
git add tests/golden/v0.3-phase-a-mn/wiki/entities/role/profile.md
git commit -m "test(golden): v0.3-phase-a-mn role entity"
git add tests/golden/v0.3-phase-a-mn/wiki/entities/user_role/profile.md
git commit -m "test(golden): v0.3-phase-a-mn user_role junction"
git add .gitignore
git commit -m "chore(gitignore): exclude golden test outputs"
```

---

## Task 7: 문서 갱신 — blueprint-spec.md / ddl-spec.md

**Files:**
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-skill\.claude\skills\karpathy-rdb\references\blueprint-spec.md`
- Modify: `D:\AI\workspace\andrej-karpathy-rdb-ddl\.claude\skills\karpathy-rdb-ddl\references\ddl-spec.md`

- [ ] **Step 1: blueprint-spec.md — 복합 PK / 복합 FK / junction 섹션 추가**

`### entities[].columns[]` 또는 `relations[].fk` 인근에 다음 subsection 추가:

```markdown
### 복합 PK / 복합 FK / junction (v0.3+)

- **복합 PK**: `columns[].pk: true` 가 여러 컬럼 → 자연 지원. `PRIMARY KEY (a, b)` 로 emit.
- **복합 FK**: `relations[].fk.column` 을 list 로 선언:
  ```yaml
  relations:
    - { from: child, to: parent, fk: { column: [a_id, b_id], on_delete: cascade } }
  ```
  Stage 2 가 다중 컬럼 `FOREIGN KEY (a_id, b_id) REFERENCES parent (a, b)` 로 emit.
- **N:M junction**: 사용자가 junction entity 를 직접 선언 (PK = 두 FK 합, 비-PK 컬럼은 메타 컬럼만). 양쪽 entity는 `relations: [{kind: has_many, to: junction, fk: <one_col>}]` 로 연결. Stage 1 V005 가 junction을 자동 인지하고 INFO 메시지 emit (cardinality N:M 표기). 별도 magical type 없음 — Karpathy 정신: 명시적·파일 기반.
```

- [ ] **Step 2: ddl-spec.md — 복합 FK 동작 1줄 추가**

`relations[].fk.column` 행 아래 또는 동일 표에 추가:

```markdown
| `relations[].fk.column` | string OR list. list 일 때 다중 컬럼 `FOREIGN KEY (a, b) REFERENCES x (a, b)` 로 emit (positional pairing). |
```

- [ ] **Step 3: per-file commit (양 레포)**

```
cd D:\AI\workspace\andrej-karpathy-rdb-skill
git add .claude/skills/karpathy-rdb/references/blueprint-spec.md
git commit -m "docs(spec): composite PK/FK + N:M junction (v0.3)"

cd D:\AI\workspace\andrej-karpathy-rdb-ddl
git add .claude/skills/karpathy-rdb-ddl/references/ddl-spec.md
git commit -m "docs(ddl-spec): composite FK list-form column"
```

---

## Milestone Gate M-A — Phase A 자체 리뷰

- [ ] **Step 1: 양 레포 pytest 전체 그린 확인**

```
cd D:\AI\workspace\andrej-karpathy-rdb-skill && python -m pytest -v
cd D:\AI\workspace\andrej-karpathy-rdb-ddl && python -m pytest -v
```
Expected: 모두 PASS, 회귀 0건.

- [ ] **Step 2: Task 6 골든 fixture E2E 수동 확인**

생성된 `V002__create_tables.sql` 에 `PRIMARY KEY (user_id, role_id)` 포함 + `V004__create_constraints.sql` 에 두 FK constraint 포함.

- [ ] **Step 3: 5축 자체 점수 산정**

| 축 | 점수 (1~5) | 이유 |
|---|:-:|---|
| 1. 반복 데이터 효율 관리 | ? | 복합 키 지원으로 entity를 잘게 쪼개지 않아도 됨 |
| 2. 도메인·엔티티 지식 누적 | ? | junction 패턴이 catalog 에서 잘 표현되는지 |
| 3. 풍부한 Seeds | ? | M:N seed 가 실 use case에 들어맞는지 |
| 4. 완성도 높은 WAR | ? | 정확한 FK constraint = 무결성 향상 |
| 5. Karpathy 정신 | ? | 명시적·파일 기반 유지 (junction = 일반 entity) |

- [ ] **Step 4: 의도 어긋남 체크**

> 사용자 의도 거울: "반복되는 데이터를 효율적으로 관리 / 도메인·엔티티를 지식으로 누적 / 풍부한 Seeds / 완성도 높은 WAR"

명시적 비교: 어긋남이 있는가? (없으면 "없음")

- [ ] **Step 5: 사용자에게 한 줄 보고**

형식: `"M-A 완료 — 점수 N·N·N·N·N, [평균 ≥ 4 → 다음 Phase B 진행 / < 4 → 멈춤+보고]"`

- [ ] **Step 6: M-A 통과 시 Task #101 status 업데이트**

5축 평균 ≥ 4 + 회귀 0건 → TaskUpdate(taskId=101, addBlockedBy를 변경하지 않고 metadata만 갱신; 또는 v0.3 완료 시점에 한꺼번에 completed). Phase B 작성 진입.

평균 < 4 또는 회귀 발생 시: 즉시 멈추고 사용자 보고, 원인 분석 후 plan 수정.

---

## Verification (Phase A 전체)

1. **양 레포 pytest 그린**: rdb-skill + rdb-ddl 모두 회귀 0
2. **신규 테스트 동작**: `test_relations_mn.py` (Stage 1) + `test_ddl_gen_composite_fk.py` (Stage 2) 모두 PASS
3. **E2E 골든 검증**: `business-fullstack-creater/tests/golden/v0.3-phase-a-mn` 빌드 결과가 기대 SQL 패턴 포함
4. **문서 일치**: blueprint-spec.md / ddl-spec.md 가 신규 동작과 일치
5. **per-file commit 규율**: 모든 commit 이 단일 파일 (CLAUDE.md 규칙)
6. **5축 평균 ≥ 4 + 어긋남 0건**: M-A 게이트 통과
