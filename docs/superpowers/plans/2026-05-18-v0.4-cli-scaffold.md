# v0.4 — CLI Scaffold (`/business-fullstack-creater scaffold`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** business-fullstack-creater 리포에 첫 슬래시 커맨드 `/business-fullstack-creater scaffold` 를 도입하여 Stage 1→2→3→4 + overlay 를 **1회 실행으로 자동 큐레이션**한다. 단계별 추가 정보 prompt (도메인, lane, 기본 pattern, package 등) → 결정된 인자로 4 stage 의 CLI 를 순차 호출하고 통합 산출물 레이아웃을 만든다. v0.4 핵심 가치 (단기 #1, USER-GUIDE 6.4) 80% 달성. Web UI 는 v0.7 로 연기.

**Architecture:**
- **2-layer split**:
  - **Slash command** (`/business-fullstack-creater scaffold`) — 에이전트 가이드. 사용자에게 단계별 prompt 진행, 응답 수집, Python 오케스트레이터 호출, 결과 리포트.
  - **Python orchestrator** (`scripts/scaffold_orchestrator.py`) — 결정 인자로 각 stage CLI 를 deterministic 하게 호출. 테스트 가능. silent fallback 금지 — 각 stage 실패 시 명시적 에러 + 어느 stage 까지 진행됐는지 보고.
- **Stage 경로 해소**: `config/stage-paths.yaml` (선택). 미존재 시 sibling 디렉터리 convention (`<creator>/../andrej-karpathy-rdb-{skill,ddl,mybatis,nexacro}`). 명시적 path 우선.
- **Wiki 입력 모드** (택 1, 사용자 prompt):
  - (a) `--preset <name>` — rdb-skill 의 preset 카탈로그 (`고객관리` 등) 에서 자동 init + compile (현재 v0.2 preset 메커니즘 활용)
  - (b) `--wiki <path>` — 이미 준비된 wiki 디렉터리 (`_schema.md`, `entities/`) 복사
  - 둘 다 없으면 명시적 에러 (silent fallback 금지)
- **출력 레이아웃** (`<out_dir>/`):
  ```
  1-wiki/         # Stage 1: _schema.md, entities/, _blueprint.yaml, compile-report.md
  2-ddl/          # Stage 2: ddl_*.sql, ddl-report.md
  3-mybatis/      # Stage 3: src/main/java/.../*, mapper.xml, endpoints.json
  4-nexacro/      # Stage 4: nxui/_form_/*.xfdl, generation-report.md
  scaffold-report.md   # 통합: 인자, 각 stage 시간/결과, 다음 단계
  ```
- **사용자 prompt 단계** (slash command 가 순서대로 묻는다):
  1. 도메인명 (예: `주문관리`) — 미입력 시 첫 인자 사용
  2. Wiki 입력 모드 (`preset` / `wiki`)
  3. Stage 3 lane (`nexacro` / `vanilla`, 기본 `nexacro`)
  4. Stage 4 기본 pattern (`D2` / `F1` / `C1`, 기본 `D2`)
  5. Spring base package (기본 `com.example.<domain_slug>`)
  6. 출력 디렉터리 (기본 `./<domain_slug>-scaffold/`)
- **Blueprint v1 호환**: scaffold 는 blueprint 를 생성/소비만 함, contract 변경 없음.

**Tech Stack:** Python 3.11+, PyYAML, subprocess, pytest. 신규 코드는 creator 리포에만. 기존 Stage 1–4 리포 무변경 (consumer 만 신규).

**Phase C lesson 반영** (M-C Gate 회고):
> "스펙 문서 변경 = 반드시 코드 변경 동반 확인"

이번 plan 은 슬래시 커맨드 markdown 과 Python 오케스트레이터를 **항상 함께 task** 로 묶어 docs/code drift 를 사전 차단한다. 각 stage wiring task 는 (Python 호출 + 슬래시 커맨드 step 추가 + 테스트) 3-set 으로 1 task.

---

## File Structure

**business-fullstack-creater 만 변경**:
- 신규: `.claude/commands/business-fullstack-creater-scaffold.md` — 슬래시 커맨드 (에이전트 가이드)
- 신규: `scripts/scaffold_orchestrator.py` — 4-stage 순차 호출 오케스트레이터
- 신규: `scripts/stage_paths.py` — stage repo 경로 해소 (config + sibling fallback)
- 신규: `config/stage-paths.yaml.example` — 사용자가 복사해서 쓰는 예시 (실파일은 .gitignore)
- 신규: `tests/test_stage_paths.py`
- 신규: `tests/test_scaffold_orchestrator.py` — 각 stage mock 호출 검증
- 신규: `tests/golden/test_scaffold_e2e.py` — 실제 4-stage E2E (sibling 경로 사용)
- 수정: `.gitignore` — `config/stage-paths.yaml` 추가
- 수정: `docs/USER-GUIDE.md` — v0.4 항목 "✅ 완료" + 사용법 1 단락 추가
- 신규: `docs/superpowers/specs/2026-05-18-v0.4-phase-d-gate.md` — Phase D Gate 자체 리뷰

---

## Task 1: stage_paths.py — 경로 해소 + config + sibling fallback

**Files:**
- Create: `D:\AI\workspace\business-fullstack-creater\scripts\stage_paths.py`
- Create: `D:\AI\workspace\business-fullstack-creater\config\stage-paths.yaml.example`
- Modify: `D:\AI\workspace\business-fullstack-creater\.gitignore`
- Test: `D:\AI\workspace\business-fullstack-creater\tests\test_stage_paths.py`

- [ ] **Step 1: Write failing tests**

```python
# tests/test_stage_paths.py
import pathlib
import pytest
from stage_paths import resolve_stage_paths, StagePathError, StagePaths


def test_resolve_from_sibling_convention(tmp_path):
    creator = tmp_path / "business-fullstack-creater"
    creator.mkdir()
    for n in ("skill", "ddl", "mybatis", "nexacro"):
        (tmp_path / f"andrej-karpathy-rdb-{n}").mkdir()
    sp = resolve_stage_paths(creator_root=creator)
    assert sp.stage1.name == "andrej-karpathy-rdb-skill"
    assert sp.stage4.name == "andrej-karpathy-rdb-nexacro"


def test_explicit_config_wins(tmp_path):
    creator = tmp_path / "creater"
    creator.mkdir()
    cfg_dir = creator / "config"
    cfg_dir.mkdir()
    s1 = tmp_path / "custom-skill"
    s1.mkdir()
    for n in ("ddl", "mybatis", "nexacro"):
        (tmp_path / f"andrej-karpathy-rdb-{n}").mkdir()
    (cfg_dir / "stage-paths.yaml").write_text(
        f"stage1: {s1.as_posix()}\n", encoding="utf-8"
    )
    sp = resolve_stage_paths(creator_root=creator)
    assert sp.stage1 == s1


def test_missing_stage_raises(tmp_path):
    creator = tmp_path / "creater"
    creator.mkdir()
    (tmp_path / "andrej-karpathy-rdb-skill").mkdir()
    with pytest.raises(StagePathError) as exc:
        resolve_stage_paths(creator_root=creator)
    assert "stage2" in str(exc.value) or "ddl" in str(exc.value)
```

- [ ] **Step 2: Run tests to verify failure**

Run: `cd D:\AI\workspace\business-fullstack-creater; $env:PYTHONPATH = "scripts"; python -m pytest tests/test_stage_paths.py -v`
Expected: FAIL with `ModuleNotFoundError: stage_paths`

- [ ] **Step 3: Implement stage_paths.py**

```python
# scripts/stage_paths.py
import pathlib
from dataclasses import dataclass
import yaml


class StagePathError(Exception):
    pass


@dataclass
class StagePaths:
    stage1: pathlib.Path
    stage2: pathlib.Path
    stage3: pathlib.Path
    stage4: pathlib.Path


_SIBLING_NAMES = {
    "stage1": "andrej-karpathy-rdb-skill",
    "stage2": "andrej-karpathy-rdb-ddl",
    "stage3": "andrej-karpathy-rdb-mybatis",
    "stage4": "andrej-karpathy-rdb-nexacro",
}


def resolve_stage_paths(creator_root):
    creator_root = pathlib.Path(creator_root).resolve()
    cfg_file = creator_root / "config" / "stage-paths.yaml"
    overrides = {}
    if cfg_file.exists():
        overrides = yaml.safe_load(cfg_file.read_text(encoding="utf-8")) or {}

    resolved = {}
    for key, sibling in _SIBLING_NAMES.items():
        if key in overrides:
            p = pathlib.Path(overrides[key]).expanduser().resolve()
        else:
            p = (creator_root.parent / sibling).resolve()
        if not p.exists():
            raise StagePathError(
                f"{key} repository not found at {p} "
                f"(override config/stage-paths.yaml or place repo at sibling path)"
            )
        resolved[key] = p
    return StagePaths(**resolved)
```

- [ ] **Step 4: Create config example + .gitignore**

`config/stage-paths.yaml.example`:
```yaml
# Copy to config/stage-paths.yaml to override sibling convention.
# Default: <creator>/../andrej-karpathy-rdb-{skill,ddl,mybatis,nexacro}
# stage1: D:/AI/workspace/andrej-karpathy-rdb-skill
# stage2: D:/AI/workspace/andrej-karpathy-rdb-ddl
# stage3: D:/AI/workspace/andrej-karpathy-rdb-mybatis
# stage4: D:/AI/workspace/andrej-karpathy-rdb-nexacro
```

Append to `.gitignore`:
```
config/stage-paths.yaml
```

- [ ] **Step 5: Run tests to verify pass**

Run: `python -m pytest tests/test_stage_paths.py -v`
Expected: 3 PASS

- [ ] **Step 6: Per-file commits**

```powershell
git add scripts/stage_paths.py
git commit -m "feat(stage_paths): resolve stage repo paths with config+sibling fallback"
git add config/stage-paths.yaml.example
git commit -m "feat(config): stage-paths.yaml example for overriding sibling convention"
git add .gitignore
git commit -m "chore(gitignore): exclude local stage-paths.yaml"
git add tests/test_stage_paths.py
git commit -m "test(stage_paths): config override + sibling fallback + missing-stage error"
```

---

## Task 2: scaffold_orchestrator.py — 골격 + Stage 1 (preset 모드)

**Files:**
- Create: `D:\AI\workspace\business-fullstack-creater\scripts\scaffold_orchestrator.py`
- Test: `D:\AI\workspace\business-fullstack-creater\tests\test_scaffold_orchestrator.py`

- [ ] **Step 1: Failing test (Stage 1 only, preset 모드)**

```python
# tests/test_scaffold_orchestrator.py
import pathlib, subprocess
import pytest
from scaffold_orchestrator import ScaffoldArgs, run_scaffold, StageFailure


def _make_fake_stage(tmp_path, name, scripts):
    """scripts: dict[name -> python source]"""
    repo = tmp_path / f"andrej-karpathy-rdb-{name}"
    (repo / "scripts").mkdir(parents=True)
    for fname, src in scripts.items():
        (repo / "scripts" / fname).write_text(src, encoding="utf-8")
    return repo


def test_stage1_preset_writes_blueprint(tmp_path, monkeypatch):
    # fake stage1: rdb_index.py compile <wiki> writes _blueprint.yaml
    s1_src = (
        "import sys, pathlib\n"
        "args = sys.argv[1:]\n"
        "if args[0] == 'init':\n"
        "    out = pathlib.Path(args[1]); out.mkdir(parents=True, exist_ok=True)\n"
        "    (out / '_schema.md').write_text('---\\ntype: schema\\n---\\n')\n"
        "elif args[0] == 'compile':\n"
        "    out = pathlib.Path(args[1])\n"
        "    (out / '_blueprint.yaml').write_text('version: 1\\nentities: []\\n')\n"
        "    (out / 'compile-report.md').write_text('OK\\n')\n"
    )
    s1 = _make_fake_stage(tmp_path, "skill", {"rdb_index.py": s1_src})
    for n in ("ddl", "mybatis", "nexacro"):
        _make_fake_stage(tmp_path, n, {})  # empty
    creator = tmp_path / "creater"; creator.mkdir()

    out = tmp_path / "out"
    args = ScaffoldArgs(
        domain="주문관리", domain_slug="order", wiki_mode="preset",
        preset="주문관리", wiki_path=None, lane="nexacro",
        default_pattern="D2", package="com.example.order", out_dir=out,
        creator_root=creator, stop_after_stage=1,
    )
    report = run_scaffold(args)
    assert (out / "1-wiki" / "_blueprint.yaml").exists()
    assert report.stages_run == ["stage1"]


def test_missing_wiki_mode_raises(tmp_path):
    creator = tmp_path / "creater"; creator.mkdir()
    args = ScaffoldArgs(
        domain="x", domain_slug="x", wiki_mode=None,
        preset=None, wiki_path=None, lane="nexacro",
        default_pattern="D2", package="com.example.x", out_dir=tmp_path/"out",
        creator_root=creator, stop_after_stage=1,
    )
    with pytest.raises(ValueError) as exc:
        run_scaffold(args)
    assert "wiki_mode" in str(exc.value)
```

- [ ] **Step 2: Run to verify failure** → `ModuleNotFoundError`

- [ ] **Step 3: Implement orchestrator skeleton + Stage 1**

```python
# scripts/scaffold_orchestrator.py
import pathlib, subprocess, sys, shutil, time
from dataclasses import dataclass, field
from typing import Optional, List
from stage_paths import resolve_stage_paths


@dataclass
class ScaffoldArgs:
    domain: str
    domain_slug: str
    wiki_mode: Optional[str]            # "preset" | "wiki"
    preset: Optional[str]
    wiki_path: Optional[pathlib.Path]
    lane: str                            # "nexacro" | "vanilla"
    default_pattern: str                 # "D2" | "F1" | "C1"
    package: str
    out_dir: pathlib.Path
    creator_root: pathlib.Path
    stop_after_stage: int = 4            # for tests


@dataclass
class ScaffoldReport:
    stages_run: List[str] = field(default_factory=list)
    stage_durations_ms: dict = field(default_factory=dict)
    out_dir: Optional[pathlib.Path] = None


class StageFailure(Exception):
    pass


def _run(cmd, cwd, label):
    t0 = time.monotonic()
    proc = subprocess.run(cmd, cwd=cwd, capture_output=True, text=True)
    dur = int((time.monotonic() - t0) * 1000)
    if proc.returncode != 0:
        raise StageFailure(
            f"{label} failed (exit {proc.returncode})\n"
            f"cmd: {cmd}\nstdout:\n{proc.stdout}\nstderr:\n{proc.stderr}"
        )
    return dur, proc.stdout


def _run_stage1(args, stage_paths, report):
    wiki_out = args.out_dir / "1-wiki"
    wiki_out.mkdir(parents=True, exist_ok=True)
    s1 = stage_paths.stage1
    if args.wiki_mode == "preset":
        dur1, _ = _run(
            [sys.executable, str(s1 / "scripts" / "rdb_index.py"),
             "init", str(wiki_out), "--preset", args.preset],
            cwd=s1, label="stage1.init",
        )
    elif args.wiki_mode == "wiki":
        if not args.wiki_path or not args.wiki_path.exists():
            raise StageFailure(f"wiki_path not found: {args.wiki_path}")
        if wiki_out.exists():
            shutil.rmtree(wiki_out)
        shutil.copytree(args.wiki_path, wiki_out)
        dur1 = 0
    else:
        raise ValueError("wiki_mode must be 'preset' or 'wiki'")
    dur2, _ = _run(
        [sys.executable, str(s1 / "scripts" / "rdb_index.py"),
         "compile", str(wiki_out)],
        cwd=s1, label="stage1.compile",
    )
    report.stages_run.append("stage1")
    report.stage_durations_ms["stage1"] = dur1 + dur2


def run_scaffold(args):
    args.out_dir = pathlib.Path(args.out_dir).resolve()
    args.out_dir.mkdir(parents=True, exist_ok=True)
    stage_paths = resolve_stage_paths(args.creator_root)
    report = ScaffoldReport(out_dir=args.out_dir)

    _run_stage1(args, stage_paths, report)
    if args.stop_after_stage <= 1:
        return report
    # Stage 2/3/4 wired in later tasks
    return report
```

- [ ] **Step 4: Tests pass** — both stage1-success and missing-wiki_mode-raises.

- [ ] **Step 5: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): scaffold skeleton + stage1 (preset|wiki modes)"
git add tests/test_scaffold_orchestrator.py
git commit -m "test(orchestrator): stage1 preset writes blueprint + wiki_mode validation"
```

---

## Task 3: Stage 2 wiring (DDL)

**Files:**
- Modify: `scripts/scaffold_orchestrator.py`
- Modify: `tests/test_scaffold_orchestrator.py`

- [ ] **Step 1: Failing test (Stage 2 produces ddl)**

```python
def test_stage2_runs_after_stage1(tmp_path):
    # fake stage2: ddl_compile.py reads _blueprint.yaml, writes ddl_*.sql
    s2_src = (
        "import sys, pathlib\n"
        "bp = pathlib.Path(sys.argv[sys.argv.index('--blueprint')+1])\n"
        "out = pathlib.Path(sys.argv[sys.argv.index('--out')+1])\n"
        "out.mkdir(parents=True, exist_ok=True)\n"
        "(out / 'ddl_create.sql').write_text('-- ' + bp.read_text())\n"
    )
    # ... same fake stage1 as before ...
    # _make_fake_stage(tmp_path, "ddl", {"ddl_compile.py": s2_src})
    # ... run scaffold with stop_after_stage=2 ...
    # assert (out / "2-ddl" / "ddl_create.sql").exists()
```

- [ ] **Step 2: Run** → fail (no stage2 wiring yet).

- [ ] **Step 3: Add `_run_stage2` + wire in `run_scaffold`**

```python
def _run_stage2(args, stage_paths, report):
    ddl_out = args.out_dir / "2-ddl"
    ddl_out.mkdir(parents=True, exist_ok=True)
    s2 = stage_paths.stage2
    bp = args.out_dir / "1-wiki" / "_blueprint.yaml"
    dur, _ = _run(
        [sys.executable, str(s2 / "scripts" / "ddl_compile.py"),
         "--blueprint", str(bp), "--out", str(ddl_out)],
        cwd=s2, label="stage2.ddl_compile",
    )
    report.stages_run.append("stage2")
    report.stage_durations_ms["stage2"] = dur
```

Wire in `run_scaffold` after stage1:
```python
    if args.stop_after_stage <= 1: return report
    _run_stage2(args, stage_paths, report)
    if args.stop_after_stage <= 2: return report
```

- [ ] **Step 4: Tests pass** (stage2 fake produces ddl_create.sql).

- [ ] **Step 5: Verify against real Stage 2 CLI shape**

Run: `python D:\AI\workspace\andrej-karpathy-rdb-ddl\scripts\ddl_compile.py --help`
실제 인자명이 다르면 (예: `-b/-o`) 위 코드를 실 CLI 에 맞춰 조정. 차이 발견 시 즉시 plan 의 fake-stage 도 동일하게 수정.

- [ ] **Step 6: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): stage2 DDL wiring"
git add tests/test_scaffold_orchestrator.py
git commit -m "test(orchestrator): stage2 produces ddl artifacts"
```

---

## Task 4: Stage 3 wiring (MyBatis + lane)

**Files:**
- Modify: `scripts/scaffold_orchestrator.py`
- Modify: `tests/test_scaffold_orchestrator.py`

- [ ] **Step 1: Failing test — lane=vanilla 전달**

```python
def test_stage3_passes_lane_and_package(tmp_path):
    s3_src = (
        "import sys, pathlib, json\n"
        "lane = sys.argv[sys.argv.index('--lane')+1]\n"
        "pkg = sys.argv[sys.argv.index('--package')+1]\n"
        "out = pathlib.Path(sys.argv[sys.argv.index('--out')+1])\n"
        "out.mkdir(parents=True, exist_ok=True)\n"
        "(out / 'meta.json').write_text(json.dumps({'lane': lane, 'package': pkg}))\n"
        "(out / 'endpoints.json').write_text('{\"endpoints\": []}')\n"
    )
    # ... assert meta.json content matches scaffold args ...
```

- [ ] **Step 2: Run** → fail.

- [ ] **Step 3: Add `_run_stage3`**

```python
def _run_stage3(args, stage_paths, report):
    out = args.out_dir / "3-mybatis"
    out.mkdir(parents=True, exist_ok=True)
    s3 = stage_paths.stage3
    bp = args.out_dir / "1-wiki" / "_blueprint.yaml"
    dur, _ = _run(
        [sys.executable, str(s3 / "scripts" / "compile.py"),
         "--blueprint", str(bp), "--lane", args.lane,
         "--package", args.package, "--out", str(out)],
        cwd=s3, label="stage3.compile",
    )
    report.stages_run.append("stage3")
    report.stage_durations_ms["stage3"] = dur
```

- [ ] **Step 4: Real CLI shape 확인**

Run: `python D:\AI\workspace\andrej-karpathy-rdb-mybatis\scripts\compile.py --help`
플래그명 (`--lane`, `--package`, `--out`) 이 실제와 다르면 plan 과 코드 동시 조정. (Phase B Task 5 에서 `--lane` 도입했음 — 재확인.)

- [ ] **Step 5: Tests pass + 실 Stage 3 smoke (`--lane vanilla` 1회)**

- [ ] **Step 6: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): stage3 MyBatis wiring (--lane, --package)"
git add tests/test_scaffold_orchestrator.py
git commit -m "test(orchestrator): stage3 forwards lane and package"
```

---

## Task 5: Stage 4 wiring (Nexacro form_gen + default-pattern)

**Files:**
- Modify: `scripts/scaffold_orchestrator.py`
- Modify: `tests/test_scaffold_orchestrator.py`

- [ ] **Step 1: Failing test — default_pattern 전달, endpoints.json 위치 전달**

```python
def test_stage4_passes_default_pattern(tmp_path):
    s4_src = (
        "import sys, pathlib\n"
        "dp = sys.argv[sys.argv.index('--default-pattern')+1]\n"
        "out = pathlib.Path(sys.argv[sys.argv.index('--out')+1])\n"
        "(out / 'nxui' / '_form_').mkdir(parents=True, exist_ok=True)\n"
        "(out / 'nxui' / '_form_' / 'marker.txt').write_text(f'pattern={dp}')\n"
    )
    # ... assert marker.txt content == 'pattern=F1' when default_pattern='F1' ...
```

- [ ] **Step 2: Run** → fail.

- [ ] **Step 3: Add `_run_stage4`**

```python
def _run_stage4(args, stage_paths, report):
    out = args.out_dir / "4-nexacro"
    out.mkdir(parents=True, exist_ok=True)
    s4 = stage_paths.stage4
    bp = args.out_dir / "1-wiki" / "_blueprint.yaml"
    eps = args.out_dir / "3-mybatis" / "endpoints.json"
    dur, _ = _run(
        [sys.executable, str(s4 / "scripts" / "form_gen.py"),
         "compile", "--blueprint", str(bp),
         "--endpoints", str(eps),
         "--default-pattern", args.default_pattern,
         "--out", str(out)],
        cwd=s4, label="stage4.form_gen",
    )
    report.stages_run.append("stage4")
    report.stage_durations_ms["stage4"] = dur
```

- [ ] **Step 4: Wire + 실 Stage 4 CLI 확인**

Run: `python D:\AI\workspace\andrej-karpathy-rdb-nexacro\scripts\form_gen.py compile --help`
Phase C Task 5 의 `--default-pattern` 플래그 존재 확인. 플래그명 차이 시 plan 과 코드 동시 조정.

- [ ] **Step 5: Tests pass**

- [ ] **Step 6: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): stage4 form_gen wiring (--default-pattern)"
git add tests/test_scaffold_orchestrator.py
git commit -m "test(orchestrator): stage4 forwards default-pattern"
```

---

## Task 6: scaffold-report.md 생성 + 실패 보고

**Files:**
- Modify: `scripts/scaffold_orchestrator.py`
- Modify: `tests/test_scaffold_orchestrator.py`

- [ ] **Step 1: Failing test — 성공 시 report 작성**

```python
def test_full_run_writes_report(tmp_path):
    # ... fake all 4 stages, stop_after_stage=4 ...
    # assert (out / "scaffold-report.md").exists()
    # content = (out / "scaffold-report.md").read_text(encoding="utf-8")
    # assert "주문관리" in content and "stage1" in content and "stage4" in content
    # assert "D2" in content or args.default_pattern in content
```

- [ ] **Step 2: Failing test — stage 실패 시 부분 진행 보고 + StageFailure raise**

```python
def test_partial_failure_reports_up_to_failed_stage(tmp_path):
    # fake stage3 exits with 1
    # with pytest.raises(StageFailure):
    #     run_scaffold(args)
    # assert (out / "scaffold-report.md").exists()
    # content = (out / "scaffold-report.md").read_text(encoding="utf-8")
    # assert "FAILED: stage3" in content
```

- [ ] **Step 3: Implement `_write_report` + 실패 시 finally 처리**

```python
def _write_report(args, report, failure=None):
    lines = [
        f"# Scaffold Report — {args.domain}",
        "",
        f"- domain: `{args.domain}` (slug: `{args.domain_slug}`)",
        f"- wiki_mode: `{args.wiki_mode}` "
        + (f"(preset=`{args.preset}`)" if args.wiki_mode == "preset"
           else f"(wiki=`{args.wiki_path}`)"),
        f"- lane: `{args.lane}`",
        f"- default_pattern: `{args.default_pattern}`",
        f"- package: `{args.package}`",
        f"- out_dir: `{args.out_dir}`",
        "",
        "## Stages",
    ]
    for name in ("stage1", "stage2", "stage3", "stage4"):
        if name in report.stages_run:
            lines.append(f"- {name}: OK ({report.stage_durations_ms.get(name, 0)} ms)")
    if failure:
        lines += ["", f"## FAILED: {failure[0]}", "", "```", failure[1], "```"]
    else:
        lines += [
            "", "## 다음 단계",
            f"- DDL: `{args.out_dir / '2-ddl'}`",
            f"- Spring (lane={args.lane}): `{args.out_dir / '3-mybatis'}`",
            f"- Nexacro forms (default={args.default_pattern}): "
            f"`{args.out_dir / '4-nexacro'}`",
        ]
    (args.out_dir / "scaffold-report.md").write_text(
        "\n".join(lines) + "\n", encoding="utf-8"
    )


def run_scaffold(args):
    args.out_dir = pathlib.Path(args.out_dir).resolve()
    args.out_dir.mkdir(parents=True, exist_ok=True)
    stage_paths = resolve_stage_paths(args.creator_root)
    report = ScaffoldReport(out_dir=args.out_dir)
    runners = [(_run_stage1, "stage1"), (_run_stage2, "stage2"),
               (_run_stage3, "stage3"), (_run_stage4, "stage4")]
    try:
        for i, (fn, name) in enumerate(runners, start=1):
            fn(args, stage_paths, report)
            if args.stop_after_stage <= i:
                break
    except StageFailure as e:
        failed = report.stages_run[-1] if report.stages_run else "stage1"
        next_stage = f"stage{len(report.stages_run)+1}"
        _write_report(args, report, failure=(next_stage, str(e)))
        raise
    _write_report(args, report)
    return report
```

- [ ] **Step 4: Tests pass**

- [ ] **Step 5: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): scaffold-report.md + partial-failure reporting"
git add tests/test_scaffold_orchestrator.py
git commit -m "test(orchestrator): report content + partial failure"
```

---

## Task 7: CLI entry point + slash command markdown

**Files:**
- Modify: `scripts/scaffold_orchestrator.py` — `__main__` 블록
- Create: `.claude/commands/business-fullstack-creater-scaffold.md`

- [ ] **Step 1: Add argparse entry point**

```python
# end of scripts/scaffold_orchestrator.py
def _main():
    import argparse
    p = argparse.ArgumentParser(prog="scaffold")
    p.add_argument("domain")
    p.add_argument("--domain-slug", default=None)
    p.add_argument("--wiki-mode", choices=("preset", "wiki"), required=True)
    p.add_argument("--preset")
    p.add_argument("--wiki-path", type=pathlib.Path)
    p.add_argument("--lane", choices=("nexacro", "vanilla"), default="nexacro")
    p.add_argument("--default-pattern", choices=("D2", "F1", "C1"), default="D2")
    p.add_argument("--package", default=None)
    p.add_argument("--out-dir", type=pathlib.Path, required=True)
    p.add_argument("--creator-root", type=pathlib.Path,
                   default=pathlib.Path(__file__).resolve().parents[1])
    a = p.parse_args()
    slug = a.domain_slug or a.domain.lower().replace(" ", "_")
    pkg = a.package or f"com.example.{slug}"
    args = ScaffoldArgs(
        domain=a.domain, domain_slug=slug,
        wiki_mode=a.wiki_mode, preset=a.preset, wiki_path=a.wiki_path,
        lane=a.lane, default_pattern=a.default_pattern, package=pkg,
        out_dir=a.out_dir, creator_root=a.creator_root,
    )
    report = run_scaffold(args)
    print(f"OK — {len(report.stages_run)} stages, report at {args.out_dir / 'scaffold-report.md'}")


if __name__ == "__main__":
    _main()
```

Smoke: `python scripts/scaffold_orchestrator.py --help` 통과 확인.

- [ ] **Step 2: Slash command markdown**

`.claude/commands/business-fullstack-creater-scaffold.md`:
```markdown
---
name: business-fullstack-creater-scaffold
description: 4-stage 자동 큐레이션 — wiki → DDL → MyBatis → Nexacro form 을 1회 실행으로 생성
argument-hint: "<도메인명>"
allowed-tools: Read, Write, Glob, Bash
---

# /business-fullstack-creater scaffold

당신은 **4-stage 자동 큐레이션 오케스트레이터** 다. Stage 1 → 2 → 3 → 4 를 순차 실행하여 도메인 1개의 full-stack 산출물을 한 번에 생성한다.

## 절차

1. **도메인명 확인** — 인자가 있으면 그대로, 없으면 사용자에게 묻기: "도메인명 (예: 주문관리)?"
2. **wiki 입력 모드** 묻기:
   - `preset` — rdb-skill 의 preset 카탈로그에서 자동 init (사용자에게 preset 이름도 추가로 묻기, 기본은 도메인명과 동일)
   - `wiki` — 이미 준비된 wiki 디렉터리 경로 (사용자에게 절대 경로 묻기)
3. **Stage 3 lane** 묻기: `nexacro` (기본) 또는 `vanilla`
4. **Stage 4 기본 pattern** 묻기: `D2` (기본) / `F1` / `C1`
5. **Spring base package** 묻기 (기본: `com.example.<domain_slug>`)
6. **출력 디렉터리** 묻기 (기본: `./<domain_slug>-scaffold/`)
7. 사용자에게 인자 요약 보여주고 confirm 받기
8. **오케스트레이터 실행**:
   ```
   python scripts/scaffold_orchestrator.py "<도메인>" \
     --wiki-mode <preset|wiki> [--preset <name> | --wiki-path <path>] \
     --lane <nexacro|vanilla> --default-pattern <D2|F1|C1> \
     --package <pkg> --out-dir <out>
   ```
9. **결과 보고** — `<out>/scaffold-report.md` 내용 + 다음 사용자 작업 안내 (DDL 실행, Spring 빌드, Nexacro 배치)

## 사전 조건
- `config/stage-paths.yaml` 존재 또는 sibling 위치 (`<creator>/../andrej-karpathy-rdb-{skill,ddl,mybatis,nexacro}`) 에 4개 stage 리포 존재
- 각 stage 리포는 v0.3.0 이상 (현재 호환 기준)

## 산출물
- `<out>/1-wiki/_blueprint.yaml` + entities + compile-report
- `<out>/2-ddl/ddl_*.sql`
- `<out>/3-mybatis/` Spring 소스 + `endpoints.json`
- `<out>/4-nexacro/nxui/_form_/*.xfdl`
- `<out>/scaffold-report.md`

## 실패 처리
오케스트레이터가 `StageFailure` 로 종료하면, `scaffold-report.md` 에 어느 stage 에서 실패했는지 기록되어 있다. 사용자에게:
1. 실패 stage 와 stderr 요약 보여주기
2. 해당 stage 단독으로 다시 실행해보도록 안내 (`python <stage>/scripts/...`)
3. 수정 후 scaffold 재실행

## 다음 단계
v0.5 어댑터화 (단기 #2) 또는 v0.3.x C-2 도메인 추천 — 사용자가 v0.4 결과물 1~2회 실 사용 후 우선순위 재평가.
```

- [ ] **Step 3: Smoke — slash command 파일 frontmatter 파싱 가능 확인**

Run: `python -c "import yaml, pathlib; t=pathlib.Path('.claude/commands/business-fullstack-creater-scaffold.md').read_text(encoding='utf-8'); fm=t.split('---')[1]; print(yaml.safe_load(fm)['name'])"`
Expected: `business-fullstack-creater-scaffold`

- [ ] **Step 4: Commits**

```powershell
git add scripts/scaffold_orchestrator.py
git commit -m "feat(orchestrator): __main__ CLI entry point (argparse)"
git add .claude/commands/business-fullstack-creater-scaffold.md
git commit -m "feat(commands): /business-fullstack-creater scaffold slash command"
```

---

## Task 8: Golden E2E — 실 4-stage 통합 1 케이스

**Files:**
- Create: `tests/golden/test_scaffold_e2e.py`

- [ ] **Step 1: Failing test — 실 sibling 경로 사용**

```python
# tests/golden/test_scaffold_e2e.py
import pathlib, sys, subprocess
import pytest

CREATOR = pathlib.Path(__file__).resolve().parents[2]
sys.path.insert(0, str(CREATOR / "scripts"))
from scaffold_orchestrator import ScaffoldArgs, run_scaffold

SIBLINGS = {
    "stage1": CREATOR.parent / "andrej-karpathy-rdb-skill",
    "stage4": CREATOR.parent / "andrej-karpathy-rdb-nexacro",
}


@pytest.mark.skipif(
    not (SIBLINGS["stage1"].exists() and SIBLINGS["stage4"].exists()),
    reason="stage repos not present at sibling paths",
)
def test_scaffold_e2e_preset_nexacro_D2(tmp_path):
    out = tmp_path / "scaffold-out"
    args = ScaffoldArgs(
        domain="고객관리", domain_slug="customer",
        wiki_mode="preset", preset="고객관리", wiki_path=None,
        lane="nexacro", default_pattern="D2",
        package="com.example.customer",
        out_dir=out, creator_root=CREATOR,
    )
    report = run_scaffold(args)
    assert report.stages_run == ["stage1", "stage2", "stage3", "stage4"]
    assert (out / "scaffold-report.md").exists()
    assert (out / "1-wiki" / "_blueprint.yaml").exists()
    assert (out / "2-ddl").exists() and any((out / "2-ddl").glob("*.sql"))
    assert (out / "3-mybatis" / "endpoints.json").exists()
    forms = list((out / "4-nexacro" / "nxui" / "_form_").glob("*.xfdl"))
    assert len(forms) >= 1
```

- [ ] **Step 2: Run** — 실패 시 어느 stage 가 CLI 호출 shape 와 mismatch 인지 확인. 필요 시 Task 3/4/5 의 _run_stageN 인자 조정.

- [ ] **Step 3: Iterate until E2E PASS**

각 stage 의 실 CLI 가 plan 가정과 다른 부분 발견 시:
- orchestrator 수정 + 단위 테스트의 fake stage 도 동시 수정 (drift 방지)
- 변경 사유를 commit 메시지에 명시

- [ ] **Step 4: Commit**

```powershell
git add tests/golden/test_scaffold_e2e.py
git commit -m "test(golden): scaffold E2E with real sibling stage repos (skipped if absent)"
```

---

## Task 9: USER-GUIDE 업데이트 + Phase D Gate 자체 리뷰

**Files:**
- Modify: `docs/USER-GUIDE.md`
- Create: `docs/superpowers/specs/2026-05-18-v0.4-phase-d-gate.md`

- [ ] **Step 1: USER-GUIDE 6.4 표 v0.4 상태를 "✅ 완료" 로**

해당 행에 완료 일자, scaffold 명령 사용법 1단락 ("`/business-fullstack-creater scaffold 주문관리` → 4 stage 자동 생성") 추가.

- [ ] **Step 2: M-D Gate 자체 리뷰 문서 작성**

회귀/품질 지표:
- `tests/test_stage_paths.py` PASS 수
- `tests/test_scaffold_orchestrator.py` PASS 수
- `tests/golden/test_scaffold_e2e.py` 결과 (실 sibling 있을 때)
- Stage 1/2/3/4 회귀 0건 (consumer 만 추가, 기존 코드 무변경)
- 기존 golden 3건 (phase-a/b/c) 회귀 0

5축 점수 + 근거:
1. **반복 데이터 효율 관리** — 한 도메인 ↔ 4 stage 산출. 동일 blueprint 1벌이 DDL+Spring+Nexacro 로 분기.
2. **도메인·엔티티 지식 누적** — preset 모드로 catalog 활용, scaffold 결과의 wiki 가 다시 contribute 가능.
3. **풍부한 Seeds** — preset catalog 가 scaffold 의 입력원. v0.2 의 contribute 와 양방향.
4. **완성도 높은 WAR** — 4 stage 1회 통합. WAR 직전 단계까지 자동.
5. **Karpathy 정신** — 슬래시 커맨드 = 명시적 prompt + confirm. silent fallback 0. 모든 인자 사용자 가시.

평균 ≥ 4 면 PASS.

의도와 어긋난 부분 (있으면) 명시.

- [ ] **Step 3: Commits**

```powershell
git add docs/USER-GUIDE.md
git commit -m "docs(user-guide): v0.4 scaffold complete + usage snippet"
git add docs/superpowers/specs/2026-05-18-v0.4-phase-d-gate.md
git commit -m "docs(spec): v0.4 Phase D Gate self-review"
```

---

## Verification (전체)

1. `cd D:\AI\workspace\business-fullstack-creater; $env:PYTHONPATH = "scripts"; python -m pytest tests/test_stage_paths.py tests/test_scaffold_orchestrator.py -v` → 전 PASS
2. `python -m pytest tests/golden -v` → phase-a/b/c 3건 + scaffold E2E PASS (또는 skip)
3. 수동: `python scripts/scaffold_orchestrator.py "고객관리" --wiki-mode preset --preset 고객관리 --lane nexacro --default-pattern D2 --package com.example.customer --out-dir ./tmp-scaffold` → `tmp-scaffold/scaffold-report.md` 생성 확인
4. 슬래시 커맨드 frontmatter 파싱 OK
5. Stage 1/2/3/4 회귀 0 (각 리포 `pytest` 통과 그대로)
6. per-file commit 규율 준수

---

## 위험 & 완화

| 위험 | 완화 |
|---|---|
| 각 Stage 의 실 CLI 인자명이 plan 가정과 다름 | Task 3/4/5 각 Step 에서 `--help` 1회 호출, 실 CLI 에 맞춰 plan+code 동시 조정. Phase C lesson "스펙 = 코드 동반" 적용 |
| Sibling 경로 가정 실패 (사용자 환경 다름) | `config/stage-paths.yaml` override 제공 + 미발견 시 명시적 에러 |
| Stage 1 preset 모드 인자 (`init --preset`) 실 구현과 다름 | Task 2 Step 5 에서 실 rdb_index.py `init --help` 확인, drift 시 즉시 동기화 |
| Windows 경로/공백 문제 | subprocess `list` 인자 사용 (shell=False), `as_posix()` 회피 — Path 객체 그대로 str() |
| 부분 실패 시 사용자 혼란 | `scaffold-report.md` 가 어느 stage 까지 진행됐는지 명시, 실패 stage 의 stderr 포함 |
| v0.3 lessons 재발 (docs/code drift) | 각 wiring task = (코드 + 테스트 + 실 CLI 확인) 3-set 으로 묶음 |

---

## Execution Mode

**Subagent-Driven Development.** Per-task implementer subagent → spec review → quality review → 다음 task. Continuous, 사용자 중간 확인 없음 (auto mode). M-D Gate 완료 시점에 사용자 보고.

### Phase C lesson 적용 체크리스트 (각 wiring task 시작 전)
- [ ] 해당 stage 의 실 CLI `--help` 호출하여 인자명 확인
- [ ] plan 의 fake-stage 와 orchestrator 호출 인자가 일치하는지 cross-check
- [ ] 차이 발견 시 plan 과 코드 동시 수정 (한쪽만 수정 금지)
