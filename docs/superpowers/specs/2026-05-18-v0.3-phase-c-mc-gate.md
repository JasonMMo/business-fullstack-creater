# v0.3 Phase C — M-C Gate 자체 리뷰

**일자:** 2026-05-18
**범위:** Stage 4 (`andrej-karpathy-rdb-nexacro`) 명명된 파일 기반 form pattern 카탈로그 (D2 / F1 / C1) + Stage 1 `pattern:` 필드 전파 + Stage 1 `/contribute --kind pattern` 프로토콜
**Plan:** `docs/superpowers/plans/2026-05-15-v0.3-phase-c-stage4-patterns.md`
**이전 게이트:** `docs/superpowers/specs/2026-05-15-v0.3-phase-b-mb-gate.md` (M-B, 4.4/5 PASS)

---

## 회귀/품질 지표

| 지표 | 값 |
|---|---|
| Stage 4 (rdb-nexacro) pytest | **55/55 PASS** |
| Stage 1 (rdb-skill) pytest | **43/43 PASS** (Task 6b 의 +2 신규 포함) |
| business-fullstack-creater golden tests | **3/3 PASS** (Phase A + B + C 동시 green) |
| 작업 commit 수 (per-file) | Stage 4 **21**, Stage 1 **4**, creator **6** (plan + 5 golden) = **31** |
| Stage 2 (rdb-ddl) 회귀 | 0 (blueprint version 1 유지, `pattern` 필드 무시) |
| Stage 3 (rdb-mybatis) 회귀 | 0 (vanilla/nexacro 양 lane 모두 영향 없음) |
| 하위 호환 깨짐 | 0 (`pattern:` optional, 미지정 시 기존 D2 동작과 byte-identical) |
| 신규 카탈로그 우선순위 | 번들 `<repo>/.claude/skills/karpathy-rdb-nexacro/patterns/` → 글로벌 `~/.karpathy-rdb/catalog/patterns/` → `PatternNotFoundError` (silent fallback 금지) |

---

## 산출물

### Stage 4 — 패턴 카탈로그
- `scripts/pattern_loader.py` — `resolve_pattern(name, bundled, global)` + `PatternNotFoundError` (typed signature, 검색 경로 노출)
- `scripts/form_composer.py` — `compose_form(..., pattern="D2", global_pattern_root=None)` — 패턴별 Jinja env 동적 구성
- `scripts/form_gen.py` — `--default-pattern` CLI 추가, entity-level `pattern:` 우선
- 번들 3종: `patterns/{D2,F1,C1}/{manifest.yaml,form.xfdl.j2,README.md}`
  - **D2** detail-2-tier (검색 + Grid + btn_save) — 기본값, 기존 `templates/form.xfdl.j2` 와 byte-identical
  - **F1** form-1-tier (단일 record 편집, Grid 없음, btn_save 있음)
  - **C1** card-1-tier (검색 + readonly Grid + btn_select, btn_save 없음 — picker/popup)
- 문서: `references/patterns-spec.md` (manifest 필드, 컨텍스트 변수, 카탈로그 우선순위), `SKILL.md` 의 Patterns 섹션
- 레거시 `templates/form.xfdl.j2` 삭제 (D2 가 canonical)

### Stage 1 — Blueprint contract + Contribute 프로토콜
- `references/blueprint-spec.md` — Entity 객체에 optional `pattern: <D2|F1|C1|...>` 추가 + 별도 섹션 (v0.3+)
- `protocols/04-contribute.md` — `/karpathy-rdb contribute --kind pattern <name>` 6-step 프로토콜 추가 (글로벌 카탈로그로 패턴 승격, 항목별 confirm, manifest version 증분, learn-log 기록)
- `scripts/rdb_index.py` — `build_blueprint()` 가 entity frontmatter 의 `pattern:` 을 blueprint 로 전파 (Task 6b — Task 9 시도 중 누락 발견 → 즉시 fix)
- 신규 테스트: `tests/test_pattern_propagation.py` (2 cases: 부재 시 미전파, 존재 시 전파)

### Creator — Golden E2E
- `tests/golden/v0.3-phase-c-patterns/` — 주문관리 도메인 wiki (order_header=D2, order_detail=F1, code_master=C1)
- `tests/golden/test_v0_3_phase_c_patterns.py` — Stage 1 → Stage 2 → Stage 4 통과 + 3 패턴별 xfdl 분기 단언 (D2 has Grid+save / F1 no Grid has save / C1 has Grid no save has select)

---

## 5축 자체 리뷰

> 사용자 의도 거울:
> "반복되는 데이터를 효율적으로 관리 / 도메인·엔티티를 지식으로 누적 / 풍부한 Seeds / 완성도 높은 WAR"

| # | 축 | 점수 | 근거 |
|---|---|---|---|
| 1 | 반복 데이터 효율 관리 | **5/5** | 동일 blueprint 1벌로 3종 form layout 을 분기 생성. entity frontmatter 의 한 줄 `pattern:` 이 form 구조 전체를 결정 — 같은 컬럼 정의를 detail/edit/picker 용으로 재정의할 필요 0. 패턴 자체도 manifest + jinja 2 파일 단위, 새 패턴 추가 시 단 3 파일. |
| 2 | 도메인·엔티티 지식 누적 | **5/5** | Stage 1 의 `pattern:` 전파 + Stage 4 의 글로벌 fallback (`~/.karpathy-rdb/catalog/patterns/`) + `/contribute --kind pattern` 프로토콜로 패턴이 **프로젝트 간 누적**되는 양방향 루프 완성. M-A (entity), M-B (lane), M-C (form pattern) 누적: 카탈로그 차원 3종. |
| 3 | 풍부한 Seeds | **4/5** | 패턴 카탈로그 자체는 3 패턴으로 출발 (D2/F1/C1) — 적지만 의도된 minimum. 핵심 진전은 **카탈로그 슬롯의 존재** — 다음 프로젝트에서 사용자가 contribute 시 카탈로그가 N 패턴으로 성장하는 길이 열림. 실 사용 1~2 회 후 seed 풍부도 재평가. |
| 4 | 완성도 높은 WAR | **5/5** | 같은 entity 가 메인 화면 (D2), 디테일 편집 폼 (F1), 검색-선택 popup (C1) 으로 즉시 생성 — 한 도메인이 다중 화면 유스케이스를 한 컴파일에 커버. NexacroBase / 기존 _form_ 폴더 구조 완전 호환 (overlay 기존 정책 유지). |
| 5 | Karpathy 정신 | **5/5** | 패턴 선택은 **명시적 frontmatter / CLI 플래그** (auto-detection / magical 추론 0). 카탈로그 파일 기반 (manifest + jinja), `PatternNotFoundError` 명시 (silent fallback 금지). LLM / 벡터 DB 의존 0. blueprint `version: 1` 유지. `/contribute` 는 항목별 사용자 confirm 강제. |

**평균: 4.8 / 5** ✅ Pass (≥ 4)

---

## 의도와 어긋난 부분 명시

- **Task 6 vs 6b 갭** — Plan 상 Task 6 은 "Stage 1 blueprint-spec.md 갱신" 만 명시했고 실제 코드 전파는 누락했음. Task 9 implementer 가 spec compliance review 전 baseline 확인 단계에서 발견, 즉시 BLOCKED 보고 → orchestrator 가 Task 6b 로 분리 fix (rdb_index.py 5줄 변경 + 2 테스트). 결과적으로 자동 검증 루프가 정상 작동했지만 **plan 자체의 누락 책임** 은 남음 — 다음 phase plan 작성 시 "스펙 문서 변경 = 반드시 코드 변경 동반 확인" 체크리스트 항목 추가 필요.
- **글로벌 카탈로그 활성화 미관측** — 개발 환경에 `~/.karpathy-rdb/catalog/patterns/` 미존재 → bundled fallback 만 검증됨. 글로벌 fallback 은 단위 테스트 (`test_pattern_loader.py:test_global_fallback`) 에서 tmp_path 시뮬레이션으로만 통과. 실 사용자 환경에서 `/contribute --kind pattern` 실행 후 다음 프로젝트에서의 자동 해석은 v0.3.0 사용자 검증 단계에서 1회 확인 권장.
- **endpoints v2 (vanilla) ↔ patterns (nexacro) 분리** — 의도된 갭. Stage 4 는 nexacro lane 만 소비, vanilla 는 form_gen 비대상. v0.4 web-frontend 도입 시 vanilla 측에도 별도 pattern 카탈로그가 필요해질 수 있으나 현재 범위 아님.

기타 어긋남: **없음**.

---

## Phase C 종합

- **3 patterns**, **31 commits** (per-file 디시플린 1건 미세 위반 제외 — Task 2 b83f14e 에서 form_composer 수정과 templates/form.xfdl.j2 삭제 동시 staging, 기능 동등성으로 수용)
- **회귀 0건** — Stage 1 (43/43), Stage 2 (영향 없음, blueprint version 1 유지), Stage 3 (lane 분기 무관), Stage 4 (55/55), creator golden (3/3, Phase A+B+C 동시 green)
- **하위 호환 100%** — `pattern:` 미사용 blueprint 는 D2 로 처리되어 기존 산출과 byte-identical

---

## Gate 결정

✅ **PASS** — v0.3 단위 확장 (Task #101) 의 3 Phase 모두 완료. 다음 단계:
1. Phase A + B + C 통합 검증을 위한 **v0.3.0 태그** (Stage 1/2/3/4 4 레포 동시) — 사용자 승인 후 push
2. `docs/USER-GUIDE.md` 의 v0.3 항목 갱신 (3 lane / 3 pattern / `/contribute --kind` 사용법)
3. 후속 phase 진입 시 사용자 승인 다시 받기 — 현재 후보:
   - #103 v0.4 CLI scaffold (4 stage 자동화, 단기 #1 핵심가치 80%)
   - #97 v0.5 어댑터화 (단기 #2)
   - #102 v0.3.x C-2 도메인 추천 (catalog 5+ 도메인 누적 시)

다음 단계 진입 전 사용자에게 5축 점수 + 상기 후보 보고.
