# M-H2 Gate — v0.5 Phase H2 (MySQL Adapter 실증)

**Date:** 2026-05-19
**Phase:** v0.5 H2 — Backend Adapter Contract 의 첫 신규 어댑터 실증 (MySQL)
**Verdict:** ✅ PASS (평균 4.4 / 5)

---

## 1. 산출물 요약

| 위치 | 변경 | 비고 |
|:--|:--|:--|
| `andrej-karpathy-rdb-ddl/scripts/dialect.py` | `MYSQL = Dialect(...)` 추가 + `_REGISTRY` 등록 | 9 키 type_map, INSERT IGNORE, REGEXP, native schema |
| `.../templates/mysql/schema.sql.j2` | `CREATE SCHEMA IF NOT EXISTS ... utf8mb4` | 신규 |
| `.../templates/mysql/tables.sql.j2` | `CREATE TABLE` + InnoDB + 백틱 식별자 | 신규 |
| `.../templates/mysql/constraints.sql.j2` | FK / CHECK ALTER | 신규 |
| `.../templates/mysql/indexes.sql.j2` | `CREATE [UNIQUE] INDEX` + 8.0.29 inline 노트 | 신규 |
| `.../templates/mysql/seed.sql.j2` | `INSERT IGNORE ... VALUES (...)` | 신규 |
| `andrej-karpathy-rdb-ddl/scripts/ddl_compile.py` | `--dialect` choices `[postgres, hsqldb, mysql]` | 확장 |
| `business-fullstack-creater/scripts/scaffold_cli.py` | 동상 — 오케스트레이터 CLI | 확장 |
| `business-fullstack-creater/scripts/scaffold_orchestrator.py` | dataclass 주석 갱신 | 마이너 |
| `andrej-karpathy-rdb-ddl/tests/test_dialect_mysql.py` | 4 케이스 (등록 / 9키 / 값 / 정책) | 신규 |
| `andrej-karpathy-rdb-ddl/tests/test_ddl_gen_mysql.py` | 4 케이스 + 1 skip (inDB) | 신규 |
| `adapters/backend-contract.md` | §10 H2 환류 + status v0.5 H2 | 환류 |
| `adapters/README.md` | Reference impls + H2 진행행 ✅ | 갱신 |

테스트 결과: **94 passed / 1 skipped** (rdb-ddl 전체) — 회귀 0건.

---

## 2. 5축 자체 리뷰

| 축 | 점수 | 근거 |
|:-:|:-:|:--|
| 1 반복 데이터 효율 관리 | **4** | MySQL 추가가 dialect.py + 5 .j2 만으로 끝남 — codegen 본체 무수정. 한편 `seed.sql.j2` 가 contract 의 `idempotent_insert` 슬롯을 실제로 쓰지 않는 사실이 H1 점수에 묻혀 있었음 → -1. |
| 2 도메인·엔티티 지식 누적 | **5** | catalog/preset 와 직교적으로 동작 (Karpathy 가드 §7 통과). 새 dialect 가 누적 메커니즘 건드리지 않음. |
| 3 풍부한 Seeds | **4** | MySQL 타겟에서도 동일 blueprint → 동일 seed 산출 검증. `INSERT IGNORE` 가 idempotent rerun 보장. -1 사유: 골든 fixture 가 customer/address 만 — junction/composite-PK MySQL 검증은 H2.x 로 이연. |
| 4 완성도 높은 WAR | **4** | MySQL 타겟 프로젝트의 Stage 2 산출물이 InnoDB + utf8mb4 + 백틱 + DATETIME 으로 production-ready. 다만 8.0.29 미만 indexes idempotency 는 운영자 책임으로 위임 (인라인 주석). |
| 5 Karpathy 정신 | **5** | 단방향 + 파일 기반 + idempotent + 누적 무관 모두 유지. H1 contract 가 정확히 예측한 envelope 내에서 끝났다는 자체가 정신의 실증. |

**평균: 4.4 / 5 — PASS** (H1 4.2 보다 +0.2, 실증으로 신뢰도 상승)

---

## 3. 의도 거울 검증

> "반복되는 데이터를 효율적으로 관리 / 도메인·엔티티를 지식으로 누적 / 풍부한 Seeds / 완성도 높은 WAR"

- ✅ 반복 데이터: 같은 entity 가 postgres/hsqldb/mysql 세 dialect 로 자동 분기 — 사용자 추가 노력 없이 3 타겟 커버
- ✅ 누적: H1 의 backend-contract.md 가 MySQL 등록으로 실증되었고 §10 환류로 contract 자체가 한 단계 더 정밀해짐
- ✅ Seeds: customer/address sample 이 MySQL 에도 idempotent 하게 적용 가능
- ✅ WAR: production grade MySQL DDL 산출 (charset/collation/engine 명시)

의도 정렬 양호. 어긋남 없음.

---

## 4. 잔여 위험 / 다음 단계

| # | 위험 | 다음 phase 처리 |
|:-:|:--|:--|
| R1 | postgres `seed.sql.j2` 가 contract slot `{{ idempotent_insert }}` 미사용 (하드코딩) | v0.5.1 patch — postgres/mysql/hsqldb seed 템플릿 일괄 슬롯화 |
| R2 | `regex_op=None` fallback 정책이 contract §2.4 에 양가 표현 | v0.5.1 — `regex_fallback` 필드 도입 검토 |
| R3 | MySQL 5.x/8.x 버전별 차이가 contract 슬롯 미보유 | v0.5.1+ — `engine_version_notes` 도입 검토. 지금은 inline 주석으로 충분 |
| R4 | inDB 회귀 미실행 (embedded MySQL 부재) | docker-compose MySQL 기동 후 H2.x patch 에 추가 — 단, contract 가 옵션으로 명시 (§6) |
| R5 | Oracle/SQLServer/SQLite 어댑터 부재 — contract 일반화 실증이 mysql 1건뿐 | 향후 사용자 요구 시 동일 절차 반복. SQLite 가 schema_strategy="prefix" 의 첫 사용자가 될 것 |

R1~R3 은 본 phase 종료 후 별도 v0.5.1 마이크로 패치로 처리 — H2 종료 자체를 지연시키지 않음.

---

## 5. 결정

- ✅ M-H2 PASS — H3 진행 가능
- 🟢 H3 우선순위: service-contract lane suffix v2 일반화 (`suffix = "" if lane == "nexacro" else f".{lane}"`) + jakarta/javax lane 추가
- 🟡 v0.5.1 backlog 등록: R1 (seed slot), R2 (regex policy), R3 (version notes), R4 (docker mysql inDB)

---

**End of M-H2 Gate.**
