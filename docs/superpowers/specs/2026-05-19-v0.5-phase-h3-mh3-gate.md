# M-H3 Gate — v0.5 Phase H3 자체 리뷰

**날짜:** 2026-05-19
**Phase:** H3 — lane suffix v2 + jakarta/javax 어댑터 (service-contract 실증)
**계약 문서:** [adapters/service-contract.md](../../../adapters/service-contract.md) v2

---

## 산출물 요약

| 영역 | 파일 | 변경 |
|:-|:-|:-|
| Stage 3 codegen | `andrej-karpathy-rdb-mybatis/scripts/codegen.py:32` | suffix v2 일반화 (`"" if lane=="nexacro" else f".{lane}"`) |
| Stage 3 precompute | `andrej-karpathy-rdb-mybatis/scripts/precompute.py` | `build_domain_fields` 에 `column`/`is_pk`/`is_search` 추가 (하위호환 — 기존 nexacro/vanilla 템플릿이 새 키 무시) |
| Stage 3 precompute | `andrej-karpathy-rdb-mybatis/scripts/precompute.py` | `build_save_branches`: jakarta/javax 가 vanilla 와 동일 I/U/D 분기 |
| Stage 3 precompute | `andrej-karpathy-rdb-mybatis/scripts/precompute.py` | `endpoint_base`: nexacro만 `/{name}`, 나머지 `/api/{name}` (jakarta/javax 표준 REST) |
| Stage 3 templates | 6개 신규 .j2 | `domain/entity.{jakarta,javax}.java.j2`, `service/service-impl.{jakarta,javax}.java.j2`, `controller/controller.{jakarta,javax}.java.j2` |
| Stage 3 CLI | `andrej-karpathy-rdb-mybatis/scripts/compile.py:24` | `--lane` choices 에 `jakarta`/`javax` 추가 |
| Orchestrator CLI | `business-fullstack-creater/scripts/scaffold_cli.py:61` | 동일 choices 확장 |
| Orchestrator dataclass | `business-fullstack-creater/scripts/scaffold_orchestrator.py:16` | lane Union 주석 갱신 |
| Stage 3 tests | `tests/test_codegen_lane_suffix.py` (신규) | 4 lane × 6 파일 렌더 검증 |
| Stage 3 tests | `tests/test_codegen_lane_jakarta.py` (신규) | jakarta.persistence import + @Entity/@Table/@Id/@Transient + REST controller + I/U/D 분기 |
| Stage 3 tests | `tests/test_codegen_lane_javax.py` (신규) | javax.persistence 동일 검증 |
| 계약 문서 | `business-fullstack-creater/adapters/service-contract.md` | Status H1→H3, ref impls + jakarta/javax, v2 row, §10 환류 추가 |
| 인덱스 | `business-fullstack-creater/adapters/README.md` | Middle service ref impls + H3 진행행 ✅ |

**테스트 결과:** rdb-mybatis `pytest` — **72 passed** (기존 59 + 신규 13). 회귀 0.

---

## 사용자 의도 거울 (5축 자체 리뷰)

> "반복되는 데이터를 효율적으로 관리 / 도메인·엔티티를 지식으로 누적 / 풍부한 Seeds / 완성도 높은 WAR / Karpathy 정신"

| # | 키워드 | 점수 | 평가 |
|:-:|:-|:-:|:-|
| 1 | 반복 데이터 효율 관리 | **5** | suffix 분기 한 줄 일반화로 lane 추가 시 `codegen.py` 무수정. lane × 슬롯 매트릭스가 코드가 아닌 파일명 컨벤션으로 표현됨 — 같은 정보 두 번 정의 안 함. |
| 2 | 도메인·엔티티 지식 누적 | **4** | 같은 blueprint 가 nexacro/vanilla/jakarta/javax 4 lane 으로 동시 생성 가능. 도메인 지식은 1회 작성, lane 변종은 무비용. (catalog 누적과 직교라 5점은 H4 이후) |
| 3 | 풍부한 Seeds | **4** | jakarta/javax 가 reference impl 로 들어가며 service-contract 실증 사례 +2. 다음 어댑터(예: kotlin lane) 작성자가 참조할 패턴 명확. |
| 4 | 완성도 높은 WAR | **5** | JDK17+ Spring Boot 3 (jakarta), JDK8/11 Spring Boot 2 (javax) WAR 를 동일 도메인 정의로 양쪽 생성 가능 — 운영 환경 다양성 직접 흡수. |
| 5 | Karpathy 정신 | **5** | 파일 기반 + 단방향 + LLM 컴파일러 모델 보존. suffix 규칙은 "코드 분기" 가 아닌 "파일명 컨벤션" — 룰이 데이터화됨. |

**평균: 4.6 / 5**

---

## 의도 어긋남

**없음.** Phase H3 의 모든 변경이 5축 키워드와 직접 정렬.

다만 다음 갭은 **명시적으로 후속 milestone 으로 이연**:

| Gap | 후속 |
|:-:|:-|
| JPA `@Transactional` 미부착 | H4 또는 v0.5.1 |
| JPA 사용 시 mapper.xml 과 역할 분담 | v0.6 |
| controller 응답 envelope 표준화 (`ResponseEntity<DTO>`) | v0.6 |

이 갭은 service-contract.md §10.3 에 기록됨 → 잊히지 않음.

---

## 회귀 확인

- `cd D:/AI/workspace/andrej-karpathy-rdb-mybatis && python -m pytest -q` → **72 passed** in 4.57s
- nexacro/vanilla 기존 59 테스트 100% 통과 — precompute 확장이 추가 키만 emit 하고 기존 키는 그대로
- 신규 lane 13 테스트 100% 통과

---

## Gate 판정

**PASS — 평균 4.6 / 5, 어긋남 없음, 회귀 0.**

다음으로 H4 (UI slot + react/vue) 또는 v0.5.0 tag 결정. H4 는 별도 phase 로 진행.
