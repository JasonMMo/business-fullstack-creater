# M-H4 Gate — v0.5 Phase H4 자체 리뷰

**날짜:** 2026-05-19
**Phase:** H4 — UI overlay 레지스트리 + react 스켈레톤 (ui-contract 실증)
**계약 문서:** [adapters/ui-contract.md](../../../adapters/ui-contract.md) v2

---

## 산출물 요약

| 영역 | 파일 | 변경 |
|:-|:-|:-|
| Stage 5 registry | `business-fullstack-creater/scripts/ui_overlay_registry.py` (신규) | `REGISTRY` dict + `register()` / `dispatch()` / `registered()` |
| Stage 5 dispatch | `business-fullstack-creater/scripts/stage5_overlay.py` | `run_overlay(*, ui="nexacro", **kwargs)` 디스패처. 기존 본체 → `_nexacro_overlay_run`. `register("nexacro", ...)` 자체 등록 + 옵션 `import react_overlay` |
| Stage 5 react | `business-fullstack-creater/scripts/react_overlay.py` (신규) | endpoints.json 읽어 `frontend/src/api/<entity>.ts` fetch hook (selectDataListMap / saveDataListMap) emit. conflict-scan + 1-shot `.bak`. `register("react", run)` |
| CLI | `business-fullstack-creater/scripts/scaffold_cli.py` | `--ui` choices=("nexacro","react"), default "nexacro". ScaffoldArgs.ui 전달 |
| Orchestrator | `business-fullstack-creater/scripts/scaffold_orchestrator.py` | `ScaffoldArgs.ui` 필드 + `run_overlay(ui=args.ui, ...)` pass-through + report 에 `ui` 행 |
| Tests | `tests/test_ui_overlay_registry.py` (신규) | register/dispatch, 미등록 KeyError, 3rd-party 등록, default 디스패치 (4 tests) |
| Tests | `tests/test_react_overlay.py` (신규) | 모듈 emit, endpoint path 포함, conflict-scan, .bak idempotent, report shape, 레지스트리 디스패치 (6 tests) |
| 계약 문서 | `adapters/ui-contract.md` | Status H1→H4, ref impls + react, §2 (예정)→✅, §5.2/§5.3 실제 시그니처, v2 row, §10 환류 |
| 인덱스 | `adapters/README.md` | Front-end UI ref impls + H4 ✅ |

**테스트 결과:** business-fullstack-creater `pytest` (8 파일) — **48 passed** (기존 38 + 신규 10). 회귀 0.

---

## 사용자 의도 거울 (5축 자체 리뷰)

> "반복되는 데이터를 효율적으로 관리 / 도메인·엔티티를 지식으로 누적 / 풍부한 Seeds / 완성도 높은 WAR / Karpathy 정신"

| # | 키워드 | 점수 | 평가 |
|:-:|:-|:-:|:-|
| 1 | 반복 데이터 효율 관리 | **5** | UI 분기가 `if/elif` 가 아닌 레지스트리 한 줄. 새 UI 추가 시 `stage5_overlay.py` 무수정 — 슬롯이 데이터화됨. `endpoints.json` 1개 정의로 nexacro+react 양쪽 fetch 코드 자동 생성. |
| 2 | 도메인·엔티티 지식 누적 | **4** | 같은 blueprint 가 nexacro WAR 와 react frontend 양쪽으로 동시 흐름. 도메인 정의 1회로 두 UI 스택 산출물. 풀 .tsx pattern catalog 누적은 v0.6 예정이라 5점은 아님. |
| 3 | 풍부한 Seeds | **4** | react 가 reference impl 으로 등록되며 UI 어댑터 작성 패턴 명확화 — 다음 작성자 (vue, svelte 등) 가 register() 한 줄 + run() 하나로 슬롯 채울 수 있는 envelope 박스화. |
| 4 | 완성도 높은 WAR | **5** | nexacro Spring Boot WAR + react SPA 가 동일 도메인·동일 endpoints 로 함께 산출. backend 어댑터 (postgres/mysql/hsqldb) × middle lane (nexacro/vanilla/jakarta/javax) × UI (nexacro/react) 매트릭스 완성도 직접 확장. |
| 5 | Karpathy 정신 | **5** | 파일 기반 + 단방향 + LLM 컴파일러 모델 보존. 디스패치가 코드 분기가 아닌 **레지스트리 (데이터)**. fetch hook 의 endpoint path 가 `endpoints.json` (단일 진실 원천) 에서 직접 옴 — UI 가 바뀌어도 fetch 표면 안정. |

**평균: 4.6 / 5**

---

## 의도 어긋남

**없음.** Phase H4 의 모든 변경이 5축 키워드와 직접 정렬.

다만 다음 갭은 **명시적으로 후속 milestone 으로 이연** (ui-contract.md §10.3):

| Gap | 후속 |
|:-:|:-|
| react 본격 pattern 시스템 (.tsx.j2 + manifest.yaml) | v0.6 |
| vue / svelte 어댑터 | v0.6 (register() 한 줄 패턴) |
| SWR / React Query 등 fetch 백엔드 선택지 | v0.6 (manifest 외부화) |
| Stage 4 vs Stage 5 react adapter 책임 분리 | v0.6 |

이 갭은 ui-contract.md §10.3 에 기록됨 → 잊히지 않음.

---

## 회귀 확인

- `python -m pytest tests/test_*.py -v` (8 파일 전체) → **48 passed**
- 기존 38 테스트 100% 통과 — stage5_overlay 가 `run_overlay(*, ui="nexacro", **kwargs)` 로 default 디스패치 → 모든 v0.4 호출자 무변경
- 신규 10 테스트 100% 통과 (registry 4 + react overlay 6)

---

## Gate 판정

**PASS — 평균 4.6 / 5, 어긋남 없음, 회귀 0.**

H4 완료로 v0.5 어댑터화 3-tier 매트릭스 모두 실증:
- backend: postgres / hsqldb / **mysql** (H2)
- middle:  nexacro / vanilla / **jakarta** / **javax** (H3)
- ui:      nexacro / **react** (H4)

다음 단계: 전체 풀 pytest sweep (rdb-skill, rdb-ddl, rdb-mybatis, rdb-nexacro, business-fullstack-creater) → 통과 시 v0.5.0 태그.
