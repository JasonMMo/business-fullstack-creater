# learn-log.md — business-fullstack-creater

> **역할 분리** (G5, 2026-05-21): CLAUDE.md는 "변하지 않는 원칙·절차", 이 파일은 "매 Growth마다 누적되는 활동 원장(ledger)". 새로 배운 도메인·구현 패턴·dialect 트랩·검증 결과를 1줄 단위로 환류한다.

**기록 컨벤션 (CLAUDE.md §작업시체크리스트 3번 항목 구체화):**

```
YYYY-MM-DD | Growth-N | <kind> | <name> | <domain/lane/dialect> | <한줄요약>
```

`<kind>` 카테고리:
- `domain` — 새 도메인 preset 추가/확장
- `entity` — 신규 entity·관계
- `pattern` — frontend pattern (D2/F1/C1/L2/MD/TR/RO/SHELL...)
- `lane` — backend lane 추가/확장 (vanilla/javax/jakarta/nexacro)
- `dialect` — dialect 어댑터 (postgres/hsqldb/mysql) 트랩 또는 확장
- `overlay` — UI overlay (nexacro/react) 패턴
- `validation` — 라이브 WAS·JDBC·Maven 검증 이정표
- `bug` — 발견된 codegen/template 결함 (환류 task ID 동봉)
- `convention` — 패턴화 결정 (seed 작성법, FK seed id, MERGE explicit-id ...)

---

## 0. Layer Ownership Card (Phase A — 2026-05-22)

6축 책임표의 활동 뷰. CLAUDE.md §핵심 운영 원칙 표가 "변하지 않는 책임", 이 표는 "현재 누적 상태"다. Growth 마다 트랩 카운트와 미해결 환류를 갱신한다. **Growth-63 (2026-05-28)**: 6번째 축 `customer` 신규 도입 — `profiles/<slug>.yaml` 한 장으로 고객 고유 관습(패키지/URL/JDBC/lane/ui)을 묶어 `/scaffold --customer-profile <name>` 로 Stage 1–5 전체에 일관 적용. 2번째 도메인부터 복리 효과(손 댈 곳 없이 ACME 화).

| 축 | 깊이 누적 위치 | 단위 테스트 디렉토리 | 누적 트랩 | 미해결 환류 |
|---|---|---|---|---|
| **skill (Stage 1)** | `andrej-karpathy-rdb-skill/.claude/skills/karpathy-rdb/presets/*.seed.md` + `protocols/` + `scripts/meta_extract.py`(v0.6 C-1) + `presets/INDEX.md` 3단계 fallback(v0.6 C-2) | `andrej-karpathy-rdb-skill/tests/` (194 그린) | 0 | — |
| **ddl (Stage 2)** | `andrej-karpathy-rdb-ddl/catalogs/preset-catalog.yaml`, dialect 어댑터(postgres/hsqldb/mysql) | `andrej-karpathy-rdb-ddl/tests/` | 3 (HSQLDB IDENTITY 0-base, SQL:2008 `LEAD`, HSQLDB vs postgres-default schema) | — |
| **mybatis (Stage 3)** | `andrej-karpathy-rdb-mybatis/templates/<lane>/`, lane(nexacro/vanilla/jakarta/javax) | `andrej-karpathy-rdb-mybatis/tests/` | 7 (explicit-id MERGE, `@Mapper` bean, typed seed sentinel, REST snake_case URL, Map placeholder case, nexacro `_RowType_` 안전추출, +T-NexacroUiaPkg-javax: nexacro controller/serviceImpl 의 `.jakarta.core.` 하드코딩 → `--uia-namespace` 파라미터) | — |
| **nexacro (Stage 4+5)** | `andrej-karpathy-rdb-nexacro/patterns/`(D2/F1/C1/L2/MD/RO/TR/TG/MT/PS/MDS), UI overlay(nexacro/react) | `andrej-karpathy-rdb-nexacro/tests/` | 0 | — |
| **creater (Orchestrator)** | `business-fullstack-creater/.claude/commands/`(scaffold·full-test·growth-start·contribute-back·cleanup-runner·list-domains·**diagnose**[+cross-layer-coherence Growth-53; +G-58 guard Growth-60; +G-61 guard Growth-62; +G-62 guard Growth-63; +G-69/70/71/72 guards Growth-69/70/71/72; +G-74 guard Growth-74 (M3 Slice b: orchestrator emit_ops_pack auto-emit contract); +G-75 guard Growth-75 (M3 Slice c: web /ops.zip route + single-source rule); +G-76 guard Growth-76 (M3 Slice d: Vault Agent sidecar emitter contract); +G-77 guard Growth-77 (M3 Slice e: Keycloak/OIDC SSO sidecar emitter contract); +G-78 guard Growth-78 (M5 Slice C-b: extract_target_profile Gradle 입력 — `build.gradle`(Groovy DSL) / `build.gradle.kts`(Kotlin DSL) 파싱 + `settings.gradle(.kts) rootProject.name` 폴백 → 동일 v1 customer profile 출력 계약); +G-79 guard Growth-79 (M5 Slice C-c: `/target/upload` 웹 통합 — `web/routes/target.py` + `web/adapters/target_extractor.py` 가 `extract_target_profile.build_profile/dump_profile` 위임 single-source 규칙 → 비 CLI 사용자가 zip 업로드만으로 v1 customer profile 획득); +G-80 guard Growth-80 (M3 Slice e+α: emit_ops_pack.py multi-client realm + LDAP federation — `--sso-ldap` CLI + `overlay.sso_clients` / `overlay.sso_ldap` / 새 `ldap:` profile section → Keycloak realm.json 의 `clients` 배열 멀티 + `components.org.keycloak.storage.UserStorageProvider` LDAP federation block 주입, `${BIND_PW}` env interpolation 보존); +G-81 guard Growth-81 (M5 Slice C-d: extract_target_profile.py settings.gradle include 파싱 + modules: list 출력 + diagnose G-81 가드 + 풀테스트 PASS); +G-82 guard Growth-82 (M5 Slice C-e: /target/upload XHR progress bar + diff 뷰 — target_upload_partial.html + target_upload.js + difflib unified_diff + diagnose 20→21 가드)]·**orient**·**web-build**[Growth-55]) + `scripts/workflow/`(full_test·live_*·lane_runner_map·learn_log·list_domains·**diagnose**·**orient**·**web_index**[Growth-55; +catalog-slug-mismatch fix Growth-61; +status_board section inject Growth-72]·**status_board**[Growth-72: 6-axis 누적 자산 가시화 — `compute()` learn-log §1/§2/§6 + diagnose.py + profiles/ 파싱 → portal HTML 조각 + JSON snapshot]) + `scripts/scaffold_cli.py`(+lane-aware `--ui` default Growth-59: vanilla→react, others→nexacro; +`--customer-profile` + `load_customer_profile()` + `resolve_with_profile()` 머지 헬퍼 Growth-63) + `docs/`(index.html·domain/·assets/ — Web v0 정적 포털, Growth-55) | `business-fullstack-creater/tests/` | 8 (javax lane URL 컨벤션, JDK8 source/target on JDK17 JAVA_HOME, +T-Probe-LaneRunner-Mismatch RESOLVED Growth-48: scaffold-lane override via `scaffold-report.md` lane line, +T-Probe-CtxPath-Missing RESOLVED Growth-50: REST probe URL 에 `/uiadapter` context-path prefix 누락 — 모든 runner application.yml `server.servlet.context-path: /uiadapter` 통일, nexacro envelope 만 prefix 갖고 REST 는 미포함 → 404, +T-Cleanup-PSSubexpr RESOLVED Growth-54: `cleanup_runner.py` 의 `Where-Object { (git ls-files ...; $LASTEXITCODE) -ne 0 }` ScriptBlock statement-list 가 array-vs-int 비교로 빠져 untracked mapper 판별 실패 → Python `git ls-files` 단일 호출 + tracked set 멤버십 + `Path.unlink()` 로 교체, Step dataclass 에 `python: Callable` hook 추가, +T-Web-EmptyPortal RESOLVED Growth-57: `/web-build` 단독 실행 시 `~/.karpathy-rdb/catalog/` 미초기화로 14×4=56 슬롯 placeholder fallback → portal 첫인상 빈약. 환류: (b) `web_index._format_placeholder(slug, fname)` 가 placeholder 본문에 `run /scaffold {slug}` + catalog 경로 inline, (a) `BuildResult` 에 `placeholder_slots/total_slots/catalog_root_missing` 필드 + `main()` 종료 직전 `⚠ EMPTY-PORTAL N/M ...` CLI 배너. `build(docs_root/learn_log_path)` 디폴트 def-time 바인딩 → `Optional[Path] = None` call-time 해석으로 교체. 회귀 5 신규 테스트 + 425/425 그린, +T-Stage4-VanillaReject RESOLVED Growth-58: Stage 3 vanilla lane endpoints.json 이 `version: 2 lane: vanilla` REST schema 산출 → Stage 4 nexacro form_gen 의 `endpoints_loader.py` `N002 unsupported version` 거부 → Growth-50 `--stop-after-stage 3` 수동 우회. **환류**: `scaffold_orchestrator._run_stage4` 진입에 `if args.lane == "vanilla"` 분기 → stderr 알림 후 `report.stages_run.append("stage4-skipped-vanilla")` early-return, `_write_report` stage-order tuple 에 `stage4-skipped-vanilla` 추가, scaffold-report.md SKIPPED 마커 표기. 회귀 3 신규 테스트(vanilla skip + 3 non-vanilla lane 정상 호출 가드 + scaffold-report 마커) + 428/428 그린, +T-Web-CatalogSlugMismatch RESOLVED Growth-61: `web_index._default_source_resolver` 가 `discover_scaffold(scaffold_root, domain_slug=entry.domain)` 로 호출 — `entry.domain` 은 catalog 한글 서브디렉터리명(`고객관리`), Java 패키지는 ASCII slug(`customer`) → `discover_scaffold` 가 한글 slug 로 `com.example.고객관리/...Controller.java` 탐색 실패 → DDL/Mapper(slug-agnostic glob)는 resolve 되지만 Controller/Service(slug-aware) 만 silent placeholder fallback. **환류**: `domain_slug=None` 전달로 `derive_domain_slug()` 가 scaffold tree(`com.nexacro.uiadapter.<slug>` / `com.example.<slug>`) 에서 ASCII slug 자동 추론. 회귀 2 신규 테스트(`test_default_source_resolver_handles_korean_catalog_with_ascii_slug` 한글 catalog 서브디렉터리 + ASCII Java slug 정상 resolve / `test_default_source_resolver_returns_none_when_catalog_missing` 카탈로그 부재 graceful) + 438/438 그린) | — |
| **customer (6th axis — Growth-63)** | `business-fullstack-creater/profiles/<slug>.yaml`(version:1 schema — customer{slug/display/status} + per-stage sections ddl/mybatis/nexacro/overlay/auth/defaults/datasource + `${ENV_VAR}` interpolation + `domains_seen` 누적 자리), `profiles/_README.md`(스키마 설명·우선순위·복리 효과표), `scripts/scaffold_cli.py`(`--customer-profile` 플래그 + `load_customer_profile()` + `resolve_with_profile()` 머지 헬퍼 — CLI > profile > default 우선순위), `scripts/scaffold_orchestrator.py`(`ScaffoldArgs.customer_profile: dict` 필드 신설), `scripts/workflow/live_overlay.py`(Growth-64: `derive_scaffold_base_and_slug()` 신설로 customer profile 패키지(`com.acme` 등) 자동 인식 + `OverlayPlan.base_package` 필드 + 5개 하드코딩 사이트 동적 base_pkg 치환 — L4 종단 검증이 profile-driven scaffold 도 지원) | `business-fullstack-creater/tests/` (test_scaffold_cli.py: TestLoadCustomerProfile 9 + TestResolveWithProfile 5; test_live_overlay.py: acme_scaffold 픽스처 + 8 base_package 케이스) | 0 | — (Slice B 완료: Growth-65/66 `domains_seen` write-back + Growth-67 9 gap 필드 wiring + Growth-68 `--url-prefix` 클로즈 + G-63 가드 diagnose.py 정착) |

**갱신 규칙:**
- 새 트랩이 §4 에 추가되면 해당 layer 행의 "누적 트랩" 카운트를 +1 하고 한 줄 요약을 괄호 안에 append
- 미해결 환류가 해결되면 그 텍스트만 지움 (행 자체는 유지)
- `/contribute-back` 체크리스트 항목 5번 (Phase A4) 이 이 표의 어느 행에 기여했는지 묻는다

---

## 1. 라이브 WAS 검증대 상태 (lane × runner)

`nexacroN-fullstack/samples/runners/` 매트릭스. CLAUDE.md §풀테스트 4계층 표 4번을 통과한 lane.

| lane | 디폴트 러너 | 검증 상태 | 비고 |
|---|---|---|---|
| **nexacro** | `boot-jdk17-jakarta` (Spring Boot 3.3, jakarta-for-nexacro) | ✅ Growth-42 (customer, envelope CRUD round-trip) | nexacro lane = jakarta runner + uiadapter overlay; envelope `POST /uiadapter/<entity>/save_datalist_map.do` 5-step CRUD 그린 |
| **jakarta** | `boot-jdk17-jakarta` (Spring Boot 3.3) | ✅ Growth-28 (finance) + Growth-31 (sales) | 2 도메인 횡단 검증 완료 |
| **javax** | `boot-jdk8-javax` (Spring Boot 2.7) | ✅ Growth-32 (sales), ✅ Growth-49 (customer fresh scaffold + nexacro lane — **풀테스트 그린** L1+L2+L3-skip+L4-rebuild+probe200/ec=0/rows=3+envelope CRUD baseline=3→+1=4→final=3) | JAVA_HOME=JDK17, target=1.8 (JDK8 부재 시 우회); G-Jackson(Growth-46) + T-NexacroUiaPkg-javax(Growth-47) + T-Probe-LaneRunner-Mismatch(Growth-48) 3개 픽스 누적 후 종단검증 그린 |
| **vanilla** | (직접 러너 없음 — `boot-jdk8-javax` 호스트 임포트) | ✅ Growth-50 (customer scaffold — **풀테스트 그린 → javax-host 검증**: L1 4-repo pytest rc=0 · L2 schema+seed 12+12 OK · L3 SKIP=PASS(runner-provides-pom) · L4 overlay 26w/2e + mvn rc=0 + REST probe http=200 errcode=0 rows=3 + REST CRUD baseline=3→+1=4→final=3) | controller 템플릿이 `org.springframework.web.bind.annotation.*` 만 사용 → Spring 5/6 (javax.servlet / jakarta.servlet) 양쪽 러너 호환. T-Probe-CtxPath-Missing(Growth-50) 픽스로 첫 REST L4 종단검증 그린. "vanilla → javax-host 검증" 라벨 컨벤션 |

---

## 2. 누적 도메인 카탈로그 (preset-catalog.yaml 동등 뷰)

`andrej-karpathy-rdb-ddl/catalogs/preset-catalog.yaml` 가 진실원천. 여기는 "어떤 Growth에서 추가되었나" 추적.

| 도메인 | preset 파일 | 추가/확장 Growth |
|---|---|---|
| 고객관리 | `고객관리.seed.md` | Growth-1 (customer_category 살붙임) |
| 주문관리 | `주문관리.seed.md` | Growth-2A (order_status_history) |
| 재고관리 | (preset) | Growth-2B (stock_movement) |
| 인사관리 | (preset) | Growth-2C (attendance) |
| 게시판/회원 | `게시판.seed.md` | Growth-2D |
| 배송관리 | (preset) | Growth-15 |
| 권한관리 | (preset) | Growth-9, Growth-21a-1 (oauth_account + refresh_token) |
| 공통코드 | (preset) | Growth-10 |
| 결재 | `결재.seed.md` | Growth-11 |
| 알림 | (preset) | Growth-12 |
| 파일관리 | (preset) | Growth-13 |
| 영업관리 (CRM) | `영업관리.seed.md` | Growth-21b (+1/2/3/4 cross-FK), Growth-31 jakarta 검증, Growth-32 javax 검증 |
| 공급망 (SCM) | (preset) | Growth-22 |
| 재무관리 | `재무관리.seed.md` | Growth-26 (journal_entry double-entry v4), Growth-27 (v5 IDENTITY 트랩), Growth-28 jakarta 검증 |

---

## 3. 누적 패턴 (frontend + lane + dialect + overlay)

| 종류 | 이름 | 추가/검증 Growth |
|---|---|---|
| frontend pattern | D2 (datalist-2-tier) | (초기) |
| frontend pattern | L2 (list-detail) | Growth-3 |
| frontend pattern | MD (master-detail-2-tier) | Growth-4, Growth-5 (compose_form 자동 wiring) |
| frontend pattern | TR (tree-1-tier) | Growth-6 |
| frontend pattern | RO (read-only-list-1-tier) | Growth-7 |
| frontend pattern | F1 / C1 | v0.4 Phase C |
| frontend pattern | SHELL (MDI/SDI) | Growth-16 |
| backend lane | nexacro (jakarta-for-nexacro) | (초기) |
| backend lane | jakarta | v0.5 H3 |
| backend lane | javax | v0.5 H3 + Growth-32 (라이브 검증) |
| backend lane | vanilla | v0.5 H3 |
| dialect | hsqldb | (초기) |
| dialect | postgres | Growth-18 |
| dialect | mysql | v0.5 H2 |
| overlay | nexacro | (초기) |
| overlay | react | v0.5 H4, Growth-24 (OAuth2 로그인 화면) |

---

## 4. dialect / lane 트랩 & 컨벤션

| 항목 | 발견 | 환류 위치 |
|---|---|---|
| **HSQLDB IDENTITY 0-base 트랩** | Growth-27 (재무 v5 E2E), Growth-31 (sales FK 재발) | `seed-conventions.md`, `dialect.py`, USER-GUIDE §3.11 |
| **SQL:2008 예약어 `LEAD`** | Growth-31 | Stage 2 DDL 자동 인용 — overlay 시 schema/data 만 손대면 됨 |
| **explicit-id MERGE 패턴** | Growth-32 | seed의 `MERGE USING (VALUES(...))`에 명시적 `id` 포함하면 IDENTITY 트랩 + `s.id` 참조 누락을 한 번에 회피. Stage 2 seed 템플릿 기본화 대상 (task #240). |
| **javax lane URL 컨벤션** | Growth-32 | `/api/<entity>` (REST). `/uiadapter/<entity>/<method>.do` 는 nexacro lane 한정. |
| **JDK8 source/target on JDK17 JAVA_HOME** | Growth-32 | pom.xml `<source>1.8</source><target>1.8</target>` 가 진실. 사용자 환경에 JDK8 없을 때 우회로. |
| **Spring Boot mapper bean = `@Mapper` 어노테이션 진실원천** | Growth-33 | `boot-jdk8-javax` runner는 `Application.java`에 `@MapperScan` 없음 → MyBatis starter 자동검출이 `@Mapper` per-interface 만 신뢰. 템플릿 (mapper-interface.rest.j2 + mapper-interface.j2) 모두 `@Mapper` 기본 탑재로 환류 (task #246). |
| **타입-aware seed sentinel** | Growth-33 | timestamp/datetime/time 컬럼은 문자열 'TBD' → `CURRENT_TIMESTAMP`/`CURRENT_TIME` 로 자동 치환. dialect-agnostic. (task #245) |
| **REST controller URL = snake_case** | Growth-33 | 생성된 `@RequestMapping` 은 `/api/<table_name>` (snake_case). camelCase 호출은 404. 스모크 테스트 작성 시 주의. |
| **MyBatis Map placeholder case = envelope key case** | Growth-42 | `<Col id="customer_id">` (nexacro envelope snake_case) ↔ `#{customer_id}` 일치 필요. mapper.xml 의 SQL 컬럼은 UPPER 유지(HSQLDB unquoted-normalize 호환), placeholder + `<if test>` key 만 lowercase. case 불일치 = silent INSERT skip (NPE 없음, ErrorCode=0 응답, log에 SQL execute 라인 부재). 환류: mapper.xml.j2 + precompute.py + 테스트 (`rdb-mybatis`). |
| **nexacro service `_RowType_` 안전 추출** | Growth-42 | runner `BoardServiceImpl` 패턴 — `Object raw = row.get(DataSetRowTypeAccessor.NAME); int rowType = (raw instanceof Number) ? ((Number)raw).intValue() : DataSet.ROW_TYPE_NORMAL;` — generated `AddressServiceImpl` 등도 동일 패턴 적용. unsafe `Integer.parseInt((String) row.get(...))` 는 NumberFormatException → 트랜잭션 rollback. 환류: `service-impl.nexacro.java.j2` (`rdb-mybatis`). |
| **HSQLDB schema vs postgres-default scaffold** | Growth-42 | scaffold default dialect=postgres → `BIGINT GENERATED BY DEFAULT AS IDENTITY ... BOOLEAN NOT NULL DEFAULT FALSE` HSQLDB 파싱 실패("unexpected token: DEFAULT"). HSQLDB 검증 시 `--dialect hsqldb` 명시 필요. L2 JDBC smoke 가 dialect mismatch 즉시 노출. |
| **T-NexacroUiaPkg-javax** (nexacro-uiadapter 패키지 변종) | Growth-47 | nexacro-uiadapter 라이브러리는 두 변종: (a) Spring 6 / jakarta.servlet → `com.nexacro.uiadapter.jakarta.core.*` (boot-jdk17-jakarta runner), (b) Spring 5 / javax.servlet → `com.nexacro.uiadapter.spring.core.*` (boot-jdk8-javax runner). 이전엔 mybatis `controller.java.j2` + `service-impl.java.j2` 가 `.jakarta.core.` 하드코딩 → javax runner 빌드 시 `ParamDataSet/NexacroResult/NexacroException/DataSetRowTypeAccessor` symbol-not-found. 환류: `precompute.py`/`codegen.py`/`compile.py` 에 `uia_namespace` 파라미터 추가, 두 템플릿이 `{{ uia_namespace }}.core.*` 사용, `scaffold_cli.py`/`scaffold_orchestrator.py` 가 `--uia-namespace {jakarta|spring}` 노출(기본 jakarta). 회귀: rdb-mybatis 3 신규 테스트(spring variant + jakarta default + invalid value rejection) → 12/12 그린. |
| **T-Probe-LaneRunner-Mismatch** (scaffold lane vs runner deployment conflation) | Growth-47 발견 → **RESOLVED Growth-48** | `lane_runner_map.lane_probe_kind/url/crud_kind/supports_crud` 가 단일 `lane` 인자만 받아 wire protocol(REST vs envelope)을 결정했는데, 그 lane 은 `full_test.py --lane` 인자(러너 선택)와 동일하게 다뤄짐 → nexacro 스캐폴드를 javax runner 위에 올리면 probe 가 `GET /api/{entity}` 호출(실제는 `POST /uiadapter/{entity}/select_datalist_map.do`). **환류** (A안 채택): 4개 dispatcher 에 `scaffold_lane: str \| None = None` optional override 추가 + `_effective_lane()` helper 가 supplied 시 우선. `live_overlay.discover_scaffold_lane(scaffold_dir)` 가 `scaffold-report.md` 의 `- lane: ` `<name>` ` 라인을 regex 로 읽어 반환(파일/라인 부재 시 None — graceful fallback). `full_test.run_l4_live` 가 probe/CRUD dispatch 전 discover 호출 후 4개 dispatcher 모두에 `scaffold_lane=` 전달. 신규 9 테스트(lane_runner_map 5 + discover_scaffold_lane 4 + full_test wiring 2) → 82/82 그린(workflow suite). |
| **T-Probe-CtxPath-Missing** (REST probe URL 컨텍스트 경로 누락) | Growth-50 발견 → **RESOLVED Growth-50** | `lane_runner_map.lane_probe_url` REST 분기가 `http://localhost:{port}/api/{entity}` 반환 → nexacroN-fullstack `samples/runners/` 7개 모두 `application.yml` 에 `server.servlet.context-path: /uiadapter` 설정 → 실 endpoint 는 `/uiadapter/api/{entity}`. nexacro envelope URL 은 항상 prefix 동봉(`/uiadapter/{entity}/save_datalist_map.do`)이라 무증상, REST 는 lane(jakarta/javax/vanilla) 모두 404. **숨겨진 이유**: 이전 jakarta/javax 종단검증은 envelope CRUD 로 수행(scaffold lane=nexacro), vanilla 가 첫 순수 REST L4-probe 케이스. **환류**: `lane_probe_url` 한 줄 수정(`/api/{entity}` → `/uiadapter/api/{entity}`) + 2 unit test 갱신(`test_lane_runner_map::test_lane_probe_url_rest_uses_api_path` + `test_full_test::test_run_l4_live_rest_lane_uses_json_probe` 2건) + 트랩 표 등재. **검증**: vanilla full_test rc=0 풀테스트 그린(라벨 "풀테스트 그린 → javax-host 검증"), L4 REST probe http=200 errcode=0 rows=3, REST CRUD baseline=3→+1=4→final=3. 회귀: 64/64 그린(lane_runner_map + full_test 결합). |
| **T-Stage4-VanillaReject** (Stage 4 nexacro form gen 이 vanilla endpoints.json v2 거부) | Growth-50 발견 (workaround `--stop-after-stage 3`) → **RESOLVED Growth-58** | `andrej-karpathy-rdb-mybatis/scripts/endpoints_emitter.py` 가 `lane="vanilla"` 일 때 `{"version": 2, "lane": "vanilla", ...}` REST-flavored endpoints.json 산출(GET/POST 메서드), nexacro 외 lane 은 `{"version": 1, ...}` envelope endpoints(select_datalist_map/save_datalist_map). 한편 `andrej-karpathy-rdb-nexacro/scripts/endpoints_loader.py` 는 `data.get("version") != 1` 즉시 `N002 unsupported version: {v!r}` 거부 — Stage 4 는 nexacro XFDL form 생성기로 vanilla(순수 REST)에는 만들 form 자체가 없음(설계 의도 일치). 기존엔 `scaffold_orchestrator._run_stage4` 가 무조건 form_gen.py 호출 → 사용자가 `/scaffold ... --lane vanilla` 단순 실행 시 stage4 에서 N002 와 함께 RuntimeError 로 전체 파이프라인 중단. Growth-50 vanilla L4-probe 검증은 `--stop-after-stage 3` 으로 수동 우회. **환류** (a 채택): `_run_stage4` 진입 즉시 `if args.lane == "vanilla"` 분기 → stderr 에 `[stage4] skipped: vanilla lane is pure REST — no nexacro XFDL forms to generate` 출력 후 `report.stages_run.append("stage4-skipped-vanilla")` 로 early-return. `_write_report` 의 stage-order tuple 에 `stage4-skipped-vanilla` 추가, scaffold-report.md 에 `- stage4: SKIPPED (vanilla lane has no nexacro XFDL forms)` 표기. 신규 3 회귀 테스트(vanilla skip → form_gen.py 미호출 + stderr 메시지 / 3개 비-vanilla lane 회귀 가드(stage4 정상 호출 + stage4-skipped-vanilla 부재) / scaffold-report.md SKIPPED 마커 검증). 변경 표면: creater orchestrator 단일 파일 — sibling repo(rdb-nexacro/rdb-mybatis) 무손상. |
| **T-Web-CatalogSlugMismatch** (web_index 가 한글 catalog 서브디렉터리명을 ASCII Java slug 로 오용) | Growth-61 발견 → **RESOLVED Growth-61** | `scripts/workflow/web_index.py:_default_source_resolver` 가 `discover_scaffold(scaffold_root, domain_slug=entry.domain)` 호출 — `entry.domain` 은 `~/.karpathy-rdb/catalog/<한글>` 디렉터리명(예: `고객관리`), 한편 scaffold Java 패키지는 ASCII slug(예: `customer`). `discover_scaffold` 가 `com.example.고객관리/...Controller.java` 탐색 → 매치 0건 → silent None. DDL/Mapper 는 `*.sql` / `mybatis/mappers/*.xml` glob 이라 slug-agnostic 으로 resolve, Controller/Service 만 placeholder 폴백 — 부분 누락이라 발견이 늦음. **환류**: `domain_slug=entry.domain` → `domain_slug=None` 로 단일 인자 교체, `derive_domain_slug()` 가 scaffold tree(`com.nexacro.uiadapter.<slug>` / `com.example.<slug>`) 에서 ASCII slug 자동 추론. 회귀 2 신규 테스트(`tests/workflow/test_web_index_scaffold.py`): (1) `test_default_source_resolver_handles_korean_catalog_with_ascii_slug` — `~/.karpathy-rdb/catalog/고객관리/{schema.sql,mybatis/mappers/customer-mapper.xml,com/example/customer/{controller,service}/Customer{Controller,Service}.java}` 디렉터리 구조 fixture 에서 4 키 모두 None 아닌 실제 Path resolve 검증, (2) `test_default_source_resolver_returns_none_when_catalog_missing` — catalog 디렉터리 부재 시 `discover_scaffold` raise 가 graceful catch 되어 4 키 None dict 반환 검증. 회귀: tests/workflow 277→**279 그린**, tests/ 전체 **438/438 그린**. |
| **T-Web-EmptyPortal** (Web v0 portal 56 슬롯 placeholder fallback) | Growth-56 발견 → **RESOLVED Growth-57** | `web_index._default_source_resolver` 는 `~/.karpathy-rdb/catalog/<domain>` 에서 Stage-2 산출물을 탐색하는데, 사용자가 `/web-build` 를 `/scaffold` 선행 없이 단독 실행하면 catalog 디렉토리가 부재 → 14 도메인 × 4 layer = 56 슬롯 모두 `// not available — generator could not find source` placeholder + zip 내부 4개 파일 54B stub. 골격(14-tile grid, Korean slug URL, 4-tab 구조, zip 무결성, L3 copy-paste snippet) 은 100% 정상 — graceful degradation 자체는 설계 의도. **트랩**: portal 첫인상이 "14 도메인 광고 → 노출 자산 0" 으로 빈약. **환류** (b+a 동시 적용): (b) `web_index._format_placeholder(slug, fname)` 가 layer label(Stage 2/3 출처) + `run /scaffold {slug} first to populate` + `~/.karpathy-rdb/catalog/{slug}/` 경로를 본문에 inline 임베드, (a) `BuildResult` 에 `placeholder_slots`/`total_slots`/`catalog_root_missing` 필드 + `main()` 종료 직전 `⚠ EMPTY-PORTAL: N/M preview slots filled with placeholder (no catalog found at ~/.karpathy-rdb/catalog/). Run /scaffold <domain> first...` CLI 배너. **추가 수정**: `build(docs_root, learn_log_path)` 키워드 디폴트가 def-time 바인딩 → 테스트 monkeypatch 무효 → `Optional[Path] = None` 으로 변경 후 함수 본문에서 call-time 해석(Growth-52 lesson 재적용). 회귀: 5 신규 테스트(`_format_placeholder` 2 + `BuildResult` 슬롯 집계 1 + `catalog_root_missing` 1 + CLI 배너 stderr 1) → workflow 12/12 + 전체 425/425 그린. |

---

## 5. 미해결 환류 항목 (codegen 결함)

발견 시 task를 만들고 여기 1줄로 인덱싱. 해결 시 줄긋고 commit SHA 첨부.

- ~~**task #240** — Stage 2 seed MERGE explicit-id 패턴 기본화 (Growth-32 발견)~~ → fixed in `andrej-karpathy-rdb-ddl` c1c2a6b (seed_gen.py) + e0f27ba (regression tests), 107 passed
- ~~**task #241** — Stage 3 javax/jakarta 도메인 orphan JPA 어노테이션 제거 (Growth-32 발견)~~ → fixed in `andrej-karpathy-rdb-mybatis` 9188339 (javax) + 04fe964 (jakarta), 둘 다 plain POJO
- ~~**task #242** — Stage 3 REST lane service interface 시그니처 동기화 (Growth-32 발견)~~ → fixed in `andrej-karpathy-rdb-mybatis` e696861 (service-interface.rest.j2) + 59e9ef1 (codegen iface_suffix routing)
- ~~**task #243** — Stage 3 REST lane mapper interface CRUD 반환형 int 통일 (Growth-32 발견)~~ → fixed in `andrej-karpathy-rdb-mybatis` a82cdb8 (mapper-interface.rest.j2) + 59e9ef1 (codegen iface_suffix routing). 회귀: 283d755 (javax tests) + 255a1bf (jakarta tests + nexacro sanity), 85 passed
- ~~**task #245** — Stage 2 seed 타입-aware sentinel (Growth-33 발견, datetime 컬럼에 'TBD' 문자열 삽입 → HSQLDB 파싱 실패)~~ → fixed in `andrej-karpathy-rdb-ddl` d7f36e0 (seed_gen.py type detection) + 8163012 (regression test)
- ~~**task #246** — Stage 3 REST lane mapper interface `@Mapper` 어노테이션 누락 (Growth-33 발견, Spring Boot MyBatis starter auto-discovery 의존)~~ → fixed in `andrej-karpathy-rdb-mybatis` 0657292 (mapper-interface.rest.j2) + 5d4bc68 (mapper-interface.j2 일관성). 회귀: 3644050 (javax test) + a87bee9 (jakarta test). Golden 갱신: a227d5e (CustomerMapper) + c8d0f9f (AddressMapper). 87/87 passed
- ~~**gap G-Jackson** (Growth-33 라이브 검증 부분실패 원인) — `boot-jdk8-javax` runner classpath의 Jackson 버전이 `JsonToken.valueDescFor` (2.16+) 미보유 → NexacroResult 직렬화 시 `NoSuchMethodError`~~ → fixed Growth-46 (2026-05-22): `nexacroN-fullstack/samples/runners/boot-jdk8-javax/pom.xml` `<dependencyManagement>` 에 **jackson-bom 2.17.2 import 를 spring-boot-dependencies 보다 먼저** 배치 — Maven BOM first-wins 규칙으로 jackson-core/annotations/databind/datatype-jdk8/datatype-jsr310/module-parameter-names 6개 모두 2.17.2 통일. 검증: (a) `mvn dependency:tree` jackson-* 전부 2.17.2 확인, (b) runner BUILD SUCCESS, (c) `POST /uiadapter/select_datalist_map.do` envelope → HTTP 200 + ErrorCode=0 + NexacroResult dataset rows(`output1`, 23 rows) 직렬화 성공 — `NoSuchMethodError` 없음.

---

## 6. Growth 이력 (요약)

상세는 git log + USER-GUIDE §3.12 검증 이력 표 참조. 여기는 "어떤 살이 붙었나" 인덱스만.

| Growth | 일자 | 살붙임 요약 |
|---|---|---|
| Growth-81 | 2026-05-29 | **M5 Slice C-d — Gradle 멀티 모듈 include 파싱 + modules 리스트 + G-81 guard** (Growth-78 G-78 의 후속, Gradle multi-module projects 의 `settings.gradle include` 선언 파싱 → profile `modules:` 리스트 자동 첨부). `_GRADLE_INCLUDE_RE` / `_parse_includes` / `_extract_modules` 헬퍼 신설. `parse_gradle()` 이 `_extract_modules()` 호출 후 `modules` 키 조건부 추가(단일 모듈 backcompat — key 생략). `build_profile()` 이 `pom_data.get("modules")` 를 최상위 profile 에 전파. G-81 가드: `diagnose.py:check_cross_layer_coherence` 19→20건. `web/adapters/target_extractor.py` `_detect_source` 가 settings.gradle include 존재 시 `"gradle-multi"` 반환. `profiles/_README.md` modules 필드 문서화. 신규 테스트 8건(6 extract + 2 diagnose) + 기존 전체 그린. |
| Growth-80 | 2026-05-29 | **M3 Slice e+α — Keycloak SSO multi-client realm + LDAP federation** (Growth-77 G-77 의 후속, 사내 AD 통합 enterprise 요구 해소). **Multi-client refactor**: `render_sso_realm(info, slug, *, clients=None, ldap=None)` 시그니처 확장, `_render_client_json(client)` 헬퍼 신설 — `clients=None` 이면 Growth-77 의 단일 `{slug}-app` 하드코딩 그대로(backcompat), `clients=[...]` 면 각 client 의 `id`/`redirect_uris`/`web_origins`/`public_client` 옵셔널 처리. **LDAP federation**: `_SSO_LDAP_COMPONENT_TPL` (Keycloak `components.org.keycloak.storage.UserStorageProvider` 단일 entry 템플릿 — host/port/bind_dn/bind_credential/users_dn 필수 + username/rdn/uuid attr + user_object_classes 기본값 inetOrgPerson), `_render_ldap_component(slug, ldap)` 헬퍼, `_SSO_LDAP_ENV_EXAMPLE_TPL`(`.env.ldap.example` 산출), `_SSO_LDAP_SOP_SECTION` (SOP §10.4 LDAP federation 운영 안내). **emit() API**: `sso_ldap: bool = False` 파라미터 추가 + `sso_ldap_opt = sso_ldap or profile.overlay.sso_ldap`, `clients` 는 `profile.overlay.sso_clients` 에서 안전 nested get, `ldap_cfg` 는 `profile.ldap` 에서 (sso_ldap_opt true 일 때만) 추출. **CLI**: `--sso-ldap` 플래그 신설(`store_true`), `args.sso_ldap` → `emit()` forward. **env interpolation 보존**: `bind_credential: "${ACME_LDAP_BIND_PW}"` 같은 `${VAR}` 리터럴이 realm JSON 에 그대로 살아남도록 `str.replace` 기반 `__PLACEHOLDER__` sentinel 치환 채택 (f-string / `.format` 의 brace 충돌 회피). JSON validity 는 base realm 을 `json.dumps` 후 사전 포맷 clients/components 블록을 문자열 결합 — 모든 경로(single/multi/with-ldap/without-ldap) 에서 `json.loads` 통과. **profile schema 확장**: `profiles/_README.md` 에 `overlay.sso_keycloak` / `overlay.sso_clients` / `overlay.sso_ldap` + 새 `ldap:` top-level section(host/port/bind_dn/bind_credential/users_dn/username_attr/rdn_attr/uuid_attr/user_object_classes) 문서화, `profiles/acme.yaml` 에 commented 예시 부착. **회귀 12 신규 테스트**: tests/test_emit_ops_pack.py 10건 — multi-client 4(single-client backcompat / multi via profile / web_origins default / custom redirect_uris) + LDAP federation 6(sso off no ldap / sso on ldap off no components / ldap via CLI flag / ldap via profile opt-in / `${VAR}` 보존 / 기본값 적용); tests/workflow/test_diagnose.py 2건(G-80 markers 누락 → FAIL detail 에 "G-80 regression" 포함, real source PASS detail "19 trap guards intact" + `/80)` 으로 종료). **G-80 가드**: `scripts/workflow/diagnose.py:check_cross_layer_coherence` 에 `ldap_markers = ["_SSO_LDAP_COMPONENT_TPL", "_render_client_json", "sso_ldap", "org.keycloak.storage.UserStorageProvider", "Growth-80"]` sub-check 추가, PASS detail 18→19 + `/80` append. **전체 702/702 그린** (357 + 345 2배치, conftest 디스커버리 분리). **diagnose 19 guards intact** (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75/76/77/78/79/80). |
| Growth-79 | 2026-05-28 | **M5 Slice C-c — `web/routes/target.py` `/target/upload` 웹 통합 + G-79 가드 등재**. Growth-70(Maven 추출) 과 Growth-78(Gradle 추출) 이 CLI 입력단을 완결한 위에, 비 CLI 사용자(업무담당자/IT-담당자 페르소나) 가 zip 업로드만으로 v1 customer profile YAML 을 얻도록 web 진입 표면을 추가 — M5 입력 표면 2단계 게이트("Maven/Gradle SpringBoot 프로젝트 zip → 브라우저에서 profile 다운로드") 완결. **변경 표면**: (a) `web/adapters/target_extractor.py` 신규 — `TargetExtractionError` 예외 + `ExtractionResult(profile_yaml, slug, source)` dataclass + `extract_from_zip(payload, *, slug_override) -> ExtractionResult` 핵심 함수. `sys.path.insert` 로 `scripts/` 추가 후 `import extract_target_profile` → `extract_target_profile.build_profile()` + `extract_target_profile.dump_profile()` 직접 위임(parse_pom/parse_gradle 재구현 0). `tempfile.TemporaryDirectory` 안에서 `zipfile.ZipFile(io.BytesIO(payload))` 추출 + zip-slip 방어(`Path.resolve()` + `tmp_root in dest.parents` 검사) + 단일 top-level 디렉터리 자동 인식 헬퍼 `_zip_root(zf)` (`acme-project/pom.xml` 같은 일반 케이스 → root, 혼합 시 tmp_root 폴백) + `_detect_source(project_dir)` 가 `pom.xml`/`build.gradle.kts`/`build.gradle` 존재 검사로 `maven`/`gradle` 라벨 도출. (b) `web/routes/target.py` 신규 — `APIRouter(prefix="/target")` + `GET /upload` 핸들러(`target_upload.html` 렌더) + `POST /upload` 핸들러(slug 검증 regex `^[a-z][a-z0-9_-]*$` + 16 MiB 업로드 cap `_MAX_UPLOAD_BYTES` + `target_extractor.extract_from_zip()` 호출 + 폼 `download=1` 시 `application/x-yaml` + `Content-Disposition: attachment; filename="<slug>.yaml"` 응답, 미선택 시 200 미리보기 HTML 렌더). `TargetExtractionError` 발생 시 422 + 에러 메시지를 템플릿에 렌더. (c) `web/templates/target_upload.html` 신규 — base.html extends + Korean 라벨 "기존 프로젝트 zip 업로드" + `<input type="file" name="project_zip">` + 옵셔널 슬러그 override 필드 + 두 submit 버튼(미리보기 / YAML 다운로드 `name="download" value="1"`) + 결과 블록 `{% if result %}` (추출된 slug + source(maven/gradle) + `<pre>` 안 YAML 미리보기). (d) `web/app.py` — `from web.routes.target import router as target_router` + `application.include_router(target_router)` 한 줄 추가. (e) `web/templates/base.html` — site-nav 에 `<a href="/target/upload">기존 프로젝트 업로드</a>` 추가(도메인 정의/상태 보기 사이). **G-79 (Growth-79 Web target-upload single-source contract)**: `diagnose.py:check_cross_layer_coherence` 가드 17→**18건**. `web/routes/target.py` 가 4 마커(`/upload`, `target_extractor`, `extract_from_zip`, `Growth-79`) 유지 + `web/adapters/target_extractor.py` 가 4 마커(`import extract_target_profile`, `extract_target_profile.build_profile`, `extract_target_profile.dump_profile`, `Growth-79`) 유지 + **단일 진실 소스 규칙**: adapter 가 `def parse_pom(` 또는 `def parse_gradle(` 함수를 정의하면 FAIL(web 경로에서 G-70/G-78 파서를 재구현하는 회귀 차단 — G-69 와 동일한 single-source 원칙) + `web/app.py` 가 `target_router` include. 회귀 시 IT-담당자 페르소나가 CLI 없이 profile 을 얻지 못하거나, web 경로가 6-axis(skill/ddl/mybatis/nexacro/creater/customer) 누적을 우회하는 패러럴 파서로 분기됨. FAIL hint `... /77/78` → `... /77/78/79`, PASS detail `18 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75/76/77/78/79)`. **회귀 2 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g79_adapter_reimplements_parser` adapter stub 에 `def parse_pom(` 추가 시 single-source 위반 감지(detail 에 "G-79 regression" + "single-source"), (2) `test_check_cross_layer_coherence_fail_g79_app_missing_target_router` web/app.py stub 에서 `target_router` include 제거 시 라우트 미연결 감지. 픽스처 `_make_workspace` 의 web/routes/ + web/adapters/ stub 트리 신설 + web/app.py stub 에 target_router include 라인 추가. tests/workflow/test_diagnose.py 56→**58 그린**. **회귀 9 신규 test_routes_target 케이스**: (1) `GET /target/upload` → 200 + 폼 렌더(`name="project_zip"` 노출), (2) POST 빈 zip(`b""`) → 422 + 한글 안내 "비어", (3) POST 잘못된 zip 바이트(`b"not a zip"`) → 422 + "zip", (4) POST Maven pom.xml + application.yml → 200 + 미리보기에 `version: 1` + 추출된 slug + "추출 결과" 라벨, (5) POST Gradle KTS + settings.gradle.kts(`rootProject.name = "foo-shell"`) → 200 + `slug: foo` (-shell 자동 strip), (6) POST pom/gradle 둘 다 부재(README.md only) → 422 + 안내 메시지, (7) POST `download=1` → 200 + `application/x-yaml` + `Content-Disposition: attachment; filename="acme.yaml"` + Growth-70 헤더(`#` 로 시작) + `version: 1` 포함, (8) POST 폼 `slug=acme-prod` override → 미리보기에 `slug: acme-prod` 반영, (9) POST 잘못된 slug(`Bad Slug!`) → 422 + 한글 "슬러그" 메시지. tests/web/test_routes_target.py 0→**9 그린**. **창의적 결정**: (i) **two-button submit pattern** — `download` 폼 필드 값에 따라 동일 POST 가 미리보기 HTML 또는 attachment YAML 응답으로 분기(별도 라우트 불필요, UX 단순). (ii) **16 MiB 업로드 cap** — `_MAX_UPLOAD_BYTES = 16 * 1024 * 1024` 상수로 명시. 일반 SpringBoot project zip(소스 only) 이 충분히 작고, runaway 업로드 차단. (iii) **zip-slip 방어** — `Path.resolve()` 후 `tmp_root in dest.parents`(또는 `dest == tmp_root`) 검증으로 `../../etc/passwd` 류 경로 차단. tmp 디렉터리는 `finally` 블록에서 항상 cleanup. (iv) **single-source 규칙의 정적 가드화** — G-69 가 web→scaffold subprocess 호출을 강제했듯, G-79 는 web→extract_target_profile import 위임을 강제. parser 재구현이 silent 하게 추가되면 customer 축의 진실원천이 두 곳으로 갈라져 6-axis 누적이 web 경로에서만 우회되는 트랩 차단. (v) **KTS-aware 자동 — downstream 효과** — adapter 는 build_profile 만 호출하므로 Growth-78 의 KTS-우선 정책이 자연스럽게 적용. web 레이어 추가 코드 0. **6-axis 보전 확인**: target_extractor 는 customer 축 입력 표면 다양화(CLI + web zip upload 둘 다 동일 build_profile → 동일 v1 YAML) — 7th axis 도입 없음. parse_pom/parse_gradle 재구현 정적 금지. **알려진 격차**: multi-module Gradle(`subproject {}`) 추출은 root only(Growth-78 격차 그대로 승계). `/target/upload` UX polish(progress indicator/multi-file diff view) 는 후속 슬라이스. M5 Slice C 게이트는 본 슬라이스로 1차 완결 — Maven/Gradle 두 input + CLI/web 두 channel × v1 customer profile single-source = 4 entry point 모두 동일 출력 계약. **회귀**: tests/web/test_routes_target.py 9/9 + tests/workflow/test_diagnose.py 58/58 + tests/web/ 전체 그린 + live diagnose 출력 `18 trap guards intact (...77/78/79)` 그린. AGENTS.md 업데이트(가드 카운트 17→18 + G-79 항목 + G-80+ 신호). |
| Growth-78 | 2026-05-28 | **M5 Slice C-b — `extract_target_profile.py` Gradle 입력 지원 + G-78 가드 등재**. Growth-70 이 Maven-only 로 닫아둔 customer profile 자동 추출 입력단을 Gradle SpringBoot 프로젝트(Groovy DSL + Kotlin DSL) 까지 확장 — M5 입력 표면 1단계 게이트("기존 Maven/Gradle SpringBoot 프로젝트 → profile 자동 생성") 완결. **변경 표면**: `scripts/extract_target_profile.py` — (a) 신규 헬퍼 5종: `_GRADLE_GROUP_RE` (multiline `^\s*group\s*=\s*['"]...['"]` — Groovy/Kotlin DSL 공통), `_GRADLE_VERSION_RE` (literal assignment 만 매치 — plugin `version '3.x'` 무시), `_GRADLE_ROOT_NAME_RE` (`rootProject.name = '...'` from settings.gradle(.kts)), `_gradle_lane(raw)` (raw text 에 `jakarta.servlet` / `mybatis-spring-boot-starter-jakarta` / `spring-boot-starter-web:3` / `spring-boot:3` 마커 ≥1 → jakarta, 그 외 javax), `_find_gradle_build(project_dir)` (build.gradle.kts 우선 → build.gradle 폴백 — KTS 가 신규 default 라는 산업 관행 반영). (b) `parse_gradle(build_path)` — `parse_pom()` 과 동일한 `{group_id, artifact_id, version, name, description, lane, raw}` 시그니처 반환 → `build_profile()` downstream 변경 0. artifact_id 결정 폴백 사슬: settings.gradle(.kts) `rootProject.name` → 프로젝트 디렉터리명. description 은 None (Gradle 표준 필드 없음 — 빈 값으로 두고 사용자 수동 채움). (c) `build_profile(project_dir, ...)` 분기 재작성: `pom.xml` 존재 시 기존 경로(Maven-first 보장), 부재 시 `_find_gradle_build()` → Gradle 파싱, 둘 다 부재 시 `FileNotFoundError("No pom.xml or build.gradle(.kts) found ...")` 으로 메시지 갱신(M5 Slice C 메시지 "Gradle is deferred" 제거). (d) 모듈 docstring 에 Growth-78 단락 추가 — Gradle 은 "또 하나의 input front-end" 이지 출력 계약(v1 profile + Growth-70 header) 변경 아님 명시. **G-78 (Growth-78 extract_target_profile Gradle input contract)**: `diagnose.py:check_cross_layer_coherence` 가드 16→**17건**. `scripts/extract_target_profile.py` 가 9 마커(`def parse_gradle(`, `_GRADLE_GROUP_RE`, `_GRADLE_ROOT_NAME_RE`, `_find_gradle_build`, `_gradle_lane`, `build.gradle.kts`, `build.gradle`, `rootProject.name`, `Growth-78`) 유지. 회귀 시 Gradle 사용자가 다시 profile 을 손으로 작성하는 시대로 되돌아감 — M5 Slice C-b 약속 위반. FAIL hint `... /76/77` → `... /76/77/78`, PASS detail `17 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75/76/77/78)`. **회귀 2 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g78_missing_parse_gradle` Growth-78 헬퍼 일괄 제거(parse_gradle 부재) — FAIL detail 에 "G-78 regression" + "parse_gradle" 포함, (2) `test_check_cross_layer_coherence_fail_g78_missing_kts_marker` Kotlin DSL 파일명 마커(`build.gradle.kts`) 만 손실 시도 — FAIL detail 에 "build.gradle.kts" 포함. 픽스처 `_make_workspace` 의 extract_target_profile.py stub 에 9 G-78 마커 stub 라인 모두 주입(G-70 와 동일 파일 공유). tests/workflow/test_diagnose.py 54→**56 그린**. **회귀 6 신규 test_extract_target_profile 케이스**: (1) `test_gradle_groovy_jakarta_postgres` Groovy DSL + jakarta.servlet 의존성 + settings.gradle rootProject.name + postgres URL → 모든 필드 정상 매핑(slug 도출/dialect/lane/maven group_id/version/shell_app_id), (2) `test_gradle_kts_jakarta_mysql` Kotlin DSL + jakarta + mysql URL → 동일 contract, (3) `test_gradle_groovy_javax_no_settings_uses_dir_name` settings.gradle 부재 → artifact_id 가 프로젝트 디렉터리명 폴백 + javax lane 감지(`javax.servlet` 의존성 only), (4) `test_gradle_kts_takes_precedence_over_groovy` 두 파일 동시 존재 시 KTS 우선(`_find_gradle_build` 산업 관행 반영), (5) `test_pom_takes_precedence_over_gradle` pom.xml + build.gradle 동시 존재 시 Maven-first 계약 보전(downstream profile 변경 0), (6) `test_gradle_roundtrips_through_loader` Gradle-추출 profile 이 `load_customer_profile` G-62/G-63 v1 pin 통과 + `${ENV}` placeholder 보존. tests/test_extract_target_profile.py 24→**30 그린**. **창의적 결정**: KTS 가 Groovy DSL 보다 우선(`_find_gradle_build` 가 KTS 먼저 검사) — 신규 Gradle 프로젝트의 default 가 KTS 라는 산업 관행(Gradle 7+/Spring Initializr 권장) 반영. 두 파일이 함께 있는 마이그레이션 중인 프로젝트에서도 "최신 의도" 가 자동으로 선택됨. Description 필드는 Gradle 에서 표준화되어 있지 않으므로 None — Maven `<description>` 의 동등물을 임의 추정하지 않음(사용자 review 가 customer profile 의 의미를 더 정확히 채움). 또한 본 슬라이스는 **input front-end 만** 확장 — `build_profile()` 본체 / `dump_profile()` 헤더 / G-70 출력 계약 모두 무손상으로 보전(Maven-first 우선순위, v1 schema 강제, draft status 폴백 정책 그대로). **6-axis 보전 확인**: Gradle 분기는 customer 축의 **입력 경로 다양화** — skill/ddl/mybatis/nexacro/creater 축 무변경. Output contract(v1 profile + Growth-70 header) 가 G-70 가드로 정적 보장되어 Maven 경로와 Gradle 경로가 동일 customer 축 자산을 산출. **알려진 격차**: build.gradle 의 `subproject {}` 블록 또는 multi-module Gradle 빌드는 root 프로젝트만 추출(첫 단계). Spring Boot Gradle plugin 의 `bootJar { archiveBaseName = ... }` override 도 미반영 — root settings.name 폴백으로 충분. M5 Slice C-c (`/target/upload` web 통합) 는 별도 슬라이스. **회귀**: tests/test_extract_target_profile.py 30/30 + tests/workflow/test_diagnose.py 56/56 + live diagnose 출력 `17 trap guards intact (...76/77/78)` 그린. AGENTS.md 업데이트(가드 카운트 16→17 + G-78 항목). |
| Growth-77 | 2026-05-28 | **M3 Slice e — `emit_ops_pack.py` Keycloak/OIDC SSO sidecar 옵트인 + G-77 가드 등재**. IT-담당자 페르소나가 사내 SSO 없는 환경에서도 즉시 OIDC 로그인이 동작하는 Keycloak 사이드카를 ops pack 으로 받고, 운영 시 `OIDC_ISSUER_URI` 만 교체해 사내 IdP 로 전환 가능하도록 `--sso` 플래그(또는 customer profile `overlay.sso_keycloak: true`) 옵트인 추가 — Growth-76 Vault 와 동일한 overlay 패턴(`docker-compose.sso.yml` 사이드카 + base compose 무변경). **변경 표면**: `scripts/emit_ops_pack.py` — (a) 신규 템플릿 4종: `_SSO_COMPOSE_TPL` (`services.keycloak: quay.io/keycloak/keycloak:24.0 command: ["start-dev", "--import-realm"]` + KEYCLOAK_ADMIN/PASSWORD env + realm.json 마운트 + `services.app.env_file: - .env.sso` + `OIDC_ISSUER_URI/CLIENT_ID/SECRET/REDIRECT_URI` env + `depends_on: keycloak: { condition: service_started }`), `_SSO_REALM_JSON_TPL` (realm import — realm: `{slug}` + enabled + 1 client `{slug}-app` + `protocol: openid-connect` + `standardFlowEnabled: true` + `directAccessGrantsEnabled: false` + redirectUris `http://localhost:8080/login/oauth2/code/keycloak` + `http://localhost:8080/*` + webOrigins `+`), `_SSO_ENV_EXAMPLE_TPL` (OIDC contract: `OIDC_ISSUER_URI=http://keycloak:8080/realms/{slug}` + `OIDC_CLIENT_ID={slug}-app` + `OIDC_CLIENT_SECRET=` (blank — IT-담당자가 Keycloak admin console 의 credentials 탭에서 채움) + `OIDC_REDIRECT_URI=http://localhost:8080/login/oauth2/code/keycloak`), `_SSO_SOP_SECTION` (DEPLOY-SOP §10 Korean runbook: 사전 준비/기동 명령 `docker compose -f docker-compose.yml -f docker-compose.sso.yml up -d`/인증 검증 `curl ... /login | grep -i keycloak`/사내 SSO 전환 — `OIDC_ISSUER_URI` 만 교체 + `keycloak` 서비스 블록 제거). (b) render 헬퍼 3종: `render_sso_compose(info, slug)` / `render_sso_realm(info, slug)` (slug → realm 이름 + clientId `{slug}-app` 자동 도출) / `render_sso_env_example(info, slug)`. (c) `emit(out_dir, profile=None, shell_subdir="shell", force=False, vault=False, sso=False)` 시그니처 확장 — `sso_opt = sso or bool(profile.get("overlay", {}).get("sso_keycloak"))` 머지(둘 다 작동, 둘 다 없으면 no-op) → sso_opt=True 시 추가 3 산출물(`<ops>/docker-compose.sso.yml`, `<ops>/keycloak-realm.json`, `<ops>/.env.sso.example`) emit + DEPLOY-SOP.md 에 `_SSO_SOP_SECTION` append. (d) CLI: `argparse.add_argument("--sso", action="store_true")` → `emit(sso=args.sso)`. Vault + SSO 동시 활성화도 정상(SOP §9/§10 둘 다 append). **G-77 (Growth-77 Keycloak/OIDC SSO sidecar emitter contract)**: `diagnose.py:check_cross_layer_coherence` 가드 15→**16건**. `scripts/emit_ops_pack.py` 가 9 마커(`render_sso_compose`, `render_sso_realm`, `render_sso_env_example`, `_SSO_SOP_SECTION`, `quay.io/keycloak/keycloak`, `docker-compose.sso.yml`, `keycloak-realm.json`, `OIDC_ISSUER_URI`, `Growth-77`) 유지. 회귀 시 IT-담당자가 즉시 SSO 데모/사내 IdP 전환 경로를 잃음. FAIL hint `... /72/74/75/76` → `... /72/74/75/76/77`, PASS detail `16 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75/76/77)`. **회귀 3 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g77_missing_sso_helpers` render_sso_* 3 헬퍼 부재, (2) `test_check_cross_layer_coherence_fail_g77_missing_keycloak_image` `quay.io/keycloak/keycloak` 마커 손실, (3) `test_check_cross_layer_coherence_fail_g77_emit_artifact_name_missing` 3 산출물 이름 중 하나(`docker-compose.sso.yml`) 부재. 픽스처 `_make_workspace` 의 emit_ops_pack.py stub 에 9 G-77 마커(Keycloak 이미지 + render_sso_* + `_SSO_SOP_SECTION` + 3 SSO 산출물 이름 + OIDC env + Growth-77) 모두 주입. tests/workflow/test_diagnose.py 51→**54 그린**. **회귀 10 신규 test_emit_ops_pack 케이스**: (1) sso=False 기본 — 3 산출물 부재 보전, (2) sso=True — 3 산출물 emit, (3) docker-compose.sso.yml keycloak 서비스 + 이미지 + start-dev + OIDC env + depends_on 검증, (4) keycloak-realm.json JSON 파싱 가능 + realm/enabled/clients 구조 + clientId `acme-app` + protocol `openid-connect`, (5) .env.sso.example 4키(ISSUER_URI/CLIENT_ID/CLIENT_SECRET/REDIRECT_URI) 포함 + CLIENT_SECRET 공백(IT-담당자 채움 자리), (6) DEPLOY-SOP §10 append + compose overlay 명령 포함, (7) sso=False 시 §10 부재 보전, (8) profile `overlay.sso_keycloak: true` 단독 opt-in, (9) CLI `--sso` 플래그 wire-up, (10) Vault + SSO 동시 활성화 — 두 overlay 산출물 모두 emit + SOP §9/§10 둘 다 append. tests/test_emit_ops_pack.py 33→**43 그린**. **창의적 결정**: realm import + 기본 admin/admin 자격증명을 디폴트로 둠 — IT-담당자가 즉시 admin console 에 들어가 client secret 확인 가능. 운영 전 `KEYCLOAK_ADMIN_PASSWORD` env override 가 SOP §10.3 에 명시. 단일 client 만 자동 import (multi-client 또는 federation 은 후속 — 본 슬라이스는 1차 게이트 "OIDC 로그인 동작" 만 깊고 좁게 충족). 사내 SSO 전환 시 `keycloak` 서비스 블록을 compose overlay 에서 제거하고 `OIDC_ISSUER_URI` 만 교체하면 됨 — 단일 변경점이 SOP 에 못 박힘. **6-axis 보전 확인**: SSO 산출물 분기는 customer 축 `profile.customer.slug` 만 입력(realm/clientId 자동 도출) — 7th axis 도입 없음. base ops pack 4 산출물 무변경 + Vault overlay 와 독립(서로 영향 0, 동시 활성화 정상). **알려진 격차** (M3 후속 슬라이스): multi-client realm + Keycloak federation + 사내 LDAP 바인딩은 Slice e+α, M5 Slice C-b Gradle 입력 지원. **회귀**: tests/test_emit_ops_pack.py 43/43 + tests/workflow/test_diagnose.py 54/54 + live diagnose 출력 `16 trap guards intact (...75/76/77)` 그린. AGENTS.md 업데이트(가드 카운트 15→16 + G-77 항목). |
| Growth-76 | 2026-05-28 | **M3 Slice d — `emit_ops_pack.py` Vault Agent sidecar 옵트인 + G-76 가드 등재**. IT-담당자 페르소나가 `.env` 평문에 DB 자격증명을 보관하지 않고 사내 HashiCorp Vault 로 위임하도록 ops pack emitter 에 `--vault` 플래그(또는 customer profile `overlay.vault_agent: true`) 옵트인을 추가 — enterprise on-prem 망분리 환경에서 self-host v1.0 출시 게이트의 자격증명 위임 약속 충족. **변경 표면**: `scripts/emit_ops_pack.py` — (a) 신규 템플릿 4종: `_VAULT_HCL_TPL` (vault-agent.hcl: `vault { address = "{{ env "VAULT_ADDR" }}" }` + `auto_auth { method "approle" { role_id_file_path = "/vault/role-id" + secret_id_file_path = "/vault/secret-id" } }` + `template { source = "/vault/templates/env.tmpl" destination = "/vault/secrets/.env" perms = "0644" }`), `_VAULT_ENV_TMPL_TPL_DB` (consul-template syntax: `{{ with secret "secret/data/{slug}/db" }}` → `{prefix}_DB_HOST={{ .Data.data.host }}` 등 5 키 매핑), `_VAULT_COMPOSE_TPL` (`services.vault-agent: hashicorp/vault:1.15 command: ["agent", "-config=/vault/config/vault-agent.hcl"]` 사이드카 + `services.app.env_file: - vault-secrets/.env` + `depends_on: vault-agent: { condition: service_started }` — base compose 무변경 overlay 패턴), `_VAULT_SOP_SECTION` (DEPLOY-SOP §9 Korean runbook: AppRole 생성/role-id+secret-id 발급/KV v2 시드/compose overlay 명령 `docker compose -f docker-compose.yml -f docker-compose.vault.yml up`). (b) render 헬퍼 3종: `render_vault_hcl(info, slug)` / `render_vault_env_tmpl(info, slug)` (slug → KV path `secret/data/{slug}/db` + env prefix `{SLUG_UPPER}_DB_*` 자동 도출) / `render_vault_compose(info, slug)`. (c) `emit(out_dir, profile=None, shell_subdir="shell", force=False, vault=False)` 시그니처 확장 — `vault_opt = vault or bool(profile.get("overlay", {}).get("vault_agent"))` 머지(둘 다 작동, 둘 다 없으면 no-op) → vault_opt=True 시 추가 3 산출물(`<ops>/docker-compose.vault.yml`, `<ops>/vault/config/vault-agent.hcl`, `<ops>/vault/templates/env.tmpl`) emit + DEPLOY-SOP.md 에 `_VAULT_SOP_SECTION` append. (d) CLI: `argparse.add_argument("--vault", action="store_true")` → `emit(vault=args.vault)`. **G-76 (Growth-76 Vault Agent sidecar emitter contract)**: `diagnose.py:check_cross_layer_coherence` 가드 14→**15건**. `scripts/emit_ops_pack.py` 가 9 마커(`render_vault_hcl`, `render_vault_env_tmpl`, `render_vault_compose`, `_VAULT_SOP_SECTION`, `method "approle"`, `docker-compose.vault.yml`, `vault-agent.hcl`, `env.tmpl`, `Growth-76`) 유지. 회귀 시 enterprise on-prem 환경에서 `.env` 평문 의존이 다시 시작되고 IT-담당자가 사내 Vault 로 자격증명을 위임할 수 없음. FAIL hint `... /72/74/75` → `... /72/74/75/76`, PASS detail `15 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75/76)`. **회귀 3 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g76_missing_vault_helpers` render_vault_* 3 헬퍼 stub 부재, (2) `test_check_cross_layer_coherence_fail_g76_missing_approle` `method "approle"` 마커 손실, (3) `test_check_cross_layer_coherence_fail_g76_emit_file_missing` 3 산출물 이름 중 하나 부재(`vault-agent.hcl`/`docker-compose.vault.yml`/`env.tmpl`). 픽스처 `_make_workspace` 의 emit_ops_pack.py stub 에 9 마커 모두 주입. tests/workflow/test_diagnose.py 48→**51 그린**. **회귀 10 신규 test_emit_ops_pack 케이스**: (1–3) render_vault_hcl AppRole + auto_auth + template 블록 구조 검증, (4) render_vault_env_tmpl consul-template KV path `secret/data/{slug}/db` + 5 키(`*_DB_HOST/PORT/NAME/USER/PASS`) 모두 포함, (5) render_vault_compose vault-agent 서비스 + env_file 의존 + depends_on, (6) emit(vault=True) 3 추가 산출물 + DEPLOY-SOP §9 append 검증, (7) emit(vault=False) 추가 산출물 부재(기본 흐름 보전), (8) profile `overlay.vault_agent: true` 단독으로 opt-in 활성화, (9) CLI `--vault` 플래그 wire-up, (10) DEPLOY-SOP §9 `docker compose -f docker-compose.yml -f docker-compose.vault.yml up` 명령 포함. tests/test_emit_ops_pack.py 23→**33 그린**. **창의적 결정**: Vault 옵트인은 **overlay 패턴** (`docker-compose.yml` 무변경 + `docker-compose.vault.yml` 별도) 으로 자료 분리. 이유: (a) Vault 는 enterprise opt-in — 기본 흐름(평문 .env) 을 단순하게 보존해 비전문 사용자 진입 마찰 0, (b) Compose 의 `-f` 네이티브 체이닝이 사이드카 패턴의 표준 — 인위적 통합 회피, (c) CLI 플래그 + profile 양쪽 모두로 옵트인 가능 — 단발 실행(CLI)·테넌트 락인(profile) 두 시나리오 모두 자연. 또한 consul-template + AppRole 조합으로 well-known Vault Agent 패턴 따름 — IT-담당자 audience 의 학습 부담 0. **6-axis 보전 확인**: Vault 산출물은 customer 축 `profile.overlay.vault_agent` 만 읽고 분기 — 7th axis 도입 없음. base ops pack 4 산출물(Dockerfile/docker-compose.yml/.env.example/DEPLOY-SOP.md) 무변경, vault opt-in 은 *추가* 가치이지 *대체* 가치 아님. **알려진 격차** (M3 후속 슬라이스): Slice e Keycloak/OIDC SSO 사이드카 바인딩(realm/client/redirect-URI 매핑), M5 Slice C-b Gradle 입력 지원(`build.gradle(.kts)` → customer profile 추출). **회귀**: tests/test_emit_ops_pack.py 33/33 + tests/workflow/test_diagnose.py 51/51 + live diagnose 출력 `15 trap guards intact (...74/75/76)` 그린. AGENTS.md 업데이트(가드 카운트 14→15 + G-76 항목). |
| Growth-75 | 2026-05-28 | **M3 Slice c — `web/routes/ops.py` /domain/{run_id}/ops.zip 라우트 + G-75 가드 등재**. Growth-74 가 orchestrator 후처리로 `out_dir/shell/ops/` 에 emit 한 4 산출물(Dockerfile / docker-compose.yml / .env.example / DEPLOY-SOP.md)을 IT-담당자 페르소나가 전체 산출물 zip 에서 추출하지 않고 별도로 받도록 `GET /domain/{run_id}/ops.zip` 노출. **변경 표면**: (a) `web/routes/ops.py` 신규 — `APIRouter(prefix="/domain")` + `domain_ops_download(run_id)` 핸들러: `run_registry.get` 으로 ScaffoldResult 조회(없으면 404), `out_dir/shell/ops` 디렉터리 존재 확인(없으면 409 + Korean 힌트), `zip_emitter.emit(ops_dir)` 로 in-memory zip → `application/zip` 응답 + `Content-Disposition: attachment; filename="<slug>-ops.zip"`. (b) `web/app.py` — `ops_router` import + include_router 한 줄 추가. (c) `web/routes/domain.py:domain_preview` — `pathlib.Path` local import 후 `(Path(result.out_dir) / "shell" / "ops").is_dir()` 한 줄로 `ops_pack_available` boolean 계산, 템플릿 컨텍스트로 전달. (d) `web/templates/domain_preview.html` — `download-section` 안 `{% if ops_pack_available %}` 분기로 "Ops Pack 다운로드 (IT-담당자)" 보조 CTA(`btn-ops` 클래스, 기존 CSS 재사용). **G-75 (Growth-75 Web ops pack download route)**: `diagnose.py:check_cross_layer_coherence` 가드 13→**14건**. `web/routes/ops.py` 가 5 마커(`/{run_id}/ops.zip`, `shell`, `ops`, `zip_emitter`, `Growth-75`) 유지 + 단일 진실 소스 규칙(`import emit_ops_pack` / `emit_ops_pack.emit(` 둘 다 부재 — Growth-74 가 단독 emit owner) + `web/app.py` 에 `ops_router` 와 `web.routes.ops` 둘 다 포함. FAIL hint `... /72/74` → `... /72/74/75`, PASS detail `14 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74/75)`. **회귀 3 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g75_missing_route` ops.py 파일 부재, (2) `test_check_cross_layer_coherence_fail_g75_reinvokes_emit` `import emit_ops_pack` 추가 시 single-source 위반 감지, (3) `test_check_cross_layer_coherence_fail_g75_app_does_not_include_router` web/app.py 가 ops_router include 미실시. 픽스처 `_make_workspace` 에 web/routes/ops.py + web/app.py stub 추가. tests/workflow/test_diagnose.py 45→**48 그린**. **회귀 6 신규 test_routes_ops 케이스**: (1) 404 unknown run, (2) 409 ops/ 부재(에러 메시지에 "ops pack" 포함), (3) 200 응답 + Content-Type `application/zip` + Content-Disposition `<slug>-ops.zip`, (4) zip 내용 — 4 산출물(Dockerfile/docker-compose.yml/.env.example/DEPLOY-SOP.md) 모두 archive 안 basename 으로 발견, (5) preview 페이지 ops/ 시드 시 "Ops Pack 다운로드" 텍스트 + 정확한 다운로드 URL 노출, (6) preview 페이지 ops/ 부재 시 버튼 비노출. tests/web/test_routes_ops.py 6/6 그린, 전체 tests/web/ 49 passed + 1 skipped(e2e gate). **창의적 결정**: ops pack 다운로드는 **별도 라우트** 로 분리(주 산출물 다운로드 `/{run_id}/download` 에 동봉 옵션을 추가하지 않음). 이유: (a) IT-담당자 페르소나는 전체 Java 소스 트리 없이 deploy bundle 만 받기를 원함 — 페르소나 분리가 URL 에 드러남, (b) 기존 `/download` 응답은 이미 out_dir 전체를 압축하므로 shell/ops/ 가 자연스럽게 포함 — 별도 zip 은 *추가* 가치이지 *대체* 가치, (c) preview 에 두 버튼이 나란히 보여 "전체 다운로드 vs ops 만" 선택권을 명시. emit 재호출 금지(G-75 가 강제): orchestrator 의 Growth-74 자동 emit 이 유일한 emit 소유자 — 라우트는 결과 *읽기만* 한다. 두 곳에서 emit 하면 customer profile 변경 시 결과 어긋남 위험. **6-axis 보전 확인**: web 축은 Growth-69 컨벤션대로 "creater 축의 frontend layer" — 새 axis 도입 없음. ops pack 은 customer 축 profile 만 입력으로 받음 — 7th axis 우회 없음. **알려진 격차** (M3 후속 슬라이스): Slice d Vault Agent sidecar(`.vault/agent-config.hcl` 템플릿), Slice e Keycloak/OIDC SSO 바인딩(realm/client/redirect-URI 매핑), M5 Slice C-b Gradle 입력 지원. **회귀**: tests/web/test_routes_ops.py 6/6 + tests/workflow/test_diagnose.py 48/48 + tests/web/ 전체 49/49(+1 skipped) 그린. AGENTS.md 업데이트(가드 카운트 13→14 + G-75 항목). |
| Growth-74 | 2026-05-28 | **M3 Slice b — `scaffold_orchestrator` Stage 5 PASS 직후 `emit_ops_pack` 자동 호출 + G-74 가드 등재**. Growth-71 이 standalone CLI 로 분리해둔 ops pack emitter 를 orchestrator post-Stage 5 흐름에 묶어 IT-담당자 페르소나의 "scaffold 직후 ops pack 도 같이 받는다" 약속 충족 — 사용자가 별도 `python scripts/emit_ops_pack.py <out>` 호출 절차를 잊지 않도록 자동화. **변경 표면**: `scripts/scaffold_orchestrator.py` — (a) 신규 `_run_emit_ops_pack(args, report)` 헬퍼: `"stage5" in report.stages_run` 사전 가드(stage5-skipped/실패 시 no-op) + `out_dir/shell/pom.xml` 존재 검사 → 부재 시 `ops_pack-skipped-no-shell` 마커만 추가(forward-compatible: shell/ 생성은 Stage 5/overlay 책임으로 분리), 존재 시 `emit_ops_pack.emit(out_dir, profile=customer_profile, shell_subdir="shell", force=True)` 호출, 예외 시 `ops_pack-failed` 마커 + stderr 경고 — Growth-66 `_append_domain_to_profile` 와 동일한 best-effort 비치명 패턴(scaffold 자체 PASS 보전), (b) `run_scaffold` 의 `_write_report(args, report)` 호출 직전(line 573 위치)에서 `_run_emit_ops_pack(args, report)` 단일 라인 추가, (c) `_write_report` 의 stage-order tuple 에 `ops_pack` / `ops_pack-skipped-no-shell` / `ops_pack-failed` 3 마커 + 전용 렌더 분기(`SKIPPED (no <out>/shell/pom.xml — Growth-74 M3 Slice b)` / `FAILED (non-fatal — see stderr; Growth-74 M3 Slice b)` / `OK (Nms)`) 추가. **G-74 (Growth-74 Orchestrator ops_pack auto-emit)**: `diagnose.py:check_cross_layer_coherence` 가드 12→**13건**. `scripts/scaffold_orchestrator.py` 가 6 마커(`def _run_emit_ops_pack(`, `_run_emit_ops_pack(args, report)`, `ops_pack-skipped-no-shell`, `ops_pack-failed`, `import emit_ops_pack`, `Growth-74`) 유지. 회귀 시 M3 Slice b 약속이 깨지고 IT-담당자가 다시 수동 emit_ops_pack 절차에 의존. FAIL hint `G-47/48/50/58/61/62/63/69/70/71/72` → `... /72/74` 추가, PASS detail `13 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72/74)`. **회귀 3 신규 test_diagnose 케이스**: (1) `test_check_cross_layer_coherence_fail_g74_missing_orchestrator` 파일 부재(G-58 도 동시 fire — multi-fail detail 검증), (2) `test_check_cross_layer_coherence_fail_g74_lost_helper` 헬퍼 함수 제거, (3) `test_check_cross_layer_coherence_fail_g74_lost_skip_marker` skip 마커 제거. 픽스처 `_make_workspace` 의 `scaffold_orchestrator.py` stub 에 6 마커 stub 라인 모두 주입(G-58 와 동일 파일 공유). tests/workflow/test_diagnose.py 42→**45 그린**. **회귀 4 신규 test_scaffold_orchestrator 케이스**: (1) `test_stage5_pass_no_shell_marks_skipped` — Stage 5 PASS 후 shell/pom.xml 부재 시 `ops_pack-skipped-no-shell` 마커 + scaffold-report.md `ops_pack: SKIPPED` 본문 표기, (2) `test_stage5_pass_with_shell_pom_invokes_emit` — `run_overlay` side_effect 로 shell/pom.xml 사전 시드 후 `emit_ops_pack.emit` 호출 횟수 1 + `profile=None`/`shell_subdir="shell"` kwargs 검증, (3) `test_stage5_pass_emit_failure_is_nonfatal` — emit 이 RuntimeError 던져도 `run_scaffold` 가 예외 전파 없이 완료 + `ops_pack-failed` 마커, (4) `test_stage5_skipped_does_not_invoke_emit` — `target_project=None` 시 stage5-skipped → ops_pack 마커 어느 것도 없음 + `emit.assert_not_called()`. tests/test_scaffold_orchestrator.py 31→**35 그린**. **창의적 결정**: forward-compatible 스킵 마커 — `out_dir/shell/pom.xml` 부재 시 silent no-op 가 아니라 `ops_pack-skipped-no-shell` 마커 + scaffold-report.md 명시 표기. 현재 Stage 5 산출물이 shell/ 디렉터리를 생성하지 않으면(예: target_project overlay 가 shell-less 일 때) 사용자가 "왜 ops pack 이 안 생겼나" 추측하지 않고 보고서 한 줄로 진단 가능. Stage 5 가 향후 shell/ 를 emit 하도록 진화하면 같은 와이어로 자동 활성화 — 코드 변경 0. **6-axis 보전 확인**: emit_ops_pack 은 기존 customer 축 `profile` 만 읽어 ops 산출물 분기(lane/dialect/slug) 도출 — 7th axis 도입 없음. scaffold logic/sibling repos 무변경. **알려진 격차** (M3 후속 슬라이스): Slice c `web/routes/ops.py` `/ops/download` zip stream, Slice d Vault Agent sidecar(`.vault/agent-config.hcl` 템플릿), Slice e Keycloak/OIDC SSO 바인딩(realm/client/redirect-URI 매핑), M5 Slice C-b Gradle 입력 지원(`build.gradle(.kts)` → customer profile 추출). **회귀**: tests/test_scaffold_orchestrator.py 35/35 + tests/workflow/test_diagnose.py 45/45 그린. AGENTS.md 업데이트(가드 카운트 12→13 + G-74 항목). |
| Growth-73 | 2026-05-28 | **M4 Hosting Modes 결정 — self-host 단일 모드 v1.0 확정, SaaS 진입 보류**. G1 마일스톤 M4: M1(Web Form Growth-69) · M2(Status Board Growth-72) · M3(Ops Pack Growth-71) · M5(Target Project Overlay Growth-70) 가 모두 닫힌 시점에서 self-host vs SaaS 분기 재평가 — **결정**: self-host 단일 모드로 v1.0 확정. **본 Growth 는 정책 결정 — 코드 변경 0, 신규 가드 0, 신규 의존성 0**. 자산: 단일 산출물 `docs/hosting-modes.md` (8절 결정 문서: §1 결정 / §2 현 시점 self-host 자산 4종 — Dockerfile+docker-compose+.env.example+DEPLOY-SOP.md from Growth-71 / §3 3-페르소나 자가검토 (CEO needs `status_board`+portal 충족, 업무담당자 needs `web/` FastAPI 충족, IT담당자 needs Ops Pack 충족 — 모두 SaaS 추가 가치 ≤0) / §4 SaaS 보류 4 사유 (① 6-axis 누적 자산은 회사 고유 자산 — multi-tenant 격리 v1.0 범위 초과, ② 타깃 enterprise on-prem 망분리 환경에서 SaaS 도달 불가, ③ "dev 환경 0 + 1시간 배포" acceptance 가 Ops Pack 으로 이미 충족 — SaaS 차별화 작음, ④ 단독 메인테이너 24/7 운영 캐파시티 초과) / §5 SaaS 진입 재평가 4 조건 ALL-AND 게이트 (① 누적 자산 multi-tenant 격리 모델 자연 진화 + G-7x 가드 1건 / ② 외부 수요 ≥3 조직 + SaaS 요청 ≥1건 실 발언 / ③ 24/7 on-call 2인+ 또는 managed cloud 자동화 / ④ self-host 불가능 차별화 가치 1건 — 시간 기반 재평가 금지) / §6 v1.0 self-host 출시 게이트 (후속 Growth, M4 범위 밖): M3 Slice b orchestrator 자동 호출, Slice c web /ops/download zip, Slice d Vault Agent sidecar, Slice e Keycloak/OIDC SSO 바인딩, M5 Slice C-b Gradle 입력 / §7 6-axis 누적 무손상 확인 / §8 참조). AGENTS.md M4 라인 갱신 ("self-host 우선, SaaS 는 M4 진입 시 재평가" → "self-host 단일 모드 v1.0 확정 — Growth-73; SaaS 는 v2.0 진입 조건 4건 모두 충족 시 재평가. 결정 근거 → `docs/hosting-modes.md`"). **신규 가드 미신설 사유 (G-73 부재)**: M4 는 **정책/결정 마일스톤**이지 코드 contract 가 아니다. M0/M1(G-69)/M5(G-70)/M3(G-71)/M2(G-72) 가 모두 정적 회귀 가드를 동반한 것과 대비. 결정 문서 `docs/hosting-modes.md` 가 회귀하려면 (a) 의도적 의사결정 번복(re-evaluation entry condition 충족 후) 또는 (b) 문서 직접 편집 — 둘 다 정적 grep 검출 의미 없음. **창의적 결정**: SaaS 재평가 트리거를 시간 기반("1년 뒤") 이 아닌 **4조건 ALL-AND 게이트** 로 잠금. 막연한 시간 재평가가 의도 드리프트(이번엔 그냥 미루자) 를 유발하는 트랩을 차단. **6-axis 보전 확인**: 본 Growth 는 정책 결정만 — skill/ddl/mybatis/nexacro/creater/customer 어느 축도 코드 변경 없음. `scripts/` · `andrej-karpathy-rdb-*/` · `profiles/` 무손상. self-host 단일 모드는 "6축이 동일하게 자라되 runtime 노출 표면이 사용자 인프라 안에 머문다" 는 의미. **G1 roadmap 클로저**: M0→M1(G-69)→M5(G-70)→M3 Slice a(G-71)→M2(G-72)→**M4(G-73)** 5 마일스톤 1차 게이트 모두 통과. v1.0 출시 게이트로 M3 Slice b-e + M5 Slice C-b 보강이 남음(self-host 깊이 보강, SaaS 분기 없음). |
| Growth-72 | 2026-05-28 | **M2 Exec Status Board — `scripts/workflow/status_board.py` 신규 + portal 통합 + G-72 가드 등재**. G1 마일스톤 M2: CEO 페르소나가 6-axis 누적 자산을 한눈에 볼 수 있도록 Growth-55 정적 portal(`docs/index.html`) 상단에 status 섹션 주입. **3 sub-task (S2.1–S2.3) 누적**: S2.1 `scripts/workflow/status_board.py` (stdlib-only — `extract_section()` `## N.` 헤딩 앵커 파서 + `extract_growth_count()` §6 max-N regex (range row 포함) + `extract_domain_count()` §2 table row count + `extract_verification_matrix()` §1 lane × runner ✅/⚠️/⛔ → verified/partial/pending 분류 + `extract_latest_growths(limit=5)` §6 single-date row 만(`Growth-N \| YYYY-MM-DD \| head` regex) + `extract_trap_guards_count()` diagnose.py `N trap guards intact` regex + `count_customer_profiles()` `profiles/*.yaml` (`_README.md` 제외) + `compute()` 드라이버(파일 부재 graceful) + `StatusBoard`/`VerificationRow`/`GrowthEntry` dataclasses + `render_status_section()` HTML 조각(metric tiles 5 + verification 테이블 + latest-5 리스트, HTML-escape) + `STATUS_BOARD_CSS` 상수(별도 asset 작성 금지 — caller 가 style.css 에 append) + `to_dict()` JSON dump + CLI(`--json/--learn-log/--diagnose/--profiles-dir`)). S2.2 `tests/workflow/test_status_board.py` 22 신규 케이스(section extractor 3 + growth_count 3 + domain_count 1 + verification 1 + latest_growths 2 + trap_guards 2 + customer_profiles 2 + compute 2 + HTML render 2 + JSON round-trip 1 + CLI text+json 2 + real-file drift smoke 1) → 22/22 그린. S2.3 portal 통합 + G-72 가드 신설. **portal 통합**: `web_index.py` `_write_assets` 가 `status_board.STATUS_BOARD_CSS` 를 style.css 에 append, `render_index_html` 이 `status_section: str = ""` 키워드 인자로 받아 `<main>` 안 tile-grid 위로 주입, `build()` 가 `status_board.compute()` + `render_status_section()` 호출 (실패 시 warnings 에 기록하고 빈 fragment 폴백 — portal build resilience 보전). **G-72 (Growth-72 Status Board emitter contract)**: `diagnose.py:check_cross_layer_coherence` 가드 11→**12건**. `scripts/workflow/status_board.py` 가 5 마커(`def compute(`, `def render_status_section(`, `def extract_trap_guards_count(`, `STATUS_BOARD_CSS`, `Growth-72`) 유지. 회귀 시 CEO 페르소나의 "축적된 자산 한눈에 보기"(M2 acceptance) 가 깨지고 portal 의 status 섹션이 비어버린다. FAIL hint 에 G-72 추가, PASS detail `12 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71/72)`. **회귀 3 신규 test_diagnose 케이스**(status_board 부재 / render_status_section 손실 / Growth-72 헤더 손실) → tests/workflow/test_diagnose.py 39→**42** 그린. **창의적 결정**: status_board 는 **읽기 전용** — `orient`/`list-domains`/`diagnose` 패턴을 따라 새 데이터 소스를 도입하지 않고 기존 learn-log §1/§2/§6 + diagnose.py 텍스트 + `profiles/*.yaml` 만 파싱. asset 작성은 web_index 가 단일 소유 — status_board 는 CSS 상수만 노출하고 callers 가 append. M2 acceptance 1차 게이트(portal status 섹션 가시)만 깊고 좁게 충족. **6-axis 보전 확인**: status_board 가 데이터 소스를 추가하지 않고 기존 6축의 누적 상태만 읽음 — 7th axis 부주의 도입 없음. AGENTS.md 업데이트(가드 카운트 11→12 + G-72 항목). **알려진 격차**: `/status` 슬래시 커맨드(터미널 노출), JSON snapshot artifact(`docs/status.json` write-back), Growth 추세 차트(SVG sparkline) 는 후속 슬라이스 — M2 1차 게이트와 분리. |
| Growth-71 | 2026-05-28 | **M3 Ops Pack Slice a — `scripts/emit_ops_pack.py` 신규 + G-71 가드 등재**. G1 마일스톤 M3 첫 산출물: IT-담당자 페르소나가 dev 환경 없이 scaffold 출력만으로 1시간 내 자체 배포 가능하도록 `<out-dir>/shell/` 을 입력으로 받아 `ops/` 4 산출물(Dockerfile + docker-compose.yml + .env.example + DEPLOY-SOP.md) 을 emit 하는 standalone CLI. **3 sub-task (S3.1–S3.3) 누적**: S3.1 `scripts/emit_ops_pack.py` (377 LoC, stdlib + PyYAML — `inspect_scaffold()` `xml.etree` 로 `shell/pom.xml` 파싱 → `{artifact_id, version, packaging, lane(jakarta/javax), dialect(postgres/mysql/hsqldb)}` 추출 + `render_dockerfile()` multi-stage `FROM maven:3.9-eclipse-temurin-17 AS builder` → war(`tomcat:10.1`/`9.0` lane 분기) 또는 jar(`eclipse-temurin:17-jre` + `ENTRYPOINT`) + `render_compose()` postgres → `postgres:16-alpine` + pg_isready healthcheck + volume / mysql → `mysql:8.0` + mysqladmin / hsqldb → db 서비스 생략(embedded) + `render_env_example()` `APP_PORT=8080` + `${SLUG_UPPER}_DB_{HOST,PORT,NAME,USER,PASS}` 블록(dialect 별 포트: 5432/3306) + `render_sop()` Korean 8-절 runbook(사전준비/압축해제/환경변수/컨테이너기동/헬스체크/SSO/트러블슈팅/백업/Vault) + `emit()` driver(존재 시 `--force` 없으면 `FileExistsError`) + `_load_profile()` v1 강제 + customer profile override(slug → env prefix, dialect → 강제 교체)). S3.2 `tests/test_emit_ops_pack.py` 23 신규 케이스(inspect 3 + Dockerfile 3 + compose 3 + env 3 + SOP 2 + profile override 1 + emit driver 3 + profile loader 2 + CLI 3) → 23/23 그린. S3.3 G-71 가드 신설. **G-71 (Growth-71 Ops Pack emitter contract)**: `diagnose.py:check_cross_layer_coherence` 가드 10→**11건**. `scripts/emit_ops_pack.py` 가 6 마커(`def render_dockerfile(`, `def render_compose(`, `def render_env_example(`, `def render_sop(`, `maven:3.9-eclipse-temurin-17 AS builder`, `Growth-71`) 유지. 회귀 시 IT-담당자 페르소나의 "dev 환경 없이 1시간 배포" 시나리오가 깨진다 — 4 산출물 중 하나라도 누락되거나 single-stage Dockerfile 로 회귀하면 사용자가 로컬에 Maven/JDK 설치를 강요받음. FAIL hint 에 G-71 추가, PASS detail `11 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70/71)`. **회귀 3 신규 test_diagnose 케이스**(emitter 부재 / multi-stage 빌더 손실 / render_* 함수 손실) → tests/workflow/test_diagnose.py 36→**39** 그린. **창의적 결정**: emitter 는 standalone CLI 로 분리 — `scaffold_orchestrator` 통합(Slice b)·web `/ops/download` 노출(Slice c)·Vault 후크(Slice d)·SSO 바인딩(Slice e) 은 후속 슬라이스로 분리해 M3 acceptance 1차 게이트(`/ops/<slug>/Dockerfile` 외 4 파일 생성)만 깊고 좁게 충족. emit 은 사용자가 명시 호출(`python scripts/emit_ops_pack.py out/acme-customer --profile profiles/acme.yaml`) — 자동 실행 안 함, 의도 노출. **6-axis 보전 확인**: ops 산출물의 모든 분기(lane/dialect/slug/datasource) 가 기존 customer 축(`profiles/_README.md` v1) 스키마에서 도출 — 7th axis 부주의 도입 없음. AGENTS.md 업데이트(가드 카운트 10→11 + G-71 항목). **알려진 격차** (M3 후속 슬라이스): Slice b `scaffold_orchestrator` 자동 호출 통합, Slice c `web/routes/ops.py` /ops/download zip, Slice d Vault 후크(HashiCorp Vault Agent template), Slice e SSO 바인딩(Keycloak/OIDC sidecar 패턴), Gradle 입력 지원. |
| Growth-70 | 2026-05-28 | **M5 Slice C — target_project overlay (customer profile 자동 추출) + G-70 가드 신설**. M1 입력단을 보완: 기존 SpringBoot 프로젝트 디렉터리를 입력으로 받아 `profiles/<slug>.yaml` (6번째 축 customer 스키마 v1) 을 자동 추출하는 CLI 확보. 사용자가 두 번째 도메인부터 손댈 곳 없는 복리 효과를 위해 첫 번째 도메인의 profile 작성 비용 자체를 자동화. **3 sub-task (S5.1–S5.3) 누적**: S5.1 `scripts/extract_target_profile.py` (`parse_pom()` xml.etree → groupId/artifactId/version/name/jakarta-vs-javax lane detection + `parse_application_config()` application.yml/.properties flat dict + `_JDBC_DIALECTS` 6종 prefix 매핑(postgres/mysql/hsqldb/h2/oracle/mssql) + `derive_slug()` `-shell`/`-portal`/`-uiadapter`/`-shell-mdi` 등 6 suffix 스트립 + `derive_base_package()`/`derive_project_root_pkg()` type-aliases-package 와 groupId 폴백 머지 + `derive_url_template()` JDBC URL → 파라메트릭 `jdbc:.../{host}:{port}/{db}` + ${SLUG_UPPER_DB_USER/PASS} 자동 placeholder + `build_profile()` v1 스키마 dict 조립 + `dump_profile()` yaml.safe_dump + Growth-70 헤더 + CLI `--slug`/`--out`/`--force`/`--print`/`--force` argparse). S5.2 `tests/test_extract_target_profile.py` 24 신규 케이스(slug 도출 파라메트라이즈 6 + dialect 8 + jakarta+postgres/javax+mysql/hsqldb-default/missing-pom-error/invalid-slug-reject 5 fixture 전체 빌드 + load_customer_profile 라운드트립 1 + dump_profile 헤더 1 + CLI --print/--force/missing-project 3) → 24/24 그린. S5.3 G-70 가드 신설. **G-70 (Growth-70 target_project extractor contract)**: `diagnose.py:check_cross_layer_coherence` 가드 9→**10건**. `scripts/extract_target_profile.py` 가 4 마커(`def build_profile(`, `"version": 1`, `"slug": slug`, `Growth-70`) 유지. 회귀 시 추출된 profile 이 `load_customer_profile` 의 G-62/G-63 pin(version: 1 enforcement) 통과 못해 M5 입력단이 silent corruption — 사용자가 손으로 profile 만드는 시대로 되돌아감. FAIL hint 에 G-70 추가, PASS detail `10 trap guards intact (G-47/48/50a/50b/58/61/62/63/69/70)`. **회귀 3 신규 test_diagnose 케이스**(extractor 부재 / version stamp 손실 / Growth-70 헤더 손실). **창의적 결정**: extractor 는 status=`draft` 만 emit — 사용자가 수동 review (contact 채움, host/port override, env var setup) 후 `active` 로 승격. 자동화의 자신감을 과시하기보다 사람의 한 번 review 를 유도해 customer profile 가 의미 있는 자산이 되도록 함. **회귀**: tests/test_extract_target_profile.py 24/24 + tests/workflow/test_diagnose.py 36/36 그린. **6-axis 보전 확인**: extractor 가 emit 하는 YAML 의 모든 필드가 기존 `customer` 축 스키마(`profiles/_README.md` v1) 에 정확히 매핑 — 7th axis 부주의 도입 없음. AGENTS.md 업데이트(가드 카운트 9→10 + G-70 항목). **알려진 격차**: Gradle (`build.gradle(.kts)`) 추출은 Slice C 범위 밖 — M5 Slice C-b 에서 처리 예정. 웹 폼 `/target/upload` 통합도 별도 슬라이스(M5 Slice C-c) 로 분리. |
| Growth-69 | 2026-05-28 | **M1 User Web Form (web/ FastAPI layer) + G-69 가드 신설**. 비전문 사용자 3 페르소나 G1 goal 진입 — 업무 담당자가 dev 환경 없이 도메인 1개 정의→preview→zip 다운로드까지 브라우저로 수행. **8 sub-task (S1.1–S1.8) 누적**: S1.1 FastAPI 스켈레톤(`web/app.py` create_app factory + `web/settings.py` env-based config + `web/templates/{base,home}.html` + `web/static/css/main.css` + 3 home route tests), S1.2 `web/adapters/scaffold_runner.py` (ScaffoldRequest/StageResult/ScaffoldResult Pydantic 모델 + `subprocess.run` → `scripts/scaffold_cli.py` + `_parse_report()` scaffold-report.md `## Stages` regex 파싱 + 7 tests), S1.3 `web/adapters/zip_emitter.py` (stdlib zipfile in-memory bytes + DEFAULT_EXCLUDES + 결정적 sorted iteration + 7 tests), S1.4 `web/routes/domain.py` GET/POST `/domain/new` + `web/run_registry.py` UUID4 in-memory store + `web/templates/domain_form.html` Korean labels + 11 tests(검증·422·303 redirect), S1.5 GET `/domain/{run_id}/preview` + `web/templates/domain_preview.html` (성공/실패 badge + stage 테이블 + collapsible logs + download CTA) + 8 tests, S1.6 GET `/domain/{run_id}/download` (`Content-Disposition: attachment; filename="<slug>.zip"` + 404/409 분기) + 6 tests, S1.7 smoke E2E `test_persona_smoke.py` (monkeypatched runner 5-step journey) + gated real `test_persona_e2e.py` (`RUN_E2E=1` skip + 30-min wall-clock budget) + `pytest.ini` `e2e` marker 등록, S1.8 G-69 가드 신설. **G-69 (Growth-69 Web-axis subprocess invariant)**: `diagnose.py:check_cross_layer_coherence` 가드 8→**9건**. `web/adapters/scaffold_runner.py` 가 `subprocess.run` + `scaffold_cli` + `scaffold_cli_path` + `def run(` 4 마커 유지 + `web/routes/domain.py` 가 `scaffold_runner.run` 호출 유지. web 경로에서 scaffold 로직 재구현 시 6-axis 누적(skill/ddl/mybatis/nexacro/creater/customer) 이 사용자 진입 표면에서 우회됨. FAIL hint 에 G-69 추가, PASS detail `9 trap guards intact (G-47/48/50a/50b/58/61/62/63/69)`. **회귀 3 신규 test_diagnose 케이스**(runner subprocess 손실 / 파일 부재 / domain route call 손실). **창의적 결정**: web/ 는 7번째 축이 아니라 **creater 축의 frontend layer** — 모든 scaffold 로직은 sibling repo + scaffold_cli 에 위임, web 은 form 변환·subprocess 호출·zip 응답만 담당. AGENTS.md 업데이트(G-69 항목). **회귀**: tests/web/ 43 passed + 1 skipped(e2e gate) + tests/workflow/test_diagnose 33/33 그린. **자산 노출형 하네스 패턴 보전**: scaffold_cli.py / scaffold_orchestrator / sibling repos 무변경 — web/ 는 노출 표면일 뿐. **gated E2E 알려진 격차**: form 이 `wiki_mode=preset` 시 `preset` 필드 required 검증 미구현 → 실 scaffold subprocess 가 즉시 실패. UX polish 차후 (별도 sub-task 또는 M2 진입 시 처리). G1.1 acceptance gate 는 smoke test 의 HTTP 와이어링 검증으로 통과; 라이브 subprocess E2E 는 form 검증 보강 후 재실행 예정. |
| **Growth-1 ~ Growth-25** | 2026-04 ~ 2026-05 | 도메인 14종 + 패턴 7종 + lane/dialect/overlay 인프라 — 상세는 [`wiki/learn-log-archive-2026-04.md`](wiki/learn-log-archive-2026-04.md) |
| **Growth-26 ~ Growth-40** | 2026-05 | 풀테스트 4계층 인프라 (live_overlay/runner/probe/crud), 4 슬래시 커맨드, `/full-test --json`, codegen 결함 4건 — 상세는 [`wiki/learn-log-archive-2026-05-early.md`](wiki/learn-log-archive-2026-05-early.md) |
| Growth-41 | 2026-05-21 | nexacro **복잡 화면 패턴(Complex Screen Patterns)** 미러 + Stage 4 loader 연동 — 외부 출처 [`nexacro-claude-skills/.../nexacro-form-maker/SKILL.md#복잡-화면-패턴`](https://github.com/JasonMMo/nexacro-claude-skills) 의 5개 복합 패턴 중 **이미 등재된 MD** 를 제외한 **4종** 을 `rdb-nexacro/.claude/skills/karpathy-rdb-nexacro/patterns/` 에 신규 디렉터리로 추가: **MT** (multi-tab-N-tier — 3탭 lazy-loading, 탭2 child entity 자동 wiring), **TG** (tree-grid-2-pane — 좌측 `dsCategory` treeitemcontrol + 우측 entity 그리드, 카테고리 선택 → `dsSearch.{category_fk_column}` 자동 주입), **PS** (popup-search-1-tier — `<Form classname="Popup">` + `opener.fnReceivePopupData(oData)` 콜백, oncelldblclick 선택, 코드 마스터/거래처 picker), **MDS** (multi-dataset-save-header-lines — 헤더 BindItem + 라인 grid 를 `dataList=ds{M}:U,dsChildList=ds{C}:U` 단일 transaction 동시 저장, 주문서/발주서/송장 작성). 각 패턴 = `manifest.yaml` + `form.xfdl.j2` + `README.md` 3-파일 컨벤션 준수. **새 템플릿** `templates/header_field.xml.j2` (MDS 헤더 BindItem 렌더). `form_composer.compose_form` 에 `pattern == "MDS"` 분기 추가 — 헤더 필드 자동 렌더 후 `ctx["header_fields"]` 주입. **테스트** 8개 신규 (pattern_loader 4 파라메트라이즈 + form_composer 렌더 4) → 93/93 그린. `references/patterns-spec.md` 에 Growth-41 절 추가 (MD vs MDS 구별 명시). 패턴 코드 컬렉션: 7개 → **11개** 로 확장 (D2/F1/C1/L2/MD/TR/RO + MT/TG/PS/MDS, SHELL 은 화면 패턴이 아닌 shell 패턴 별도). blueprint 사용 예: `entities[*].pattern: MT\|TG\|PS\|MDS`. |
| Growth-43 | 2026-05-22 | **karpathy alignment plan 완료** — `loader.py` `extends` 해결자 구현(M3 잔여 갭). `_find_catalog_entity()` 가 `~/.karpathy-rdb/catalog/*.seed.md` 스캔 → 이름 일치 entity 블록 반환. `_resolve_extends()` 가 base columns/indexes/constraints 를 merge 후 현재 entity 우선(same-name override). `load_blueprint()` 가 모든 entity 를 자동 resolve — extends 없으면 passthrough, 있는데 catalog 미발견 시 `BlueprintError`(silent fallback 금지). `test_extends.py` 13 테스트 신규(find/resolve/integration) → **121/121 그린** (rdb-ddl, 1 skipped). 2 커밋 pushed `andrej-karpathy-rdb-ddl` origin/master (0de75d4·2e9408e). M1-M3 5축 자체평가: 반복 데이터 효율(4) · 지식 누적(5) · 풍부한 Seeds(5) · WAR 완성도(3, 메커니즘) · Karpathy 정신(5) = **평균 4.4/5**. |
| Growth-42 | 2026-05-21 | nexacro **envelope CRUD round-trip** probe — Growth-40 가 미뤘던 nexacro 엔벨로프 wire 를 신규 모듈 `live_crud_nexacro.py` 로 구현: `POST /uiadapter/<entity>/save_datalist_map.do` + `<Root xmlns=".../platform/dataset"><Dataset id="ds<Pascal>"><Rows><Row Type="insert"\|"delete">...</Row></Rows>` 5-step (baseline `select_datalist_map.do` → insert envelope → +1 verify → delete envelope → baseline verify). `<Col id="...">` XML escape (`&` `<` `>`) + `null → <Col/>` + bool → `"true"/"false"`. `_parse_error_code`/`_count_rows` 는 live_probe 의 정규식 contract 재사용(circular import 회피 위해 inline). 디스패처 일반화: `lane_runner_map.lane_crud_kind(lane)` → `"rest"` (jakarta/javax/vanilla) \| `"envelope"` (nexacro), `lane_supports_crud` 는 backward-compat shim — 이제 nexacro 도 True. `full_test.run_l4_live` 가 probe full pass 직후 `ckind` 로 dispatch — REST 는 `live_crud.crud_roundtrip_rest`, envelope 는 `live_crud_nexacro.crud_roundtrip_envelope(select_url, save_url, dataset_id=ds<Pascal>, insert_row, pk_column, pk_value)`. 결과: `layers["L4_crud"]`(bool)/`["L4_crud_reason"]`(str)/`["L4_crud_kind"]`("rest"\|"envelope") — 실패 시 어느 직렬화 스택(REST controller vs NexacroResult)인지 즉시 어트리뷰트 가능. 신규 23 테스트(envelope builder 8 + 5-step happy 1 + 7 error paths + 6 dispatcher/helper + transport contract) + 기존 `test_run_l4_live_crud_skipped_for_nexacro_lane` 를 `..._dispatches_envelope_crud` 로 의미 반전(REST 0회/envelope 1회 + dataset_id="dsLead" + save_url 컨벤션 검증) → **173/173 그린**. `nexacro_dataset_id(entity)` = snake → `ds<Pascal>` (`account` → `dsAccount`, `order_item` → `dsOrderItem`). **라이브 검증 (2026-05-22, real `boot-jdk17-jakarta` runner, `out/customer-growth42-nexacro` scaffold, dialect=hsqldb)**: L1 4-repo pytest rc=0 · L2 schema+data 12+12 stmt OK · L3 SKIP=PASS (nexacro lane convention) · L4 overlay 26w/2e + mvn rc=0 + probe 200/ErrorCode=0/rows=3 + **CRUD envelope ok=True baseline=3→+1=4→final=3** → **풀테스트 그린**. 환류 codegen 결함 2건 동시 수정: (a) `AddressServiceImpl` 등 generated service 의 unsafe `_RowType_` 추출 → runner `BoardServiceImpl` 패턴 (instanceof + DEFAULT fallback) 로 안전화, (b) mapper.xml 의 SQL 컬럼은 UPPER (HSQLDB unquoted-normalize) 유지하되 `#{placeholder}` + `<if test>` key 는 lowercase 로 통일 — envelope `<Col id="customer_id">` ↔ Map key 대소문자 mismatch 로 인한 silent INSERT-skip 차단. 추가 코드 정리: `cleanup_runner` 가 generated overlay tree(`com.example`) 만 제거하고 runner 자체 패키지(`com.nexacro.uiadapter`) 는 보존. |
| Growth-45 | 2026-05-22 | **v0.6 C-2 유사 도메인 추천 강화** — `protocols/01-init.md` Phase 1 Q2 를 3단계 fallback 으로 재구성: (2-1) 로컬 `presets/INDEX.md` 매칭(v0.3.x 점수 알고리즘 그대로 — 제목/aliases +5, keywords 토큰당 +2, entities +3, 한 줄 +1~3) → (2-2) `~/.karpathy-rdb/catalog/` 글로벌 frontmatter 추가 스캔, 동일 알고리즘으로 후보 풀 합산(로컬·글로벌 동명 도메인 시 글로벌 우선·점수는 max) → (2-3) `~/.karpathy-rdb/catalog/meta/<X>_meta.md` (C-1 출력) 존재 시 반복 등장 entity/concept/false-belief 표를 추천 컨텍스트로 부착. 글로벌 디렉터리/메타 파일 부재 시 단계별 **SKIP=PASS**(로컬 INDEX only fallback). 점수 분기 ≥5/2~4/0 + 출처 라벨(local|global) 사용자 노출. 벡터 DB 금지(Karpathy 정신). `SKILL.md` 에 "유사 도메인 추천 (v0.6 C-2)" 1단락 추가. 신규 6 테스트(`test_init_protocol_c2.py` — 3단계 헤더/글로벌 경로/메타 경로/SKIP=PASS/§7.2 참조/점수 임계) → **194/194 그린**. 알리먼트 리뷰 §7.2 충족(C-1 짝꿍, 진입 게이트 = 글로벌 카탈로그·메타 부재 시 자동 폴백). |
| Growth-44 | 2026-05-22 | **v0.6 C-1 메타 추출 진입** — Phase A 정렬(§0 Layer Ownership Card + CLAUDE.md 5축 + 4 layer SKILL.md ownership + contribute-back self-check + bgIsolation settings) 8커밋 선행. `andrej-karpathy-rdb-skill/scripts/meta_extract.py` 신규 — 복수 프로젝트의 `wiki/learn-log.md` 횡단 → 도메인별 반복 등장(≥`--min-projects`, default 3) entity/concept/rule/false-belief 를 `~/.karpathy-rdb/catalog/meta/<도메인>_meta.md` 로 산출. **2단 게이트**: (a) 프로젝트 수 < threshold → SKIP=PASS, (b) 도메인 등장 프로젝트 수 < threshold → 해당 도메인만 SKIP, (c) 모든 도메인 미달 → SKIP=PASS. CLI: `extract --project <p>... [--projects-file <f>] [--output-root <dir>] [--min-projects N]`. contribute 이벤트(kind=`contribute`/`pattern-contribute`)는 메타 추출 대상 제외(메타-메타 회피). 신규 12 테스트(parse/aggregate/skip-gate 3종/render/CLI 양형) → **188/188 그린** (rdb-skill 전체). 명시 호출 전용·자동 머지 없음·LLM/벡터 검색 없음(Karpathy 정신 유지). 알리먼트 리뷰 §7.1 진입조건(실 프로젝트 3개) 도달 시 자동 가용 — 현재 wiki 컨벤션 프로젝트 0개라 dry-run 은 [warn] rc=2 정상. |
| Growth-46 | 2026-05-22 | **G-Jackson gap 해결** (Growth-33 부터 미해결) — `nexacroN-fullstack/samples/runners/boot-jdk8-javax/pom.xml` `<dependencyManagement>` 에 **jackson-bom 2.17.2 import 를 spring-boot-dependencies(2.7.18) 보다 먼저** 배치. Maven BOM 임포트는 first-wins 규칙(전이 의존성 해결 시 먼저 선언된 BOM 우선) → jackson-core/annotations/databind/datatype-jdk8/datatype-jsr310/module-parameter-names **6개 모듈 모두 2.17.2 통일**. 원인: 이전엔 `jackson-databind` 만 explicit 2.17.2, 나머지는 spring-boot 2.7.18 BOM 의 ~2.13.5 사용 → databind 가 `JsonToken.valueDescFor`(Jackson 2.16+ API) 호출 시 core 2.13.5 가 `NoSuchMethodError` 던짐 → NexacroResult 직렬화 500. **검증 3-tier**: (a) `mvn dependency:tree` jackson-* 6개 모두 `2.17.2:compile` 확인, (b) `mvn -DskipTests package` BUILD SUCCESS (30.9s), (c) runner boot + `POST http://localhost:8080/uiadapter/select_datalist_map.do` (board domain runner-default) 엔벨로프 → HTTP 200 + `<Parameter id="ErrorCode" type="int">0</Parameter>` + `<Dataset id="output1">` 23 rows 직렬화. NoSuchMethodError 부재. **codegen 결함 아님 — runner-side dep 픽스만**. §0 mybatis 행 미해결 환류 갱신, §5 G-Jackson 항목 strikethrough + 픽스 SHA 후속 첨부. javax lane fresh scaffold 종단 검증(L1-L4 그린 라벨)은 별도 task — 현 `out/sales-growth33-javax` 가 pgsql DDL 사용해 HSQLDB 검증대 부적합. |
| Growth-49 | 2026-05-22 | **javax fresh-scaffold 풀테스트 그린** (Growth-46/47/48 3-픽스 누적 종단검증) — `customer` 도메인 + `--lane nexacro --dialect hsqldb --uia-namespace spring` fresh scaffold(`out/customer-growth49-javax`) → `scripts/workflow/full_test.py javax` 실행 → **모든 4계층 PASS + 라벨 "풀테스트 그린"**. L1 4-repo pytest rc=0 · L2 schema 12 stmts + seed 12 stmts OK · L3 SKIP=PASS(runner-provides-pom) · L4 overlay 26w/2e + mvn rc=0 + probe envelope http=200 errcode=0 rows=3 + CRUD envelope ok=True baseline=3→+1=4→final=3. **결정적 검증 메시지**: `[L4] scaffold_lane=nexacro differs from runner lane=javax — probe/CRUD dispatch follows scaffold` (Growth-48 픽스가 envelope endpoint 로 dispatch). 3-픽스 누적 의미: G-Jackson(Growth-46, runner-side jackson-bom first-import) → NexacroResult JSON 직렬화 NoSuchMethodError 해소; T-NexacroUiaPkg-javax(Growth-47, `--uia-namespace spring` 파라미터 6단계 환류) → ParamDataSet/NexacroResult symbol-not-found 해소; T-Probe-LaneRunner-Mismatch(Growth-48, scaffold_lane override) → REST probe 가 envelope endpoint 미스 해소. 회귀 0 (신규 코드 변경 없음 — 기존 픽스 검증만). cleanup 후 runner clean(`git status --short` 0줄). §0 creater row 미해결 환류 deferred 1건은 그대로(cleanup_runner PowerShell quoting). §1 javax row `⚠️ Growth-47` → `✅ Growth-49`. 알려진 잡음: `full_test.py` cleanup의 `Remove untracked mappers` 가 PowerShell subexpression quoting 으로 FAIL → 수동 `Remove-Item` 으로 처리(creater row 미해결 환류와 동일 트랩). |
| Growth-48 | 2026-05-22 | **T-Probe-LaneRunner-Mismatch 픽스** (Growth-47 잔여 차단) — `scripts/workflow/lane_runner_map.py` 의 4개 dispatcher(`lane_probe_kind` / `lane_probe_url` / `lane_crud_kind` / `lane_supports_crud`) 에 optional `scaffold_lane: str \| None = None` 매개변수 추가. 신규 `_effective_lane(lane, scaffold_lane)` helper 가 supplied 시 scaffold_lane 우선, 미지정 시 lane 그대로(pre-Growth-48 API 무손상). `scripts/workflow/live_overlay.py` 에 `discover_scaffold_lane(scaffold_dir)` 신규 — `scaffold-report.md` 의 `^\s*-\s*lane:\s*\`<name>\`` 라인을 regex 로 추출, 파일/라인 부재 시 graceful None. `scripts/workflow/full_test.py:run_l4_live` 가 probe/CRUD dispatch 전 `discover_scaffold_lane` 호출 → 4개 dispatcher 모두에 `scaffold_lane=` 키워드 전달, divergence 발견 시 informational print(`[L4] scaffold_lane=... differs from runner lane=...`). **wire-protocol 의 진실원천이 deployment runner 가 아니라 Stage 3 codegen 산출물** 임을 코드로 인코딩. 신규 11 테스트 — `test_lane_runner_map.py` 5(scaffold_lane override probe_kind/url/crud_kind + None legacy preserve + unknown ValueError), `test_live_overlay.py` 4(report-only/3-lane parametrize/missing-file/missing-line), `test_full_test.py` 2(scaffold_lane=nexacro on javax runner uses envelope · no-report falls back to runner lane). 회귀: workflow suite 82/82 그린, 본 코어 변경 후 sibling pre-existing failures(`test_live_crud_nexacro` 7건 — Growth-42 후속 stale tests, 본 작업 무관) 외 zero regression. §0 creater row trap 카운트 그대로 3(T-Probe-LaneRunner-Mismatch 가 트랩 → RESOLVED 라벨 갱신) + 미해결 환류에서 제거. §4 트랩 행 상태 deferred → RESOLVED. **연계로 vanilla lane 검증대 신설** (mybatis row 미해결 환류 "vanilla 검증대 부재" 동시 해소): `board` 도메인 fresh scaffold (`--lane vanilla --dialect hsqldb`) → `scripts/workflow/full_test.py vanilla` → 라벨 **JDBC + 빌드까지만 검증 → javax-host 검증** (L1 4-repo pytest rc=0 · L2 schema+seed OK · L3 `mvn package` rc=0 · L4 overlay 26w/2e PASS · L4 mvn rc=1 = boot-repackage 가 stale `runner-boot-jdk8-javax-0.1.0-SNAPSHOT.jar` 잠금 해제 실패 = **환경성, codegen 결함 아님**). 핵심 발견: `controller.vanilla.java.j2` 가 `org.springframework.web.bind.annotation.*` 만 사용(`jakarta.servlet`/`javax.servlet` import 없음) → vanilla scaffold 는 Spring 5(`boot-jdk8-javax`) / Spring 6(`boot-jdk17-jakarta`) 양 러너 호환. `BoardController` 산출물에서 servlet-API import 0건 직접 확인. §1 vanilla row 가 `⚠️ 검증대 부재` → `✅ Growth-48 (board scaffold)` 로 갱신, "vanilla → javax-host 검증" 라벨 컨벤션이 라이브 검증대 매트릭스에 최초 적용. |
| Growth-51 | 2026-05-26 | **서비스 리뷰 R1/R3/R4 환류** (외부 사용자 첫인상 3종 동시 보강 — agent/feature 신설 없이 기존 orchestrator 만 손댐). **R1 (slug silent-fallback 제거)**: `scaffold_cli.py` 가 한글 도메인(`주문관리` 등)에서 silent `slug=domain` fallback → 사용자가 출력 디렉터리/패키지 이름이 망가진 줄 모르고 진행하던 트랩. 신규 `resolve_slug(domain, *, explicit_slug, package_fallback)` 3-source 함수 + `--slug <ascii-name>` CLI 인자 + `--package` 마지막 segment 자동 fallback + `INFO:` 알림(stderr). source 우선순위: `explicit > derived > package-fallback`, 모두 실패 시 `cannot derive slug` ValueError 로 즉시 중단. 기존 `derive_slug()` 함수는 backward-compat 유지. 신규 6 unit tests(`test_scaffold_cli::TestResolveSlug` 전 source path 커버). **R3 (full-test 실패 시 다음 명령 1줄 힌트)**: `full_test.py` 신규 `recovery_hint(layers, lane, scaffold)` — L1 실패 → `pytest -q -v` 위치, L2 → `HSQLDB_JAR` env + `jdbc_smoke.py`, L3 → `mvn -X package -DskipTests in <scaffold>`, L4 → `Get-Content <runner>/was.log -Tail 60`, L4-partial 만 → "probe URL 직접 호출(/uiadapter prefix 포함)". `FullTestResult.next_hint: str|None` dataclass field 추가 + `to_dict()` JSON 출력 동봉 + main() 비-JSON 분기 print. 신규 7 unit tests(L1/L2/L3/L4/L4-partial 각 케이스 + all-green-None + JSON 노출 검증). **R4 (`/list-domains` 슬래시 커맨드)**: `presets/INDEX.md` 14 도메인을 한 줄 요약으로 노출 — `/scaffold` 진입 전 도메인 선택 컨텍스트 단축. 신규 `scripts/workflow/list_domains.py` (parse_index_md/Domain dataclass/format_table/load_domains/CLI `--index/--verbose/--json`) + `.claude/commands/list-domains.md` (frontmatter `business-fullstack-creater:list-domains`). `## 매칭 알고리즘` meta 절은 skip. 신규 8 tests(parse skip-meta/4-field/format-default/format-verbose/empty/CLI-json/CLI-missing/real-INDEX.md 14-domain smoke). **회귀**: R1 18/18 그린 (scaffold_cli) · R3 51/51 그린 (full_test) · R4 8/8 그린 (list_domains). **후속 안전망 정비** (별도 commit 3ad084d): R3 recovery_hint 가 처음으로 회귀 안전망 위에서 의미를 가지려면 tests/ 풀 회귀 rc=0 가 베이스라인이어야 함 → Growth-42 후속 stale 7건(`test_live_crud_nexacro`) 정렬. 진실원천 두 가지가 test 에 미반영 상태였음: (a) `nexacro_dataset_id(entity)` 가 항상 literal `"dataList"` 반환(commit 5a6cd57 — scaffold-generated controller 가 `@ParamDataSet(name="dataList")` 로 선언, `ds<Pascal>` 은 form-side XFDL Transaction 한정), (b) save envelope row state = `<Col id="_RowType_">I|D</Col>`(commit a8fea49 — PlatformXmlDataDeserializer 는 `_RowType_` Col 만 인식, `<Row Type="...">` attribute 는 framework `hasData()` guard 가 silent filter → 빈 List<Map> 바인딩). 3 dataset_id assertions → 단일 "literal dataList regardless of entity" 로 통합, 4 row-type assertions → `_RowType_` Col + 의미 보존 새 이름(`_has_rowtype_col`, `_preserves_order`). 회귀: live_crud_nexacro 21/21 + **전체 tests/ 349/349 그린 (rc=0 회복)** → R3 hint 가 진짜 fail signal 위에서 작동할 환경 정비 완료. §0 creater row "깊이 누적 위치" 에 list-domains 슬래시·스크립트 추가, trap 카운트 변동 없음. |
| Growth-50 | 2026-05-26 | **vanilla L4-probe 라이브 검증 + T-Probe-CtxPath-Missing 픽스** (라벨: **풀테스트 그린 → javax-host 검증** — 4계층 전부 PASS, vanilla lane 첫 REST L4 종단검증 그린). `customer` 도메인 fresh scaffold(`--lane vanilla --dialect hsqldb`, Stage 4 nexacro form gen 우회 `--skip-stage 4` 사용 — "unsupported version: 2") → `out/customer-growth50-vanilla` → `full_test.py vanilla` 첫 시도에서 L4 probe FAIL(http=404 at `/api/address`). **근본 원인 추적**: (1) componentScan 가설 → live_overlay `_edit_application_java` 가 `scanBasePackages = {"com.nexacro.uiadapter", "com.example.customer"}` + `@MapperScan` 정상 주입 확인(이전 단계 cleanup이 git restore 로 revert 했던 일시적 문제), (2) 재배포 후에도 `/api/address` 404 유지 → `/uiadapter/api/address` 직접 호출 시 200 + 행 3건 → **lane_probe_url REST 분기의 컨텍스트 경로 누락 확정**. **환류**: `lane_runner_map.lane_probe_url` REST 분기 한 줄 수정(`/api/{entity}` → `/uiadapter/api/{entity}`) — nexacro envelope 은 항상 `/uiadapter` prefix 가졌으나 REST 는 미포함. 모든 nexacroN-fullstack runner(boot/mvc × javax/jakarta × plain/egov4/egov5) `application.yml` 에 `server.servlet.context-path: /uiadapter` 통일. **숨겨진 이유**: 기존 jakarta/javax 종단검증은 모두 envelope CRUD 로 수행(scaffold lane=nexacro), vanilla 가 첫 순수 REST L4-probe 케이스. **회귀 픽스**: `test_lane_runner_map::test_lane_probe_url_rest_uses_api_path`(REST 3-lane parametrize) + `test_full_test::test_run_l4_live_rest_lane_uses_json_probe` 2개 assertion 갱신 → 64/64 그린(workflow + lane_runner_map 결합). **4계층 결과**: L1 4-repo pytest rc=0 · L2 schema 12 + seed 12 stmt OK · L3 SKIP=PASS(runner-provides-pom) · L4 overlay 26w/2e + mvn rc=0 + REST probe http=200 errcode=0 rows=3 + REST CRUD baseline=3→+1=4→final=3. §0 creater row trap 3→4 + §1 vanilla row `⚠️/✅(L3까지)` → `✅ Growth-50(풀테스트 그린)` + §4 T-Probe-CtxPath-Missing 트랩 신규 등재. cleanup 잡음: `Remove untracked mappers` PowerShell quoting FAIL(Growth-49 미해결 환류 동일) + `learn-log auto-label skipped Growth-49 not found` (Growth-50 row 가 이 커밋 이전 시점이라 자동 라벨러 skip — 정상). |
| Growth-53 | 2026-05-26 | **서비스 리뷰 #4 흡수 — cross-layer coherence hook을 /diagnose 6번째 카테고리로 통합 (α+δ 하이브리드)**. 별도 `/coherence` 명령(γ) 또는 PostToolUse hook(β) 대신 기존 `/diagnose` 확장(α) — 진입 체인(orient→diagnose→list→scaffold→full-test) 의 단순함 보존, 자산 노출형 하네스 패턴 일관 유지. **신규 `check_cross_layer_coherence(workspace, creater_root)`**: §4 트랩 이력에서 도출한 **4건 정적 회귀 가드**, grep-level 검증이라 fresh scaffold 없이 수십 ms 내 — (a) **G-47 (T-NexacroUiaPkg-javax)**: `andrej-karpathy-rdb-mybatis/.claude/skills/karpathy-rdb-mybatis/templates/controller/controller.java.j2` + `service/service-impl.java.j2` 가 `{{ uia_namespace }}` parametrize 유지(.jakarta.core. 하드코딩 회귀 차단), (b) **G-50a (T-Probe-CtxPath-Missing, runner-side)**: `nexacroN-fullstack/samples/runners/*/src/main/resources/application.yml` 들이 `context-path: /uiadapter` 일관 유지, (c) **G-50b (T-Probe-CtxPath-Missing, dispatcher-side)**: `scripts/workflow/lane_runner_map.py` 가 `/uiadapter/api/` prefix 유지, (d) **G-48 (T-Probe-LaneRunner-Mismatch)**: `scripts/workflow/full_test.py` 가 `discover_scaffold_lane` 호출 + `scaffold_lane=` dispatch wiring 유지. 회귀 발생 시 단일 Check 행 FAIL + detail 에 위반 항목 나열 + hint `learn-log §4 트랩 회귀 — 해당 Growth commit (G-47/48/50) 추적 후 복원`. `run_all_checks` 가 `runner` 와 `jdk` 사이에 wire. **G-42 (mapper.xml.j2 snake_case)**는 정적 검증이 약해 drop — 템플릿 매크로 변환 흐름 분석이 필요. 신규 5 tests(PASS + G-47/50a/50b/48 각 FAIL 케이스). **회귀**: workflow suite 27/27 그린(diagnose 22/22 + orient 13/13에서 누락 없음). **δ pure-convention fallback**: 새 트랩 발견 시 §4 등재 + 회귀 테스트 작성(현행 컨벤션) → 다음 Growth 에서 5번째/6번째 sub-check 으로 승격. 정적 규칙의 한계는 컨벤션으로 보완. **자산 노출형 하네스 패턴 완성**: orient(진입) → diagnose(사전 환경 + cross-layer 정합성) → full-test(실행) → recovery_hint(사후) 가 4-point 자산 노출 체인. §0 creater row "깊이 누적 위치" 의 diagnose 슬래시에 `[+cross-layer-coherence Growth-53]` 표기. trap 카운트 변동 없음(read-only 가드). |
| Growth-52 | 2026-05-26 | **서비스 리뷰 1+2순위 — pre-flight 진단 + newcomer 진입 화면 (자산 노출형 하네스 패턴, agent 신설 없음)**. **`/diagnose` (1순위)**: `/full-test` 진입 전 환경 조건 — R3 `recovery_hint` 가 실패 *후* 다음 명령을 안내한다면, `/diagnose` 는 실패 *전* 환경 조건을 본다. 5축 sibling repo 존재 / `presets/INDEX.md` 도메인 ≥10 / `learn-log.md` §0 Layer Ownership Card / `nexacroN-fullstack/samples/runners/{boot-jdk17-jakarta,boot-jdk8-javax}` 존재 + `com/example` stale overlay 부재 / `java -version` 성공(JDK17 권고, 비-17 WARN). 각 항목 PASS/WARN/FAIL + 회복 명령 1줄(R3 와 동일 형식). 비싼 빌드(mvn/pytest) 호출 금지 — 파일/PATH 존재만 본다(수초 내). `--json` 구조화 출력. exit 0(FAIL 없음) / exit 1. 신규 `scripts/workflow/diagnose.py` (Check dataclass + 5 check 함수 + run_all_checks + format_table + main) + `.claude/commands/diagnose.md` (frontmatter `business-fullstack-creater:diagnose`, argument-hint `[--json]`). 신규 17 tests(layer/preset/learn-log/runners/jdk 각 PASS/WARN/FAIL + format_table summary + main exit-code + JSON shape). 모듈-레벨 상수(`WORKSPACE`, `CREATER_ROOT`)는 keyword default 가 아닌 call-time 해결 — `run_all_checks(workspace=None, creater_root=None)` 가 None 일 때만 모듈 상수 읽어서 `monkeypatch.setattr(diagnose, "WORKSPACE", ...)` 가 동작하도록. **`/orient` (2순위)**: newcomer 진입 화면 — 클론 직후 사용자가 "여기서 무엇부터 만지면 되는지" 한 화면 파악. 별도 onboarding 문서 신설이 아니라 *이미 누적된 자산* 세 곳을 합성한다(`/list-domains` 와 동일한 자산 노출형 하네스 패턴). 소스: (a) `docs/USER-GUIDE.md` 첫 blockquote(프로젝트 한 줄 정의), (b) `learn-log.md` §0 Layer Ownership Card(5축 + 현재 누적 트랩), (c) `learn-log.md` §6 마지막 Growth(가장 최근 활동). §6 행이 시간순이 아닌 점 고려해 `extract_latest_growth` 가 `Growth-N` 의 N 최댓값 선택(범위 표기 `Growth-1 ~ Growth-25` 도 끝 숫자로 정렬). 출력 마지막에 "다음 명령" 블록(/diagnose, /list-domains, /scaffold, /full-test) — 진입 게이트가 항상 명시. 신규 `scripts/workflow/orient.py` (LayerRow/GrowthRow/Orientation dataclass + extract_pitch/extract_layer_card/extract_latest_growth + build_orientation + format_orientation + CLI) + `.claude/commands/orient.md`. 신규 13 tests(pitch 1줄 collapse + 다음 섹션 정지 + 최대-N 선택 + 범위 표기 처리 + 누락 파일 graceful + format 절단 + CLI smoke + 실 파일 drift smoke). **회귀**: 전체 tests/ 379/379 그린(이전 349 + diagnose 17 + orient 13). **`/full-test` 와의 관계**: `/diagnose` 가 사전 차단, `/full-test` 실패 시 `recovery_hint` 가 사후 안내 — 진입(orient)→사전(diagnose)→실행(full-test)→사후(recovery_hint) 가 하나의 자산-노출 체인. §0 creater row "깊이 누적 위치" 에 diagnose/orient 슬래시·스크립트 추가, trap 카운트 변동 없음(둘 다 read-only 노출 도구). **#4 (cross-layer coherence hook)** 은 별도 검토로 분리 — 흡수 방식 재논의 대기. |
| Growth-64 | 2026-05-28 | **Customer Profile Slice B-1 — `live_overlay` profile-aware base_package 발견** (Growth-63 §Verification 3·4 풀테스트 시 노출된 L4 차단 해소). Growth-63 Slice A 종료 후 `acme` profile 로 fresh scaffold(`python scripts/scaffold_cli.py --customer-profile acme --domain 고객관리 --slug customer --lane jakarta --dialect hsqldb --out ./out/acme-customer`) → Stage 1–4 산출물은 `com.acme.customer.*` 패키지로 정상 ACME 화(25 Java/XML 파일 grep 검증), but `/full-test jakarta out/acme-customer` 의 L4 가 `[L4] scaffold incomplete: could not derive domain slug from nexacro uiadapter package structure` 로 실패. **루트 원인**: `scripts/workflow/live_overlay.py` 가 5곳에서 `com/nexacro/uiadapter/<slug>` (또는 `com/example/<slug>`) 패키지 prefix 를 하드코딩 — Growth-50 시점 nexacro/example 2-소스만 가정한 설계. profile 이 도입한 임의 base_package(`com.acme`) 는 디렉터리 트리에 존재하지만 detection 로직이 무시. **환류** (단일 파일 refactor + 신규 helper): `live_overlay.OverlayPlan` dataclass 에 `base_package: str = "com.example"` 필드 추가. 신규 `derive_scaffold_base_and_slug(scaffold_dir) -> tuple[str, str] | None` — `3-mybatis/src/main/java/` 하위 `rglob("controller")` 로 `<base_pkg>/<slug>/controller/` 패턴 자동 발견(단일 candidate 일 때만 성공), 1차 detection 실패 시 legacy fallback(`com/nexacro/uiadapter`, `com/example` prefix 직접 검사 — 기존 fixture 회귀 보존). 기존 `derive_domain_slug(scaffold_dir)` 는 새 함수의 두 번째 요소만 반환하는 back-compat shim 으로 축소. `_find_java_src(scaffold_dir, slug, base_pkg=None)` 시그니처 확장 — `base_pkg` 주어지면 `src/main/java/<base_pkg parts>/<slug>/` 우선, 아니면 legacy prefix 순회. `discover_scaffold` 가 `derive_scaffold_base_and_slug` 호출 후 OverlayPlan 에 `base_package` 채워서 반환. `_edit_application_java(app_path, slug, base_pkg="com.example")` 시그니처 확장 — `scanBasePackages = {"com.nexacro.uiadapter", "com.acme.customer"}` 같이 동적 패키지 주입(이전에는 `"com.example.<slug>"` 하드코딩). `apply_overlay` 의 Java 트리 copy 가 `runner_dir.joinpath("src/main/java", *base_pkg.split("."), slug)` 로 base_pkg-aware 변경. **회귀 8 신규 테스트** (`tests/workflow/test_live_overlay.py`): `acme_scaffold` 픽스처(`3-mybatis/src/main/java/com/acme/customer/controller/CustomerController.java` 등 com.acme tree 미러), `test_derive_scaffold_base_and_slug_acme` → `("com.acme", "customer")`, `test_derive_scaffold_base_and_slug_nexacro` → `("com.nexacro.uiadapter", "sales")`, `test_derive_scaffold_base_and_slug_legacy_example` → `("com.example", "finance")`, `test_discover_scaffold_acme_plan_has_base_package`, `test_discover_scaffold_legacy_default_base_package`, `test_apply_overlay_acme_injects_com_acme_into_application` (`scanBasePackages` 문자열에 `"com.acme.customer"` 임베드 검증), `test_apply_overlay_acme_copies_java_into_com_acme_tree` (`runner/src/main/java/com/acme/customer/controller/CustomerController.java` 존재 검증), `test_apply_overlay_acme_idempotent` (2회 적용 시 멱등). **추가 잡음 픽스**: 기존 `test_discover_scaffold_prefers_stage3_sql_over_2ddl` 가 `controller/` 서브디렉터리 없는 `finance/Dummy.java` 만 fixture 에 두던 트랩 — 새 primary detection(`rglob("controller")`)이 0건 매치하던 문제를 legacy prefix fallback 으로 회복. **acme 풀테스트 검증** (2026-05-28, `out/acme-customer` scaffold, `boot-jdk17-jakarta` runner): L1 4-repo pytest rc=0 · L2 schema 12 + seed 12 stmt OK · L3 SKIP=PASS(runner-provides-pom) · L4 overlay 26 written/2 edited + mvn rebuild rc=0 + REST probe http=200 ErrorCode=0 rows=3 + REST CRUD baseline=3→+1=4→final=3 → **라벨 풀테스트 그린**. 첫 profile-driven scaffold 의 4-tier 종단검증 통과 — 6번째 축이 L4 실행면까지 일관 적용된다는 증명. **회귀**: test_live_overlay.py 26/26 + workflow 291/291 + 전체 464/464 그린 (Slice A baseline 440 + 24 신규/픽스). **자산 노출형 하네스 패턴 보전**: 새 의존성 0, 새 agent 0, 새 슬래시 0 — 기존 live_overlay 의 detection 표면만 profile 인지로 확장. trap 카운트 변동 없음(트랩 자체는 비공개 — Growth-63 §Verification 단계에서 발견 즉시 Growth-64 로 해소, §4 미등재). **Slice B 잔여 (Growth-65/66 대기)**: (1) 새 gap 필드 wiring(maven_group_id/maven_artifact_id/maven_version/table_prefix/frame/url_prefix/ds_username/ds_password/ds_url) `ScaffoldArgs` 추가 + `_run_stage{2,3,4,5}` sibling-repo CLI argument forwarding, (2) Stage 5 PASS 라벨링 직후 `profiles/<slug>.yaml` 의 `domains_seen` write-back, (3) G-63 가드(`version != 1` 거부 회귀 차단). |
| Growth-63 | 2026-05-28 | **6번째 축 `customer` 신규 도입 — Customer Profile Slice A**. 5축(skill/ddl/mybatis/nexacro/creater)에 사용 횟수만큼 자산이 누적되는 성장형 프로젝트에서, 같은 고객의 두 번째 도메인을 만들 때 패키지·URL·DB 자격증명 등 **고객 고유 관습**이 자동 적용되지 않아 매번 손으로 맞추던 복리 효과 단절 지점 해소. **신규 파일**: `profiles/_README.md`(스키마 설명·파일명 규칙 ASCII slug·우선순위 CLI>profile>default·`${ENV_VAR}` 비밀값 처리·복리 효과 표·원칙 위반 신호), `profiles/acme.yaml`(참고 예제 — version:1 + customer{slug:acme/display:ACME 코퍼레이션/status:active/contact} + ddl.dialect:postgres + mybatis{base_package:com.acme/project_root_pkg/uia_namespace:jakarta} + nexacro.default_pattern:D2 + overlay{target_pkg_prefix/shell_app_id:acme-portal} + auth{mode:session/lane:jakarta} + defaults{lane:jakarta/ui:nexacro} + datasource{username:${ACME_DB_USER}/password:${ACME_DB_PASS}/url_template/host/port/db} + 빈 domains_seen). **변경 표면**: `scripts/scaffold_cli.py` — `import os` + `_ENV_VAR_RE` regex 추가, `_interpolate_env(value)` / `load_customer_profile(slug, *, profiles_root=None)` / `_profile_get(profile, *path, default=None)` / `resolve_with_profile(cli_value, profile, *path, default=None)` 4 helper 신설. `load_customer_profile` 강제: 파일 존재 + root dict + `version == 1` + `customer.slug == filename`. `--customer-profile` argparse 플래그(dest=customer_profile) 신설. `--lane/--dialect/--target-package-prefix/--shell-app-id/--auth-mode/--auth-lane/--uia-namespace` 디폴트 None 으로 전환(call-time resolve). `--package` `required=True` 해제(profile 의 `mybatis.base_package` + slug 자동 조립). `main()` 이 profile 로드 → resolve_with_profile 로 각 knob 머지 → `ScaffoldArgs(customer_profile=profile)` 전달. `scripts/scaffold_orchestrator.py:ScaffoldArgs` 에 `customer_profile: Optional[dict] = None` 필드 추가. **G-62 cross-layer coherence guard 등재** (Growth-53 카테고리 7번째 sub-check): `scripts/workflow/diagnose.py:check_cross_layer_coherence` 가드 6→7건. G-62 가 `scripts/scaffold_cli.py` 본문에서 4개 마커(`def load_customer_profile(` / `"--customer-profile"` / `expected 1` / `customer_profile=`) 모두 잔존 검증. 한 줄이라도 회귀하면 단일 Check 행 FAIL. docstring Guards 절 G-62 항목 추가, FAIL hint `G-47/48/50/58/61` → `G-47/48/50/58/61/62`, PASS detail `6 trap guards intact (...)` → `7 trap guards intact (G-47/48/50a/50b/58/61/62)`. **회귀 14 신규 테스트** (`tests/test_scaffold_cli.py`): TestLoadCustomerProfile 9 케이스(None slug → None / 파일 부재 ValueError / version != 1 거부 / version 부재 거부 / slug mismatch 거부 / happy path dict 반환 / `${ENV_VAR}` interpolate / 누락 env literal 보존 / 비-mapping root 거부), TestResolveWithProfile 5 케이스(CLI 값 우선 / profile 값 fallback / 둘 다 부재 default / nested key 누락 default / CLI empty string 도 우선). `tests/workflow/test_diagnose.py`: `_make_workspace` 픽스처에 `scaffold_cli.py` 4-마커 stub 추가, PASS 단언 `G-47/48/50a/50b/58/61` → `G-47/48/50a/50b/58/61/62` 갱신, G-62 FAIL 2 케이스(마커 lost / 파일 부재) 신규. **회귀**: scaffold_cli 38/38 + diagnose 28/28 그린. **Slice 분리**: 본 Growth(A)는 schema + 머지 헬퍼 + 1차 wiring + G-62 가드만. Slice B(Growth-64 대기)는 (1) 새 gap 필드(maven_group_id/maven_artifact_id/maven_version/table_prefix/frame/url_prefix/ds_username/ds_password/ds_url) ScaffoldArgs 추가 + `_run_stage{2,3,4,5}` sibling-repo CLI argument forwarding, (2) Stage 5 PASS 라벨링 직후 `profiles/<slug>.yaml` 의 `domains_seen` write-back(중복 제거 + 정렬 + `${...}` placeholder 보존), (3) G-63 가드(`version != 1` 거부 회귀 차단). **누적 효과(복리 의도)**: 1st 도메인은 profile 작성 비용 발생; 2nd 부터 패키지·URL·DB·shell-id 자동 적용 손 댈 곳 없음; 3rd+ 부터 `domains_seen` 누적으로 G-61 가드 customer-tagged. **자산 노출형 하네스 패턴 보전**: 새 agent 0, 새 의존성 0(yaml 만 사용 — 기존 의존). 6번째 축이 catalog/preset 컨벤션(version:1 + per-stage sections + auto-accumulation 자리) 그대로 따름. **Overlay mode (①, target_project 자동 추출)**는 본 Growth 의 Out of Scope — 후속 Growth 로 분리(profile 자리잡은 후 "기존 프로젝트 디렉터리 → profile 자동 추출" 형태로). |
| Growth-62 | 2026-05-27 | **G-61 cross-layer coherence guard — Growth-53 카테고리 6번째 sub-check 등재**. Growth-61 이 T-Web-CatalogSlugMismatch 를 RESOLVED 했으나 단일 한 줄 픽스(`domain_slug=None`)가 회귀할 경우 fresh scaffold + `/web-build` 실행까지 가야 부분 placeholder 가 보이는 비대칭 — grep-feasible 정적 가드 추가 적격. Growth-60 패턴(모든 RESOLVED 트랩은 grep-가능하면 정적 회귀 가드로 등재) 적용. **변경 표면**: `scripts/workflow/diagnose.py:check_cross_layer_coherence` 가드 5→6건. G-61 sub-check 가 `scripts/workflow/web_index.py` 본문에서 (a) `domain_slug=None` 잔존 + (b) `domain_slug=entry.domain` 부재 양방향 검증 — 한쪽이라도 깨지면 단일 Check 행 FAIL. docstring Guards 절 G-61 항목 추가, FAIL hint `G-47/48/50/58` → `G-47/48/50/58/61`, PASS detail `5 trap guards intact (G-47/48/50a/50b/58)` → `6 trap guards intact (G-47/48/50a/50b/58/61)`. **회귀 2 신규 테스트** (`tests/workflow/test_diagnose.py`): (1) `test_check_cross_layer_coherence_fail_g61_web_index_slug_regression` — `web_index.py` 가 `domain_slug=entry.domain` (broken 패턴) 잔존 시 FAIL + detail "G-61" 검증, (2) `test_check_cross_layer_coherence_fail_g61_missing_web_index` — 파일 자체 부재 시 "G-61 guard" 메시지 검증. 기존 `test_check_cross_layer_coherence_pass` PASS detail 단언 `G-47/48/50a/50b/58` → `G-47/48/50a/50b/58/61` 갱신. 픽스처 `_make_workspace` 에 `web_index.py` 2줄 stub 추가(`def _default_source_resolver(entry): plan = discover_scaffold(scaffold_root, domain_slug=None)`). **회귀**: tests/workflow 279→**281 그린** + tests/ 159 그린 = **440 그린** (G-61 의 438 에서 +2). 수정 표면: creater 단일 파일 `diagnose.py` 가드 1건 신설, sibling repo 무손상. T-Web-CatalogSlugMismatch 가 정적 회귀 가드 망에 편입 — G-61 픽스가 침범당하면 `/diagnose` 6번째 sub-check 가 즉시 노출. **자산 노출형 하네스 패턴 보전**: orient → diagnose(6 guards) → list → scaffold → full-test → recovery_hint 체인 유지. trap 카운트 변동 없음(read-only 가드). |
| Growth-61 | 2026-05-26 | **T-Web-CatalogSlugMismatch RESOLVED — web_index 한글 catalog × ASCII Java slug 부정합 픽스 + 실 도메인 portal 자산 검증**. Growth-55/56/57 의 portal 흐름을 외부 사용자 관점에서 처음으로 실제 자산을 채워 검증한 결과 발견 — `~/.karpathy-rdb/catalog/고객관리/` 를 실 scaffold 산출물로 초기화(`python scripts/scaffold_cli.py --domain 고객관리 --slug customer --package com.example.customer --lane jakarta --dialect hsqldb --wiki-mode preset --preset 고객관리`)한 뒤 `/web-build` 실행 → CLI 가 `WARNING: 고객관리/jakarta/Controller.java: source not found; using placeholder` + 동일한 Service.java warning 출력. DDL/Mapper preview 는 실제 코드 표시되는데 Controller/Service 만 placeholder 잔존하는 비대칭 부분 누락. **루트 원인**: `scripts/workflow/web_index.py:_default_source_resolver` 가 `discover_scaffold(scaffold_root, domain_slug=entry.domain)` 호출 — `entry.domain` 은 `MatrixEntry` build 시점에 catalog 한글 서브디렉터리명(`고객관리`) 으로 채워지나, scaffold Java 패키지는 `--slug customer --package com.example.customer` 로 ASCII 사용. `discover_scaffold` 의 Controller/Service 분기는 `com.example.<slug>/controller/*Controller.java` glob → 한글 slug 로는 매치 0건. DDL(`schema.sql`) / Mapper(`mybatis/mappers/*.xml`) 는 slug-agnostic glob 이라 resolve. **환류**: `domain_slug=entry.domain` → `domain_slug=None` 1줄 교체. `derive_domain_slug()` 가 scaffold tree(`com.nexacro.uiadapter.<slug>` / `com.example.<slug>`) 에서 ASCII slug 자동 추론 — Java 디렉터리 구조가 진실원천. 인라인 주석으로 트랩 컨텍스트 잔존. **회귀 2 신규 테스트** (`tests/workflow/test_web_index_scaffold.py`): (1) `_make_scaffold_tree` 헬퍼 — `/scaffold` 출력 구조(schema.sql + mybatis/mappers/<slug>-mapper.xml + com/example/<slug>/{controller,service}/<Pascal>{Controller,Service}.java) 미러링 fixture, (2) `test_default_source_resolver_handles_korean_catalog_with_ascii_slug` — 한글 catalog 서브디렉터리 + ASCII Java slug 환경에서 4 키 모두 실제 Path resolve 검증, (3) `test_default_source_resolver_returns_none_when_catalog_missing` — 카탈로그 디렉터리 부재 시 `discover_scaffold` 의 예외가 graceful catch 되어 4 키 None dict 반환. **검증**: 픽스 후 `python -m scripts.workflow.web_index --domain 고객관리` 재실행 → Controller/Service placeholder warning 0건, 생성된 `docs/domain/%EA%B3%A0%EA%B0%9D%EA%B4%80%EB%A6%AC.html` 의 Controller preview 블록에 `package com.example.customer.controller;` 실 코드 표시. **회귀**: tests/workflow 277→**279 그린** + tests/ 159 그린 = **438/438 그린** (G-60 의 436 에서 +2). **§4 T-Web-CatalogSlugMismatch 트랩 신규 등재** + §0 creater row trap 7→8 + web_index.py 깊이 누적 위치에 `+catalog-slug-mismatch fix Growth-61` 마커. **자산 노출형 하네스 패턴 보전**: 변경 표면 `web_index.py` 단일 함수 1줄 + 회귀 테스트 — 외부 의존성/구조 변경 0. **검증 의의**: Growth-55 portal 출시 이후 처음으로 실 도메인 자산(`~/.karpathy-rdb/catalog/고객관리/`)을 채워 4-tab preview 가 placeholder 가 아닌 실 코드를 표시함을 확인 — Growth-56 verification matrix 의 "콘텐츠 PARTIAL → COMPLETE" 전환. |
| Growth-60 | 2026-05-26 | **G-58 cross-layer coherence guard — Growth-53 카테고리 5번째 sub-check 등재**. Growth-58 이 vanilla Stage 4 자동 skip 으로 T-Stage4-VanillaReject 를 RESOLVED 했지만, 그 단일 게이트가 사라지면 vanilla `/scaffold` 가 다시 N002 unsupported version 으로 깨짐 — 회귀 검출이 fresh scaffold 까지 가야 보이는 비대칭. Growth-53 `check_cross_layer_coherence` (5축 정적 회귀 가드) 에 5번째 sub-check 추가: `scripts/scaffold_orchestrator.py` 본문에 `args.lane == "vanilla"` 분기 + `stage4-skipped-vanilla` 마커 둘 다 잔존 — 한 줄이라도 사라지면 단일 Check 행 FAIL 로 즉시 검출. **변경 표면**: `scripts/workflow/diagnose.py:check_cross_layer_coherence` 가드 4→5건, docstring 의 Guards 절 G-58 항목 추가, FAIL hint `G-47/48/50` → `G-47/48/50/58`, PASS detail `4 trap guards intact (G-47/48/50a/50b)` → `5 trap guards intact (G-47/48/50a/50b/58)`. **회귀 2 신규 테스트** (`tests/workflow/test_diagnose.py`): (1) `test_check_cross_layer_coherence_fail_g58_vanilla_skip_lost` — `scaffold_orchestrator.py` 가 `args.lane == "vanilla"` 분기 + `stage4-skipped-vanilla` 마커 부재 시 FAIL + detail "G-58" 검증, (2) `test_check_cross_layer_coherence_fail_g58_missing_orchestrator` — 파일 자체 부재 시 "G-58 guard" 메시지 검증. 기존 `test_check_cross_layer_coherence_pass` PASS detail 단언 갱신(`G-47/48/50a/50b` → `G-47/48/50a/50b/58`). 픽스처 (`_make_workspace`) 에 `scaffold_orchestrator.py` 4줄 stub 추가(vanilla 분기 + stage4-skipped-vanilla append). **회귀**: tests/workflow 275→**277 그린** + tests/ 159 그린 = **436 그린** (G-59 의 434 에서 +2). 수정 표면: creater 단일 파일 `diagnose.py` 가드 1건 신설, sibling repo 무손상. Stage 4 vanilla 게이트가 정적 회귀 가드 망에 편입 — G-58 픽스가 침범당하면 `/diagnose` 5번째 sub-check 가 즉시 노출. **자산 노출형 하네스 패턴 보전**: orient → diagnose(5 guards) → list → scaffold → full-test → recovery_hint 체인 유지. trap 카운트 변동 없음(read-only 가드). |
| Growth-59 | 2026-05-26 | **lane-aware `--ui` default — vanilla→react, 기타→nexacro** (Growth-58 다음 후보 해소). Growth-58 entry 가 "vanilla + Stage 5 overlay 조합 — 현 `--ui nexacro` 디폴트가 vanilla 와 부조화, `--ui react` 가 자연 선택" 으로 관찰만 했던 UX 친화 갭. 트랩 등재 없이 read-only 디폴트 정책 변경. **환류**: `scripts/scaffold_cli.py` `--ui` argparse `default="nexacro"` → `default=None`. 신규 `resolve_ui_default(ui, lane)` 순수 함수 — 명시 `--ui` 가 항상 우선, omit 시 `vanilla → "react"` / `기타 → "nexacro"`. `main()` 에서 `ui_resolved = resolve_ui_default(a.ui, a.lane)` 후 `ScaffoldArgs(ui=ui_resolved)` 전달, omit 시 `INFO: --ui not specified; defaulting to '<x>' for --lane <lane>` stderr 알림(R1 slug INFO 메시지 스타일 통일). **회귀 6 신규 테스트**(`tests/test_scaffold_cli.py::TestResolveUiDefault`): vanilla→react / nexacro→nexacro / jakarta→nexacro / javax→nexacro / 명시 `nexacro` 가 vanilla 디폴트 override / 명시 `react` 가 nexacro 디폴트 override. **회귀**: tests/ 428→**434 그린** (6 신규, 0 regression). 수정 표면: `scaffold_cli.py` 단일 파일 — sibling repo / orchestrator 무손상. trap 카운트 변동 없음(정책 개선 read-only). 자산 노출형 하네스 패턴 보전: lane 의도 → overlay 자동 선택, 사용자가 lane×ui 조합 학습 부담 0. |
| Growth-58 | 2026-05-26 | **T-Stage4-VanillaReject RESOLVED — vanilla lane Stage 4 자동 skip (Growth-50 워크어라운드 정리)**. Growth-50 vanilla L4-probe 검증 시 발견: `andrej-karpathy-rdb-mybatis/scripts/endpoints_emitter.py` 가 `lane="vanilla"` 일 때 `{"version": 2, "lane": "vanilla", "entities": [{"endpoints": [{"method": "GET", "http_path": "..."}, {"method": "POST", ...}]}, ...]}` REST-flavored 산출(envelope 가 아닌 GET/POST), 한편 `andrej-karpathy-rdb-nexacro/scripts/endpoints_loader.py` 는 `data.get("version") != 1` 즉시 `N002 unsupported version: 2` raise. Stage 4 는 nexacro XFDL form 생성기 — vanilla(순수 REST) 에 만들 form 자체가 없는 게 설계 의도 일치. Growth-50 은 `--stop-after-stage 3` 수동 우회로 진행. **환류** (단일 파일, A안 채택 — auto-skip in orchestrator): `scripts/scaffold_orchestrator.py:_run_stage4` 진입 즉시 `if args.lane == "vanilla":` 분기 → `print("[stage4] skipped: vanilla lane is pure REST — no nexacro XFDL forms to generate", file=sys.stderr)` + `report.stages_run.append("stage4-skipped-vanilla")` early-return (subprocess 미호출, `4-nexacro/` 디렉토리 생성 안 함). `_write_report` 의 stage-order tuple `("stage1", "stage2", "stage3", "stage4", "stage4-skipped-vanilla", "stage5", "stage5-skipped")` 으로 확장, scaffold-report.md 에 `- stage4: SKIPPED (vanilla lane has no nexacro XFDL forms)` 표기. **회귀 3 신규 테스트** (`tests/test_scaffold_orchestrator.py`): (1) `test_stage4_auto_skips_for_vanilla_lane` — fake stage4 가 `sys.exit(1)` 으로 즉시 실패하도록 설계 → 호출되면 무조건 빌드 깨짐 → `report.stages_run == ["stage1", "stage2", "stage3", "stage4-skipped-vanilla"]` + `not (out / "4-nexacro").exists()` + capsys.err 에 "stage4" + "vanilla" 검증, (2) `test_stage4_runs_for_non_vanilla_lanes` — 3 lane(nexacro/jakarta/javax) 파라메트라이즈 회귀 가드: 각각 `report.stages_run` 에 `"stage4"` 포함 + `"stage4-skipped-vanilla"` 부재 + `4-nexacro/argv.txt` 존재(form_gen.py 정상 invoke), (3) `test_scaffold_report_records_vanilla_skip` — scaffold-report.md 에 SKIPPED 마커 문자열 정확 매치. **회귀**: tests/ 425→**428 그린** (3 신규, 0 regression). **수정 표면**: creater orchestrator 단일 파일 — sibling repo (rdb-nexacro/rdb-mybatis) 무손상, schema 호환성 변경 0, 신규 의존성 0. **§4 T-Stage4-VanillaReject 트랩 행 신규 등재**(Growth-50 발견 → Growth-58 RESOLVED), §0 creater row trap 6→7. **자산 노출형 하네스 패턴 보전**: 사용자가 `/scaffold ... --lane vanilla` 단순 실행 시 stage 4 워크어라운드 학습 부담 0 — orchestrator 가 lane 의도를 읽고 자동 분기. **다음 후보**: vanilla + Stage 5 overlay 조합(현 `--ui nexacro` 디폴트가 vanilla 와 부조화, `--ui react` 가 자연 선택) — 별도 트랩으로 관찰. |
| Growth-57 | 2026-05-26 | **T-Web-EmptyPortal RESOLVED — placeholder UX 이중 보강 (b+a 동시 적용)**. Growth-56 발견 트랩(`/web-build` 단독 실행 시 `~/.karpathy-rdb/catalog/` 미초기화로 14×4=56 슬롯 모두 `// not available — generator could not find source` placeholder fallback) 의 portal 첫인상 빈약 문제를 **두 표면에서 동시에** 보강. **(b) Inline 힌트** — `scripts/workflow/web_index.py:_PLACEHOLDER` flat 상수 제거 → `_PREVIEW_LAYER_LABELS` dict + `_format_placeholder(slug, fname)` 함수 신설. 각 placeholder 본문이 layer label("DDL (Stage 2 — andrej-karpathy-rdb-ddl)" / "MyBatis Mapper (Stage 3 — andrej-karpathy-rdb-mybatis)" / "Spring Controller (Stage 3 — ...)" / "Spring Service (Stage 3 — ...)") + ``run `/scaffold {slug}` first to populate this preview.`` + `~/.karpathy-rdb/catalog/{slug}/` 경로를 inline 임베드. `materialize_scaffold` 2 callsite(DDL.sql, 3 Java/XML 파일)이 `_format_placeholder(slug, fname)` 호출. **(a) CLI 배너** — `BuildResult` dataclass 에 3 필드 추가(`placeholder_slots: int = 0` / `total_slots: int = 0` / `catalog_root_missing: bool = False`). `build()` 함수 진입 직후 `Path.home() / ".karpathy-rdb" / "catalog"` 존재 여부를 `catalog_root_missing` 에 기록(later 단계 short-circuit 에도 살아남도록). `materialize_scaffold` 결과를 받은 후 `result.placeholder_slots += sum(1 for w in warns if w.endswith("using placeholder"))` + `result.total_slots += len(_PREVIEW_FILE_KEYS)`. `main()` 종료 직전 stderr 배너: `⚠ EMPTY-PORTAL: 56/56 preview slots filled with placeholder (no catalog found at ~/.karpathy-rdb/catalog/). Run /scaffold <domain> first to expose real DDL/Mapper/Controller/Service content.` (`placeholder_slots == total_slots` → "EMPTY-PORTAL", 부분 채움이면 "PARTIAL-PORTAL"). **추가 픽스 (Growth-52 lesson 재적용)**: `build(docs_root: Path = DOCS_ROOT, learn_log_path: Path = LEARN_LOG)` 키워드 디폴트가 def-time 바인딩 → 테스트 `monkeypatch.setattr(web_index, "DOCS_ROOT", ...)` 무효 + 실 docs/ 오염. 시그니처를 `Optional[Path] = None` 으로 변경 후 함수 본문 진입에서 `if docs_root is None: docs_root = DOCS_ROOT` 패턴(call-time 해석). `build_matrix` 도 동일 패턴 적용. **회귀 5 신규 테스트** (`test_web_index_scaffold.py`): (1) `_format_placeholder` 가 slug + `/scaffold` + `~/.karpathy-rdb/catalog/<slug>/` + "Stage 2" 임베드 검증, (2) layer label 이 파일명별 변동(DDL.sql ≠ Mapper.xml ≠ Controller.java ≠ Service.java), (3) `BuildResult.placeholder_slots / total_slots` 가 2 도메인 × 4 슬롯 = 8 정확 집계, (4) `Path.home` monkeypatch 로 빈 디렉토리 가리킬 때 `catalog_root_missing=True`, (5) `capsys` 로 `main()` stderr 에 "EMPTY-PORTAL" + "/scaffold <domain>" + "~/.karpathy-rdb/catalog/" + "4/4 preview slots" 모두 포함 검증. **CLI 스모크 검증**: `python -m scripts.workflow.web_index` (no filter) → `⚠ EMPTY-PORTAL: 56/56 preview slots filled with placeholder (no catalog found at ~/.karpathy-rdb/catalog/). Run /scaffold <domain> first to expose real DDL/Mapper/Controller/Service content.` 정확 출력 + 14 도메인 페이지 placeholder 본문에 새 inline 힌트 임베드 확인(예: `docs/domain/%EA%B3%A0%EA%B0%9D%EA%B4%80%EB%A6%AC.html` 의 DDL preview 블록이 ``// DDL (Stage 2 — andrej-karpathy-rdb-ddl) not available — generator could not find source\n//\n// run `/scaffold 고객관리` first to populate this preview.\n// Stage 2 (DDL) will produce files under\n// ~/.karpathy-rdb/catalog/고객관리/ that this portal reads from.`` 형태). **회귀**: workflow 12/12 + **전체 425/425 그린** (420→425 +5 신규). §4 T-Web-EmptyPortal 행 "Growth-56 발견 → **RESOLVED Growth-57**" 갱신 + 환류 내용 (b+a) 기술. §0 creater row 미해결 환류 컬럼 `T-Web-EmptyPortal (Growth-57 후보)` → `—` (RESOLVED 라벨 trap 행에 등재). **자산 노출형 하네스 패턴 보전**: 새 agent 0, 새 의존성 0, 새 명령 0 — 기존 `/web-build` 출력 채널만 두 표면(inline + CLI) 에서 더 친절해짐. trap 카운트 변동 없음(트랩 행에 RESOLVED 라벨 추가만). |
| Growth-56 | 2026-05-26 | **Web v0 라이브 검증 + T-Web-EmptyPortal 트랩 등재** (Growth-55 산출물 외부 사용자 관점 verification). 5-item verification matrix: (1) **14-tile grid PASS** — `docs/index.html` 23-92 라인에 14 tile + `data-domain` 속성 percent-encoded 정상 + chip-row `default-unverified jakarta` 일관, (2) **Korean slug URL PASS** — `docs/domain/*.html` 14개 파일 모두 `urllib.parse.quote` percent-encoded(고객관리→`%EA%B3%A0%EA%B0%9D%EA%B4%80%EB%A6%AC.html`, 결재→`%EA%B2%B0%EC%9E%AC.html`, ...) 생성 + 도메인 페이지 breadcrumb `<a href="../index.html">Home</a>` → 디코드된 한글 표시, (3) **4-tab preview 구조 PASS / 콘텐츠 PARTIAL** — tab-bar `role="tablist"` 4 button(DDL/Mapper/Controller/Service) + `<pre class="preview-block" data-tab=... data-lane="jakarta" hidden>` 4 블록 구조 OK, but **콘텐츠가 14 도메인 × 4 layer = 56 슬롯 모두 `// not available — generator could not find source` placeholder** (별도 트랩 — 본 검증과는 분리), (4) **L2 zip 무결성 PASS** — `python -c "zipfile.ZipFile(...).testzip()"` 14개 도메인 zip 전부 VALID + 내부 4 파일 (Controller.java/DDL.sql/Mapper.xml/Service.java) namelist 검증, but 각 파일 54B (placeholder stub), (5) **L3 copy-paste snippet PASS** — `cd %EA%B3%A0%EA%B0%9D%EA%B4%80%EB%A6%AC-scaffold &amp;&amp; mvn spring-boot:run` HTML-escape 정상(`&amp;&amp;` 가 브라우저 렌더 시 `&&` 디코드). **루트 원인 추적**: `scripts/workflow/web_index.py:240 _default_source_resolver` 가 `~/.karpathy-rdb/catalog/<domain>` 에서 Stage-2 DDL 산출물을 탐색하는 설계인데, 현재 환경에서 `Path.home() / ".karpathy-rdb" / "catalog"` **부재** → `discover_scaffold` 가 4개 키 모두 None 반환 → `materialize_scaffold` 의 placeholder fallback 분기(line 228 `_PLACEHOLDER = "// not available..."`)가 56 슬롯 모두 트리거. **트랩 본질**: `/web-build` 단독 실행 시 `/scaffold <domain>` 선행 없으면 portal 골격은 정상 렌더되지만 노출 자산이 0(`graceful degradation` 자체는 설계 의도였으나 UX 첫인상 빈약). **신규 §4 트랩 등재**: **T-Web-EmptyPortal** (Growth-56 발견, Growth-57 후보) — 회피책 두 가지 후보 (a) `/web-build` 출력에 `/scaffold` 선행 안내 추가, (b) placeholder 본문에 `"run /scaffold {domain} first to populate"` 힌트 inline. **§0 creater row trap 5→6** (T-Web-EmptyPortal 등재) + 미해결 환류 컬럼에 `T-Web-EmptyPortal (Growth-57 후보)` 채움. 회귀: 코드 변경 0 — 기존 portal 산출물에 대한 사용자-perspective verification 만 수행, tests/ 변경 0(420/420 그린 유지). **자산 노출형 하네스 패턴 검증**: portal 의 구조적 노출 표면(14-tile grid · Korean slug · 4-tab preview · L2 zip · L3 snippet) 5개 항목 모두 첫인상 통과 — 콘텐츠 파이프 endpoint 만 환경 의존성 미스. |
| Growth-55 | 2026-05-26 | **Web v0 정적 포털 출시 — 세 번째 asset-exposure 채널 (`/web-build`)**. CLI(`/scaffold`·`/full-test`) + Markdown(USER-GUIDE·learn-log) 에 이어 외부 사용자가 클론 없이 도메인 자산을 둘러볼 수 있는 정적 HTML 포털. **5 milestone 누적** (M0 spec lockdown · M1 matrix builder · M2 scaffold pre-generator · M3 HTML/CSS/JS rendering · M4 `/contribute-back` tail hook + `/web-build` 슬래시). **신규 파일**: `scripts/workflow/web_index.py` (347 LoC — MatrixEntry/BuildResult dataclass + build_matrix + materialize_scaffold + render_index_html + render_domain_html + embedded STYLE_CSS/PREVIEW_JS + CLI `--domain/--check/--json`), `docs/superpowers/specs/2026-05-26-web-v0-spec.md` (423 LoC 10 절 — goal/non-goals/output contract/CLI/dataclass/HTML skeleton/CSS-JS/integration/goal-check/out-of-scope), `.claude/commands/web-build.md` (frontmatter `business-fullstack-creater:web-build`, argument-hint `[--domain <slug>] [--check] [--json]`), 4 신규 test 파일(test_web_index_matrix.py 10 + test_web_index_scaffold.py 7 + test_web_index_render.py 10 + test_contribute_back_web_hook.py 4 = **31 신규**). **`/contribute-back` 연동**: `_run_web_hook()` helper 가 run() 종료 시 non-blocking 으로 `web_index.build()` 호출 — 실패해도 전체 환류 실패로 이어지지 않음(stderr 보고 + 예외 swallow). **생성 산출물**: `docs/index.html` (14 도메인 tile grid 포털), `docs/domain/<slug>.html` × 14 (한글 slug 는 `urllib.parse.quote` percent-encoding — 예: 고객관리 → %EA%B3%A0%EA%B0%9D%EA%B4%80%EB%A6%AC.html), `docs/assets/style.css` (4.9K), `docs/assets/preview.js` (3.4K). **L1+L2+L3 try-it 범위** 구현: L1 코드 미리보기 4 탭(DDL.sql / Mapper.xml / Controller.java / Service.java) · L2 zip 다운로드 · L3 복사실행 스니펫. **L4 (live sandbox)** = GitHub Codespaces deep-link 으로 v1 deferred. **OverlayPlan adapter 패턴**: `_default_source_resolver` 가 `live_overlay.discover_scaffold()` 의 OverlayPlan 출력(schema_sql / mapper_xml_dir / rglob *Controller.java / *Service.java) 을 4개 preview 키로 매핑 — catalog 부재 시 graceful skip (`~/.karpathy-rdb/catalog/` 미존재 → docs/scaffolds/ 빈 채로 정상 종료). **사전생성 매트릭스**: Verified(learn-log §1 검증대) + jakarta default per domain. HTML escape 적용(source 내 `<script>` 는 `&lt;script&gt;`). **회귀**: tests/ 389→**420 그린** (+31 신규, 0 regression). **자산 노출형 하네스 패턴 보전**: 새 agent 0, 외부 의존성 0, 새 런타임 0 — 기존 자산(learn-log §0/§1, list_domains, live_overlay) 의 또 다른 노출 표면일 뿐. §0 creater row "깊이 누적 위치" 에 `web-build` 슬래시 + `web_index` 스크립트 + `docs/` 정적 포털 추가, trap 카운트 변동 없음(read-only 노출 도구). |
| Growth-54 | 2026-05-26 | **미해결 환류 처리 — cleanup_runner.py PowerShell quoting trap 픽스 (T-Cleanup-PSSubexpr RESOLVED)**. §0 creater row 유일 미해결 환류(Growth-42 deferred·Growth-49/50 재현). **트랩 본질**: `cleanup_runner.plan_steps` 의 `Remove untracked mappers` Step 이 `Where-Object { (git -C ... ls-files --error-unmatch $_.FullName 2>$null; $LASTEXITCODE) -ne 0 }` 사용 — PowerShell ScriptBlock body 는 모든 expression 을 output stream 으로 집어넣어 git stdout + `$LASTEXITCODE` 가 array 가 됨 → `array -ne 0` 은 element-wise filter (boolean predicate 아님) → 모든 *-mapper.xml 이 untracked 판정되거나 누수, FAIL surface. **픽스**: PowerShell pipeline 폐기 → Python 측 단일 `git ls-files <mappers-dir>` + tracked 절대경로 set + `Path.unlink()`. `Step` dataclass 에 optional `python: Callable[[], (bool, str)]` hook 신설, `_execute_step` 이 `step.python` 있으면 우선 호출(없으면 기존 subprocess 분기). 기존 3 Step(positional `Step(label, command)`)은 dataclass field 재배치(`command: Optional[list[str]] = None`)로 호환. **신규 테스트 5건**: (a) 3-파일 fixture 에서 tracked 1건 보존 + untracked 2건 삭제, (b) all-tracked → 'all tracked' detail no-op, (c) 디렉터리 부재 → graceful PASS, (d) `git ls-files` rc=128 → ok=False + 파일 보존(destructive guard), (e) `plan_steps` 가 mapper step 에 `python is not None / command is None` wiring (정적 회귀 가드 — PowerShell pipeline 재도입 차단). **회귀**: tests/ 384→**389 그린** (5 신규). §0 creater trap 카운트 4→5 (RESOLVED 라벨 등재), 미해결 환류 컬럼 `—` 로 비움(0건). 작은 단일-파일 환류 — agent 신설/구조 변경 0, 자산 노출형 하네스 패턴 보전. |
| Growth-68 | 2026-05-28 | **mybatis `--url-prefix` sibling gap 해소 — Growth-67 known gap 클로즈**. Growth-67 에서 `ScaffoldArgs.url_prefix` 필드는 추가됐지만 `andrej-karpathy-rdb-mybatis/scripts/compile.py` 가 `--url-prefix` argparse 를 미지원해 stage3 forwarding 이 보류된 상태였다. **환류** (2파일 2레포): (A) `rdb-mybatis/scripts/compile.py` — `--url-prefix` argparse 추가(`default="/uiadapter"`), `context_path = "/uiadapter"` 하드코딩 2곳(dry-run 분기 line 115 + full codegen 분기 line 185) 을 `args.url_prefix` 로 교체. (B) `creater/scripts/scaffold_orchestrator.py` — `_run_stage3` 에 `if args.url_prefix is not None: cmd += ["--url-prefix", args.url_prefix]` 추가(table_prefix 패턴 미러), `ScaffoldArgs.url_prefix` 필드 주석의 "known gap" 문구 제거. **테스트**: mybatis 4 신규(dry-run default=`/uiadapter` / dry-run override `/api/v1` / full codegen override `/services` / vanilla lane 무영향) → 94/94 그린. creater 2 신규(url_prefix=/api/v1 forward 검증 / url_prefix=None 미전달 검증) → 377/377 그린. diagnose 8 guards intact. 하드코딩 사이트 2건 모두 파라미터화 완료. |
| Growth-67 | 2026-05-28 | **Customer Profile Slice B-3 — 9 gap field wiring 완료**. `ScaffoldArgs` 에 `table_prefix / frame / maven_group_id / maven_artifact_id / maven_version / ds_username / ds_password / ds_url / url_prefix` 9 필드 추가. `_run_stage3`: `--table-prefix` 조건부 forward (mybatis compile.py 가 수락). `_run_stage4`: `--frame` 조건부 forward (form_gen.py 가 수락). `_run_stage5`: shell + non-shell 두 경로 모두 `maven_* / ds_*` 를 `run_overlay` 에 명시 전달 (shell path 의 `getattr` fallback 을 명시 필드로 교체). `stage5_overlay._nexacro_overlay_run` 에 `**_unused` 추가 — nexacro domain overlay 는 pom.xml 미산출, 시그니처 parity 확보. `nexacro_shell_overlay._resolve_datasource(dialect, domain_slug, ds_username=None, ds_password=None, ds_url=None)` 시그니처 확장 — None 이 아닌 override 가 `_DIALECT_DATASOURCES` 기본값보다 우선. `_shell_overlay_run` 도 동일 3 파라미터 수용. `scaffold_cli._build_parser`: 9개 argparse 옵션 신설(모두 `default=None`). `main()` profile merge 블록에 `resolve_with_profile` 9 호출 추가 → `ScaffoldArgs` 에 전달. `profiles/acme.yaml`: 9 YAML 경로 픽스처 추가(mybatis.table_prefix/url_prefix, nexacro.frame, overlay.maven.*, overlay.datasource.*). **회귀 24 신규 테스트**: TestGrowth67GapFields 15(scaffold_cli — profile > default, CLI > profile 각 필드), 5(orchestrator — stage3 table_prefix forward/omit, stage4 frame forward/omit, stage5 non-shell maven+ds mock capture), 4(nexacro_shell_overlay — ds_username/ds_password/ds_url override + None fallback). **`url_prefix` known gap**: mybatis compile.py 가 `--url-prefix` argparse 미지원 → stage3 forwarding 보류, `ScaffoldArgs.url_prefix` 필드만 보관(미래 Growth forward 대기). **전체 503/503 그린** (Growth-66 기준 +24 신규). diagnose 8 guards intact. |
| Growth-47 | 2026-05-22 | **javax fresh scaffold 종단 검증 + T-NexacroUiaPkg-javax 픽스** (라벨: **라이브 WAS 부분검증** — L1+L2+L4-빌드 PASS, L4-probe FAIL). `customer` 도메인 fresh scaffold(`lane=nexacro` + `dialect=hsqldb` + `--uia-namespace spring` + boot-jdk8-javax 러너 타깃) 시도 중 L4 `mvn package` 가 `ParamDataSet/NexacroResult/NexacroException` 심볼 미발견으로 컴파일 실패. **원인**: mybatis `controller.java.j2` + `service-impl.java.j2` 가 `com.nexacro.uiadapter.jakarta.core.*` 하드코딩 → boot-jdk8-javax 러너의 uiadapter 라이브러리는 `.spring.core.*` 변종(Spring 5/javax.servlet). **6단계 환류** (A안 — 템플릿 수정 + 종단검증 채택, B fallback / C trap-only 거부): (1) `rdb-mybatis/scripts/precompute.py` `build_entity_context` 에 `uia_namespace: str = "jakarta"` 파라미터 + validation(`{jakarta,spring}` 외 ValueError) + 컨텍스트 dict 주입, (2) `codegen.py` `render_entity_files` 가 `uia_namespace` 를 receive → precompute 로 forward, (3) `compile.py` 가 `--uia-namespace {jakarta,spring}` argparse 추가, (4) `controller.java.j2` 가 3개 import 를 `{{ lib_prefix }}.{{ uia_namespace }}.core.*` 사용, (5) `service-impl.java.j2` 의 `DataSetRowTypeAccessor` import 도 동일, (6) `creater/scripts/scaffold_cli.py` + `scaffold_orchestrator.py` 가 `--uia-namespace` 노출하고 `_run_stage3` cmd 에 forward. **회귀 3 신규 테스트**(`test_codegen.py`): spring variant 4-import 검증 + jakarta default 미오염 검증 + invalid value ValueError → **12/12 그린** (rdb-mybatis). **L4 재빌드 rc=0** (검증). **잔여 L4-probe FAIL**: `lane_runner_map.lane_probe_kind` 가 `--lane javax`(runner 선택)만 보고 REST probe 사용 → 실제 endpoint(`/uiadapter/customer/select_datalist_map.do`, scaffold-side nexacro lane) 미스. 새 트랩 **T-Probe-LaneRunner-Mismatch** §4 등재(Growth-48 후보). G-Jackson 실 종단 클로저는 Growth-46 (board 도메인 hand-written BoardController) 가 이미 달성. |
| Growth-82 | 2026-05-29 | **M5 Slice C-e — `/target/upload` UX 폴리시: XHR progress bar + diff 뷰 + G-82 가드** (Growth-79 G-79 후속, "알려진 격차" 2건 해소). **변경 표면**: (a) `web/routes/target.py` — `import difflib` + `X-Requested-With` XHR 감지; XHR 모드 시 성공/오류 모두 `target_upload_partial.html` fragment 반환; 성공 시 `profiles/<slug>.yaml` 존재 확인 → `difflib.unified_diff` → `diff_lines`; `# Growth-82` 마커. (b) `web/templates/target_upload_partial.html` 신규 — base.html extend 없는 순수 HTML fragment; `diff_lines` 있으면 diff 컬러 렌더(`.diff-add`/`.diff-del`/`.diff-hunk`), 없으면 신규 프로필 `<pre>` + YAML 다운로드 링크. (c) `web/static/js/target_upload.js` 신규 — IIFE; XHR upload progress → `#upload-progress-bar` % 표시; `load` → `#extraction-result-area` innerHTML 교체; `error` → 오류 메시지. (d) `web/templates/target_upload.html` 수정 — js defer 스크립트 태그 + `#upload-progress` div + `#extraction-result-area` wrapper. (e) `web/static/css/main.css` — `.upload-progress`/`.upload-progress-bar` + `.yaml-diff`/`.diff-add`/`.diff-del`/`.diff-hunk` CSS 추가. **G-82 가드**: `diagnose.py:check_cross_layer_coherence` 20→**21건**. 7개 마커(`X-Requested-With`, `difflib`, `Growth-82` in target.py; `XMLHttpRequest` in js; `target_upload.js`+`upload-progress` in html; `extraction-result` in partial) 잔존 검증. PASS detail `21 trap guards intact (...G-81/82)`. 신규 테스트 +3(web) +4(diagnose). |

---

## 7. 사용 규칙

1. 새 Growth 시작 시 § 6 행 추가 (1줄)
2. lane/runner 검증 결과 변경 시 § 1 갱신
3. 신규 도메인/패턴/dialect/overlay 등장 시 해당 표 (§ 2 또는 § 3) 갱신
4. dialect/lane 트랩 발견 시 § 4 1행 + 환류 위치 명시
5. codegen 결함 발견 시 task 생성 → § 5에 task ID 인덱싱
6. 해결되면 § 5에서 줄긋고 commit SHA 첨부 (예: `~~task #240~~ → fixed in abc123def`)

CLAUDE.md 의 § 작업시체크리스트 3번 "wiki/learn-log.md 에 1줄 기록" 의 구체 위치가 **이 파일**이다.
